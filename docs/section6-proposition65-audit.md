# Proposition 6.5: frozen contract and assumption audit

Current status: the full frozen proposition and separate contract check are proved by the direct normalized-gap proof. See `section6-proposition65-normalized-gaps.md` for the completed argument, scope, and verification artifacts. The historical checkpoints below are retained; their unresolved-status claims are superseded. The initial hash check in the completed-proof run found a pre-existing mismatch only in the manuscript among the 47 snapshot files. No baseline was reset.

Final proof verification: full build passed (4331 jobs); all 63 new theorem types and transitive axiom reports passed the dedicated audit. The whole-Section-6 audit was regenerated, but 22 unrelated concurrent declarations are absent from aggregate imports (825 reports for 847 source declarations), so that audit and the historical validator are reported as failed. The manuscript remains the sole frozen-file mismatch. Full evidence is in `audit/section6-proposition65-validation.json`.

Source: `fixed_points_sampled_profile.tex`, Proposition `prop:sp-joint-insertion` (initially line 2371, now line 2341 after an external edit), the kernel definitions at lines 1980 onward, and Assumption `ass:simple-power-profile` (line 385). This is the joint local insertion law. The following material records the earlier development checkpoints.

## Model inputs and restrictions

The only model antecedents are `PowerProfile f left right` and `SampledRates grid w f`. Their complete field/type audit is in `section6-lemma64-contract.md`: positive continuous f on (0,1); left alpha>1 or finite positive limit; right beta>0 or finite positive limit; positive coefficients and relative power-error exponents; at least one active endpoint; exact sampled rates. Neither normalization nor a shell assumption is added. Weights contain strictly positive rates, already implied by f at the sample points. Both grids are supported. No assumed independence, integrability, concentration or local-limit estimate is supplied by an instance or structure.

Marked data: `s<=r`, `side : Fin s -> Corner`, and injective `u,j : Fin s -> Fin n`. Every selected side must be active, but different marks may use different sides. The natural label represents the manuscript one-based label by val+1. Corner distances are exactly val+1 on the left and n-val on the right. Conditions require both distances between h0 and delta*n; moderation uses real division and x_sigma <= min(a,h)^v. Rank separation is `2*r < Nat.dist (j e).val (j g).val` for e!=g, exactly |j_e-j_g|>2r since adding one to both labels does not change distance. There is no separation assumption on the source labels beyond injectivity.

The positive constants h0,delta,v,d,kappa,C are existential OUTPUTS before n,s,marks. They may depend on profile, grid and fixed r, as the source context permits. h0>=1 and delta<1 describe the chosen endpoint region. The proof may choose moderation exponent v sufficiently small; no uniform approximation for every v is asserted by the manuscript. No lower-source cutoff independent of these constructed parameters is assumed.

## Exact mathematical objects and conclusion

`Section6LocalKernelDefinitions` defines the exact q_R=Gamma(1+1/beta)^beta and q_L=Gamma(1-1/alpha)^(-alpha). `localCornerRatio` is x_R=(a/h)^beta or x_L=(h/a)^alpha; `localIdealKernel` is gamma*q*x/h*exp(-q*x); `localEnvelopeKernel` is x/h*exp(-d*x). Finite endpoint values are unused under the explicit active-corner condition. These are new deterministic kernels; no rate, probability law, rank or count definition is changed.

`Proposition65Contract.localLaw` is CLOSED. It asserts the literal absolute error between the actual marked-rank cylinder probability under the exponential race and the product of these K kernels. The upper bound is C times the product of H kernels times the sum of the two prescribed errors. Its second clause includes the [A,B] consequence with real A>0 and both distances in that interval. A>0 is the positive-depth domain of the expression A^(-kappa), not a model assumption. No moment or convergence predicate occurs as an input.

`Proposition65Contract.proposition65 := localLaw ∧ Lemma64Contract.matrix` retains the full global matrix domination from Lemma 6.4 for other configurations, including adjacent demanded ranks. The matrix component is a CONCLUSION and is already proved independently. It does not supply or replace the unproved local law. This imports the frozen contract, not the implementation theorem. No assumption about a global dominating matrix is made.

## Obligation ledger

| Required fact | Status / available proof | Allowed inputs and discharge |
|---|---|---|
| Full global domination outside the separated moderate regime | Proved `lemma64_matrix` | Original profile/sampling only; no new endpoint input |
| Exact finite cylinder representation including arbitrary demanded ranks | Proved `markedRankCylinder_real_probability_eq_openSimplex_product` and distinct-gap product identities | Positive finite rates and injective marked labels/ranks |
| Separation implies distinct selected background gaps | Proved `sorted_gap_injective_of_separated` | Exact full-rank separation, s<=r, and injective ranks; rank subtraction identities and finite-index bounds discharge the proof |
| Separated cylinder equals expectation of the product of actual gap kernels | Proved `separated_cylinder_eq_gap_product` | Positive finite weights and injective labels/ranks. Monotonicity plus the proved gap injectivity supplies strict gap order. The exact ordered-insertion identity removes simplex ordering while retaining a SINGLE expectation of the product |
| Selected-spacing integration under the explicit product law | Proved `selected_gap_integral`, `selected_gaps_product_integral` | W>0, theta>0, Lambda>=0; both integrability and exact rational integral proved by exponential-density integration. Actual-race assembly is now supplied by `separated_cylinder_skeleton_formula` below |
| Skeleton time is the actual start minus selected preceding gaps | Proved `skeleton_start_eq_actual_start_sub_selected`, backed by `gap_start_eq_skeleton_add_selected` | Existing normalized spacings and ordered remaining rates; telescoping and a finite selected/unselected partition, no probability hypothesis |
| Skeleton depends only on unselected coordinates | Proved `skeleton_start_congr_unselected`, `continuous_skeleton_start`, `skeleton_start_nonneg` | Explicit finite sum over unselected earlier gaps; positive remaining rates are derived from Weights |
| Group all selected earlier/later survival contributions | Proved `selected_survival_sum_swap`, `selected_weighted_starts`, `selected_survival_product`, `selected_insertion_product_skeleton` | Exact finite algebra on actual normalized starts; `laterSelectedRate` is the literal sum of theta over later selected gaps. No endpoint or independence premise |
| Exact integral with unselected spacings fixed | Proved `selected_coordinate_integral` | Fixed actual weights and elimination permutation, positive theta on selected gap set; ordered remaining-rate positivity and later-rate nonnegativity discharged internally. `selectedFill` leaves unselected coordinates fixed and `skeleton_start_fill` proves skeleton invariance. Uses the exact product exponential integral, not insertion independence |
| Actual deleted-race product transferred to each normalized order law | Proved `deleted_order_normalized_integral`, `deleted_order_kernel_product_integral`, `deleted_kernel_product_order_sum` | Compact-deletion measure preservation, actual order partition, and proved normalized-spacing law; measurability and raw-product integrability discharged for the concrete kernels |
| Separated cylinder expressed as the sum over actual elimination orders | Proved `separated_cylinder_order_sum` | Generic terminal buffer is explicit; `endpoint_block_terminal_buffer` derives it from delta<=1/2 and target depth>=r+1, both choices of output constants. `sorted_gap_nonfinal_of_terminal_buffer` derives the finite gap-index domain; no extra model assumption |
| Normalized insertion product integrability | Proved `normalized_insertion_product_bounds`, `normalized_insertion_product_integrable` | Nonnegative marked rates suffice; actual weights have positive rates. Product lies in [0,1] on the nonnegative orthant, which has probability one under the proved standard gap law; no integrability premise |
| Selected-coordinate Fubini under the actual full normalized law | Proved `normalized_skeleton_integral` | Splits the existing product law by a measure-preserving equivalence and uses proved normalized-product integrability. Equality of the two finite enumerations of the selected set is proved by Subsingleton.elim; no new measure or assumed independence |
| Marked-rate reindexing and literal later-rate sum | Proved `marked_normalized_skeleton_integral`, `later_selected_rate_reindex` | Injective selected-gap map; Function.extend agrees with theta at every selected gap by injectivity. Its off-image zero values are unused. Positivity on the image follows from original marked rates |
| Complete skeleton cylinder formula for the actual deleted race | Proved `separated_cylinder_skeleton_formula` | Combines actual order sum, reindexing, and Fubini; selected-gap injectivity discharged from rank separation, marked-rate positivity from Weights. Generic terminal buffer is supplied by the endpoint-block lemma. This is an explicit unconditional mixture/integral identity, not a separately asserted conditional-expectation version |
| Chronological weight comparison and deterministic skeleton displacement | Proved `ordered_remaining_rate_antitone`, `skeleton_displacement_bound` | Finite positive weights; selected normalized spacings nonnegative, available almost surely under standardGapLaw. Applies across corners. Proves 0<=T-U<=(sum over selected xi)/W at the current gap; quantitative probabilistic concentration remains to be assembled |
| Relative fixed-time concentration of deleted counts | Proved `bernoulli_sum_exp_integrable`, `bernoulli_relative_upper_tail`, `bernoulli_relative_lower_tail`, `deleted_arrival_relative_tails`, `deleted_survivor_relative_tails` | Arbitrary 0<=eps<=1; each tail is at most exp(-eps^2*m/4), with m=n*deletedG or n*deletedH. The concrete race discharges independence, zero-one range, measurability and exact mean; exponential integrability is proved. No identical-distribution or count-mean bound is assumed |
| Transfer count deviations to early/late actual gap starts | Proved `deleted_gap_start_relative_early_arrival`, `deleted_gap_start_relative_late_arrival`, `deleted_gap_start_relative_early_survivor`, `deleted_gap_start_relative_late_survivor` | Explicit numerical mean-separation premises only; all race-law premises discharged. Three supporting deterministic count identities cover non-strict arrivals, strict survivors, and the sentinel gap. These numerical premises remain to be discharged from profile estimates, not added to the main contract |
| Deleted quantile mean separation from the actual profile | Proved `PowerProfile.left_deleted_quantile_separation`, `PowerProfile.right_deleted_quantile_separation`, using `scaled_deletion_error` | Original PowerProfile and SampledRates; slope a is an output chosen in (0,1]. The finite deletion error is at most r in the unnormalized count mean; no assumed mean estimate |
| Profile-derived quantile-window probability bounds | Proved `PowerProfile.left_gap_quantile_window`, `PowerProfile.right_gap_quantile_window`, using `relative_mean_separation` | All mean-separation premises of the gap-time lemmas discharged. Explicit numerical domains remain: depth large/interior to endpoint block, |effective gap depth-m|<=r, 0<u<=1/2, and 8r<=a*u*m. Their final marked-configuration and cutoff discharge is still required |
| Actual marked-gap depth displacement | Proved `sorted_gap_left_displacement`, `sorted_gap_right_displacement` | Original injective sources/ranks and mark budget; the left depth is the one-based rank, the right effective depth is the literal undeleted cardinality N minus q. No new separation or rate premise |
| Fixed-time remaining-weight variance and polynomial concentration | Proved `survival_indicator_memLp_two`, `survival_indicator_variance_le_mean`, `deleted_remaining_weight_variance`, `deleted_remaining_weight_mean`, `deleted_remaining_weight_chebyshev` | Finite positive weights only; mean=n*deletedD(1,t), variance<=n*deletedD(2,t), tail at absolute tolerance eps bounded by n*deletedD(2,t)/eps^2. MemLp, independent-sum variance, and exact expectation proved; no normalization or uniform rate bound |
| Profile-scale weight concentration | Proved `deletedD_le_populationD`, `deleted_weight_scaled_chebyshev`, and `PowerProfile.left_weight_quantile_concentration` / `PowerProfile.right_weight_quantile_concentration` | The concrete bound is C/(eps^2*m) at tolerance eps*m/t, centered at the exact deleted mean at each fixed time s in [t/4,4t]. The second-moment premise is discharged by the original profile estimates. Deleted sets are arbitrary; no bounded-rate, normalization or integrability premise |
| Actual random-time weight identity and deterministic transfer | Proved `deleted_surviving_weight_antitone`, `raceGapRate_eq_surviving_weight`, `deleted_gap_rate_eq_surviving_weight`, `deleted_weight_random_time_bound`, `deleted_gap_rate_eq_surviving_weight_ae` | Gap rate equals the actual surviving-weight sum at the gap start. Injectivity and positive clocks are discharged by the original race in the ae theorem. Monotonicity transfers endpoint path bounds to any time between them |
| Joint concentration of skeleton times and remaining weights | Unresolved | Endpoint-scale fixed-time concentration and the exact deterministic random-time bridge are proved. Still must combine the window-event probabilities, deterministic mean drift/deletion bounds, and selected-spacing tails, then assemble joint skeleton control and the weighted exceptional-event estimate. The original exponential concentration bound is not claimed proved |
| Weighted bad-event bound | Unresolved concrete assembly | Existing moment/envelope and finite Holder proofs are available; all norms must be discharged |
| Mixed-corner rate-time and hazard approximations | Unresolved concrete joint assembly | Existing scalar quantile estimates; no assumption that all marks use one corner |
| Rational selected-gap factor approximation | Proved `selected_gap_relative_error`, `selected_gap_factor_error` | W>0, theta>0, Lambda>=0. Gives 0<=theta/W-W*theta/((W+Lambda)*(W+Lambda+theta))<=(theta/W)*((2*Lambda+theta)/W). No smallness or independence hypothesis. Its integral interpretation is proved; the quantitative local-law application remains unresolved |
| Telescoping products and actual small-error premises | Unresolved | Must prove all numerical premises for the actual mixed-corner configuration |
| Full local error and [A,B] consequence | Unresolved | Exact frozen conclusion; no extra regularity or rate assumption permitted |
| Separate closed proposition65_contractCheck | Unresolved | Must obtain full contract from completed concrete theorem with no external parameters |

The prior Lemma 6.4 baseline remains untouched, as does the older baseline. The manuscript changed externally between the Lemma 6.4 completion and the new snapshot: the old 45-file check now reports only `fixed_points_sampled_profile.tex` changed. The current Lemma 6.4 statement and Proposition 6.5 statement were reread and match the previously inspected statements; the Lemma 6.4 proof now starts with the r+1 matrix construction. No full historical-file equivalence is claimed. The new 47-file Proposition 6.5 snapshot passes and records its specification/import closure before proof implementation. No baseline was reset.

## Checked first milestone

Latest Proposition 6.5 additions: nine proved results in Section6WeightScaleConcentration and Section6RandomWeightBridge, with a separate exact type/axiom audit. They establish the profile-scale remaining-weight bound C/(eps^2*m), the exact gap-rate/surviving-weight identity, and deterministic monotone transfer to intermediate times. All numerical moment premises are discharged in the concrete profile theorems; the original model and frozen contract are unchanged. This still does not prove the probability bound for the full joint good event or the final local error.

Concurrent-work note: the audit generator now sees additional source declarations in Section6SingleReplacement and Section6CoordinateIntegration, which concern Lemma 6.6 and were not edited in this turn. They were absent from the Section6 build imports at inspection. Their presence must not be counted as verified Proposition 6.5 work. The bounded `audit/Section6WeightScaleAudit.lean` prints the unchanged closed contract and all nine new theorem types/axioms; the project-wide audit is still attempted and its actual result is recorded below. No existing audit target or source-coverage rule is weakened to accommodate the concurrent files.

Actual verification: individual `lake env lean -o ...` checks of the two new modules exited 0. `lake build` exited 0 (4291 jobs), log `audit/section6-proposition65-weight-scale-build.log`. The unchanged project-wide generator saw 728 declarations. `lake env lean audit/Section6Current.lean` exited 1 with 16 unknown-constant errors for the eight unimported concurrent Lemma 6.6 declarations (one type and one axiom print each). The unchanged `python audit/validate_section6_progress.py` consequently fails at its declaration-coverage assertion on line 13; it does not reach the older manuscript-hash check in this run. The older hash mismatch has not been repaired or reset.

`lake env lean audit/Section6WeightScaleAudit.lean` exited 0. `python audit/report_section6_weight_scale.py` exited 0 and verified exactly all nine declarations from the two new modules against the permitted axiom whitelist, extracting full elaborated types and actual axiom output to `section6-proposition65-weight-scale-evidence.md`. This bounded check is not presented as a substitute for a passing project-wide check. `python audit/freeze_section6_proposition65_contract.py` exited 0 with 47 files and no changes. The main theorem and closed contract check remain absent. No checking limits, frozen mathematical definitions, or trusted dependencies were changed.

Latest mathematical milestone (711 declarations): seven additional results prove both actual marked-gap depth displacements and the variance/mean/Chebyshev bounds for the actual deleted remaining weight. Following the user's request to pursue an alternative proof, the current proposed route uses polynomial concentration. The target permits an unspecified positive polynomial exponent kappa; the intended assembly is to use the existing weighted Holder estimate so that a polynomial bad-event probability still retains every edge envelope. This sufficiency is a remaining proof obligation, not an asserted completed theorem.

The concrete fixed-time result bounds P(eps<=|W-n*deletedD(1,t)|) by n*deletedD(2,t)/eps^2. Every summand is the original rate times its actual survival indicator. Its variance is bounded by rate^2 times its survival mean; independence gives the sum bound. Integrability follows from finite per-label bounds, not a uniform-in-n bounded-rate premise. All statements remain meaningful for arbitrary finite positive rates without normalization. The main Proposition 6.5 statement and its definitions are unchanged; the main theorem and separate closed contract check remain unproved.

Verification for 711 declarations: individual `lake env lean -o ...` checks of Section6MarkedGapDisplacement and Section6RemainingWeightVariance exited 0 after replacing a non-definitional function-sum change by a proved equality and normalizing multiplication order in the expectation. The deprecated integral sum alias was replaced with `integral_finsetSum`. `lake build` exited 0 (4289 jobs), log `audit/section6-proposition65-variance-build.log`. The generator produced 711 declarations; `lake env lean audit/Section6Current.lean` exited 0. The report script checked all 711 source/axiom reports against the whitelist and extracted 39 supporting theorem types (exit 0). The Proposition 6.5 freeze returned 47 files and no changes (exit 0). The older validator still exits 1 at line 21 on its preserved manuscript hash, after passing exact declaration and axiom checks. No checking limits or trusted dependencies changed. Full current types and actual axiom output are in `section6-proposition65-transfer-evidence.md`.

Latest mathematical milestone (704 declarations): six new theorems prove scaled finite deletion error, left and right deleted quantile mean separation, the numerical relative-separation conversion, and both profile-derived gap-quantile windows. The windows now derive every mean premise of the four generic gap-time bounds from PowerProfile and SampledRates. They use a slope a in (0,1] chosen by the proof, preserve the exact sampled quantile and deleted means, and allow arbitrary deleted sets of size at most r.

These remain intermediate results: the window-size requirement 8r<=a*u*m and effective gap-depth displacement bound are explicit numerical helper domains. The main proof still must establish them from its actual marked configuration and chosen h0, specialize u to the shrinking scale, and lower-bound the relevant deleted means to obtain the required uniform depth-dependent exponent. Remaining-weight concentration, joint skeleton concentration, the local approximation, and the closed main contract check are still unproved. No such numerical helper domain was inserted into the frozen contract. There is no completed main theorem or contract-check axiom output.

Verification for 704 declarations: individual `lake env lean -o ...` checks for Section6DeletedQuantileSeparation, Section6RelativeMeanSeparation, and Section6ProfileGapQuantileTail exited 0. Initial attempts required removing a no-progress dsimp, explicitly proving nonnegativity of the lower time, and including the already proved nonnegative product in the restricted arithmetic search. The numerical module uses the existing PopulationFinite import; an obsolete compile was stopped after narrowing its initial broad import. No trust or checking limits changed. `lake build` exited 0 (4287 jobs), log `audit/section6-proposition65-profile-window-build.log`. The audit generator produced 704 declarations; `lake env lean audit/Section6Current.lean` exited 0. The report script checked all 704 exact source/axiom reports against the permitted whitelist and extracted 32 full supporting types (exit 0). The Proposition 6.5 freeze returned 47 files, no changes (exit 0). The old validator still exits 1 at line 21 on the preserved manuscript hash, after passing declaration and axiom checks. Full types and actual axiom output are recorded in `section6-proposition65-transfer-evidence.md`.

Latest mathematical milestone (698 declarations): seven theorems in Section6GapTimeRelativeTail compile. Non-strict arrivals at t are bounded by strict arrivals at any later T. A late gap start forces at most q arrivals, with the zero-index sentinel handled explicitly by t>=0. Non-strict arrivals plus strict survivors equal the exact undeleted cardinality even at ties. The four probability bounds then transfer the proved relative count tails to early/late actual deleted gap times, in both arrival and survivor forms. Injectivity is used only on the existing probability-one race event, not supplied as a new probabilistic assumption.

The numerical premises are explicit: early arrival requires (1+eps)nG_deleted(t)<=q; late arrival requires q<=(1-eps)nG_deleted(t); early survivor requires N-q<=(1-eps)nH_deleted(t); late survivor requires (1+eps)nH_deleted(t)<=N-q. Here N is the literal undeleted cardinality. These are generic helper premises. Their profile/quantile discharge remains UNPROVED, as do the full skeleton concentration bound and the main Proposition 6.5 contract check. No output theorem with these extra mean premises is claimed to complete the manuscript proposition.

Verification for 698 declarations: `lake env lean -o .lake/build/lib/lean/Luce/Section6GapTimeRelativeTail.olean Luce/Section6GapTimeRelativeTail.lean` exited 0 after fixing an initial elementary conditional-nonnegativity goal by case splitting. `lake build` exited 0 (4284 jobs), log `audit/section6-proposition65-gap-time-build.log`. `python audit/generate_section6_current_audit.py` generated 698 declarations; `lake env lean audit/Section6Current.lean` exited 0. `python audit/report_section6_proposition65_transfer.py` checked all 698 exact source/axiom reports with the permitted whitelist and extracted 26 full supporting theorem types (exit 0). `python audit/freeze_section6_proposition65_contract.py` returned 47 files and no changes (exit 0). The older `python audit/validate_section6_progress.py` still fails at line 21 on the preserved manuscript hash after passing declaration and axiom checks. No baseline or checking setting changed. Exact types and actual axiom output are in `section6-proposition65-transfer-evidence.md`; the main theorem and contract check remain unproved and have no axiom output.

Latest proof milestone (691 declarations): the five relative-count theorems above compile. For any independent, not necessarily identically distributed Bernoulli family with total mean m and 0<=eps<=1, they prove both tails at thresholds (1+eps)m and (1-eps)m are bounded by exp(-eps^2*m/4). The proof chooses tilt +/-eps/2, uses the already proved Bernoulli MGF estimate and the quadratic exponential remainder bound, and proves all exponential integrability. The concrete deleted-count theorems discharge all probability-law premises from the existing exponential race and identify m exactly with n*deletedG or n*deletedH. These are fixed-time count theorems, not the still-unproved joint skeleton concentration theorem. The full Proposition 6.5 and its contract check remain absent.

Verification of this milestone: individual `lake env lean -o ...` checks of Section6BernoulliRelativeTail and Section6DeletedCountRelativeTail exited 0. The initial lower-tail proof failed on a positivity tactic applied to a nonpositivity goal; the final proof gives the sign inequality explicitly. An obsolete broader arithmetic search was stopped after replacing it by `nlinarith only` on the proved exponential remainder estimate. No checking limits changed. `lake build` exited 0 (4283 jobs), log `audit/section6-proposition65-relative-count-build.log`. `python audit/generate_section6_current_audit.py` generated 691 declarations; `lake env lean audit/Section6Current.lean` exited 0. `python audit/report_section6_proposition65_transfer.py` checked all 691 exact source/axiom reports against the whitelist and extracted 19 full supporting types (exit 0). `python audit/freeze_section6_proposition65_contract.py` returned 47 files and no changes (exit 0). `python audit/validate_section6_progress.py` still exits 1 at line 21 on the preserved old manuscript hash after passing declaration and axiom checks. Exact current types and actual axiom output are in `section6-proposition65-transfer-evidence.md`; no main-theorem or contract-check axiom output exists because those proofs remain absent.

Current mathematical milestone (686 declarations; earlier entries below are historical): `normalized_skeleton_integral` proves Fubini under the original full normalized spacing law; `marked_normalized_skeleton_integral` reindexes the actual marked rates; `later_selected_rate_reindex` proves Lambda is exactly the sum over later marked edges; `separated_cylinder_skeleton_formula` proves the exact actual-cylinder mixture of skeleton integrals. Thus the previously unresolved exact transfer/assembly is complete. The result explicitly integrates the unselected marginal coordinates and sums the actual elimination-order masses. It neither assumes insertion independence nor changes the actual probability law.

`ordered_remaining_rate_antitone` and `skeleton_displacement_bound` additionally prove the deterministic chronological comparison and 0<=T-U<=(sum selected xi)/W. The two normalized-product integrability lemmas now quantify an arbitrary finite index type instead of only Fin s, permitting the actual selected-gap subtype. Their formulas and real-law meanings are unchanged, and the prior Fin s instances remain direct specializations. This is a helper generalization, not a restriction of model data or a change to a definition referenced by the frozen contract.

Still unproved: the full closed `Proposition65Contract.localLaw` and `proposition65_contractCheck`. The next missing estimate is the joint skeleton concentration bound `eq:sp-skeleton-bad-probability`, from the original sampled profile, followed by the weighted exceptional-event estimate and the full mixed-corner local approximation. The deterministic displacement bound alone does not prove concentration. No source error or counterexample has been established.

Verification for the 686-declaration milestone:

* Individual `lake env lean -o ...` checks for Section6SkeletonFubini, Section6SelectedGapReindex, Section6CylinderSkeleton, and Section6SkeletonDisplacement: exit 0. Initial Fubini attempts failed on two different Fintype instances for the same selected subtype; the proof now explicitly establishes their equality. A recursive simplifier attempt was replaced by a pointwise function equality; no recursion or checking limits were changed.
* First `lake build`, log `audit/section6-proposition65-skeleton-build.log`: failed because the displacement import was added while that build was already running, before its object file existed. This failure is retained in the log.
* Final `lake build`, log `audit/section6-proposition65-skeleton-final-build.log`: exit 0, 4281 jobs.
* `python audit/generate_section6_current_audit.py`: exit 0, 686 declarations.
* `lake env lean audit/Section6Current.lean`: exit 0, actual full types and axiom output in `audit/section6-current-audit.log`.
* `python audit/report_section6_proposition65_transfer.py`: exit 0, checks all 686 source/axiom reports against the exact whitelist and extracts 14 full supporting theorem types into `section6-proposition65-transfer-evidence.md`.
* `python audit/freeze_section6_proposition65_contract.py`: exit 0, 47 files, no changes.
* `python audit/validate_section6_progress.py`: exit 1 at line 21 on the preserved old manuscript hash, after exact declaration and axiom checks passed. This is not a fully passing validator, and no old baseline was reset.

Latest milestone (680 declarations; supersedes earlier status below): the eight new order-transfer, finite-gap-domain, and integrability theorems are checked. Exact fully elaborated supporting theorem types, actual transitive axiom output, and the unchanged complete contract are in `section6-proposition65-transfer-evidence.md`. No generalized local-law theorem or `proposition65_contractCheck` is declared, so neither has axiom output.

Commands actually run for this milestone:

* `lake env lean -o .lake/build/lib/lean/Luce/Section6NormalizedProductIntegrability.olean Luce/Section6NormalizedProductIntegrability.lean`: exit 0, with a non-failing letI style warning.
* `lake build Luce.Section6SeparatedOrderSum`: exit 0, 3699 jobs; log `audit/section6-separated-order-sum-build.log`.
* `lake build`: exit 0, 4277 jobs; log `audit/section6-proposition65-order-transfer-build.log`.
* `python audit/generate_section6_current_audit.py`: exit 0, 680 declarations.
* `lake env lean audit/Section6Current.lean`: exit 0; full types and axioms in `audit/section6-current-audit.log`.
* `python audit/validate_section6_progress.py`: fails at line 21 on `fixed_points_sampled_profile.tex` in the older baseline, after passing exact declaration coverage and the transitive axiom whitelist. Not a passing validation.
* `python audit/freeze_section6_proposition65_contract.py`: exit 0, 47 frozen files, no changes.
* `python audit/report_section6_proposition65_transfer.py`: exit 0; independently checks all 680 current source/axiom reports and extracts the eight new full types. It does not replace or reset the older baseline check.

The precise unproved main proposition is `Luce.Section6.Proposition65Contract.localLaw`, printed in full in the evidence file. Available model assumptions remain exactly PowerProfile and SampledRates, plus the configuration restrictions quantified there. In particular, the remaining skeleton concentration bound is the manuscript's `eq:sp-skeleton-bad-probability`, and the desired error is `eq:sp-joint-insertion-error`; neither is available as a premise. At present this is an unfinished formal proof, not an identified counterexample or a demonstrated source error. No main theorem statement, mathematical definition referenced by the contract, or manuscript text was edited in this milestone.

Latest milestone (672 declarations; supersedes earlier status below): six theorems in `Section6SkeletonProduct` prove literal later-selected-rate nonnegativity, pair-sum interchange, selected-index start decomposition, weighted-start decomposition, survival-product factorization, and the full pointwise insertion-product identity. Three theorems in `Section6SelectedCoordinateIntegral` prove selected-coordinate filling, skeleton invariance under filling, and the exact selected-coordinate integral with integrability. The integral fixes the actual elimination permutation and unselected coordinates and produces the manuscript skeleton survival times the rational product. Positive ordered remaining rates and nonnegative later-rate sums are proved internally; only positivity of selected marked rates is the generic input that the eventual original-race application must discharge.

Verification: all nine declarations compiled. An initial recursive finite-product simplification hit the recursion limit; a subsequent slow compile was explicitly stopped after replacing that recursive conversion by three explicit rewrites. No checking limit or statement was changed. The dependency branch passed (3589 jobs), the full build passed (4274 jobs), and the complete Lean type/axiom audit exited 0 for 672 source declarations plus the new definitions `laterSelectedRate` and `selectedFill`. New main helper axiom outputs are `[propext, Classical.choice.{u}, Quot.sound.{u}]`. The complete validator passed declaration/axiom checks then failed at the preserved manuscript hash. The Proposition 6.5 47-file freeze remains unchanged. New-module source scan found no placeholders or checking bypasses.

Remaining exact transfer: map the distinct selected demanded gaps and their marked rates into the compacted deleted-race coordinates, apply `selected_coordinate_integral` under the existing elimination-chain product-law identity, and then control the resulting skeleton expression by joint concentration and weighted bad-event estimates. The selected-coordinate identity is now proved; transport to the actual cylinder and the quantitative local approximation remain unresolved. Neither is an assumed premise of the frozen contract. This is still an incomplete Proposition 6.5.

Latest milestone (663 source declarations; supersedes the initial status below): nine further proofs in `Section6SelectedGapIntegral` and `Section6SkeletonStart` establish the exact exponential density calculation, scalar Laplace transform with integrability, selected-gap rational integral, its finite product under the explicit standard exponential product law, and the skeleton decomposition/congruence/continuity/nonnegativity and exact actual-clock identity. These do NOT yet prove the conditional cylinder identity for the deleted race. All nine declarations compiled; separate integral/skeleton branches passed (2781 and 3586 jobs), followed by the full default build (4272 jobs). The complete generated Lean type/axiom audit exited 0 for 663 source declarations and the new skeleton definition. Every axiom report uses only propext, Classical.choice and Quot.sound. The complete validator again passed declaration/axiom checks then failed at its preserved old manuscript hash; the new 47-file Proposition 6.5 freeze passes. No new-module placeholders or checking bypasses were found. Main Proposition 6.5 and its closed contract check remain unresolved.

Next exact transfer obligation: for a fixed actual elimination chain and distinct selected gaps, rewrite the product of survival factors using `gap_start_eq_skeleton_add_selected`, identify Lambda_g as the sum of rates of selected later marks, and integrate selected coordinates using `selected_gaps_product_integral` while holding unselected coordinates fixed. Then transport through the existing actual normalized-spacing law and prove concentration/bad-event control. Skeleton invariance is proved; the required conditional integration and model-level estimates are not assumed.

Four new theorem declarations are kernel-checked: `selected_gap_relative_error`, `selected_gap_factor_error`, `sorted_gap_injective_of_separated`, and `separated_cylinder_eq_gap_product`. All have actual transitive axiom output `[propext, Classical.choice.{u}, Quot.sound.{u}]`. The complete audit prints the new closed contract and all five kernel definitions with explicit arguments, universes and full names, together with all 654 source theorem types and axiom reports. No Proposition 6.5 main theorem or contract-checking theorem exists yet.

Commands/results: individual modules compiled; the initial combined build FAILED to read existing compiled dependency files (preserved in `audit/section6-proposition65-initial-build-failure.log`). The files existed; no dependency or toolchain was changed. Sequential contract, selected-factor and separated-insertion branches passed (3687, 3006 and 3458 jobs). The subsequent default `lake build` passed (4270 jobs). `lake env lean audit/Section6Current.lean` exited 0. The complete validator passed the declaration-set and axiom whitelist checks, then failed at its old manuscript-hash check. The new Proposition 6.5 freeze reports 47 files and changed=[]. The new source scan found no placeholders or checking bypasses.

Exact remaining target: `Luce.Section6.Proposition65Contract.localLaw`, printed in full in `audit/section6-current-audit.log` and defined independently in `Luce/Section6Proposition65Contract.lean`. Available assumptions are exactly PowerProfile and SampledRates with the literal separated/moderate configuration restrictions quantified inside the conclusion. The immediate missing step is a proved selected-spacing conditional integration identity for the ACTUAL deleted race, including the later-rate Lambda contribution, followed by joint concentration for its skeleton start times and remaining weights. The existing exact expectation-of-product identity alone does not imply the product approximation. This is an implementation gap, not an identified mathematical counterexample or an authorized extra hypothesis.
