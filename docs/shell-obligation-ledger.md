# Shell migration obligation ledger

## Final implementation ledger

The current implementation proves both frozen contracts. The final combined build and transitive audit are recorded in `docs/section5-final-report.md`. The historical progress entries below are retained as a chronology; statements there that a step was unresolved are superseded by this table.

| Required obligation | Kernel-checked implementation | Permitted inputs and discharge |
|---|---|---|
| Exact shell indexing, nonempty minimum, finite support | `Section4EndpointShells.lean`: `terminalDepth_add_label`, `shellFloor_attained`, `shell_index_le_row`, `shellTailCost_eq_sum` | Positive finite-row rates; literal nonempty shell. No array bounds. |
| Exact iterated shell limit and compatibility | `endpointShellAssumption_iff_eventually`; `UniformEndpointAssumption.shell` | Equivalent quantifier formulation proved both ways. Old uniform premise appears only in compatibility. |
| Sharp Jensen survivor lower bound | `NormalizedWeights.meanSurvivors_jensen` | Separate normalization only; exact coefficient one in logarithmic time. |
| Block capacity and Q integral | `exponentialRace_block_capacity`, `block_endpoint_capacity`, `integral_capacity_kernel` | Local cutoffs and extrema constructed in the shell application; clock independence follows from the canonical model. |
| Square-root shell buffer | `EndpointShellAssumption.buffered_Q_raw` | Raw shell condition only; no uniform positive endpoint rate. |
| Endpoint expectation and probability tightness | `EndpointShellAssumption.expectation_tightness`, `probability_tightness` | Normalization + raw shell; all capacity thresholds and Markov premises proved. |
| Full fixed-point intensity, spatial law, scalar TV | `Luce.Shell.section4_main_poisson_general` | Original profile + normalization + raw shell + defining finite Luce law. |
| Closed Section 4 check | [`section4_contractCheck`](../Luce/Section4ShellContractCheck.lean) | No external theorem parameters. |
| Global high-rate truncation | `ProfileLimit.highCycleExpectation_small` in [`Section5ExceptionalHigh.lean`](../Luce/Section5ExceptionalHigh.lean), using `Section5HighRates.lean` | Original normalization/profile inputs; reused without endpoint positivity. |
| Fixed-interior low-rate truncation | `ProfileLimit.interiorLowCycleExpectation_bound`, `interiorLowCycleExpectation_small` | Original profile and fixed interior cutoff. No endpoint input or delta < gamma. |
| Cyclic local limit | `section5_cyclic_local` | Normalization and profile only. Microscopic inputs constructed internally. |
| Exact cycle counting and factorial multiplicities | `bulk_cycle_factorial_eq_assignment_sum`, `bulk_factorial_expectation_eq_rank_sum` | Original finite permutations, counts and cycle lengths; finite-statistic integrability proved. |
| Early/late shell cycle estimates | `early_ghost_shell_probability`, `earlyCycleShellTail_limit`, `late_retained_expectation_bound`, `retained_expectation_tightness` | Canonical clocks, normalization, raw shell. Source cutoff before low-rate cutoff before target cutoff; source order preserved before enlargement. |
| Discarded-cycle bounds and full cycle expectation tightness | `cycle_tail_small`, `short_cycle_tail_small`, `cycle_shell_tightness` | High-rate and fixed-interior low-rate truncations discharged before combining. Singleton case uses Section 4. |
| Closed intermediate endpoint check | [`cycleShell_contractCheck`](../Luce/Section5CycleShellContractCheck.lean) | Exact original summed endpoint expectation; no helper assumptions. |
| Joint bulk factorial moments | [`bulk_joint_factorial_moments`](../Luce/Section5FactorialMoments.lean) | Normalization/profile only. Product-measure regrouping, original rotation divisor, and zero-order case proved. |
| Full cyclic intensity integrability and cutoff limit | [`cycle_trace_integrable`, `bulk_intensity_tendsto`](../Luce/Section5IntensityFinite.lean) | Normalization/profile/raw shell. Uniform integral bound proved from first moments and endpoint tightness; full nonnegativity proved. |
| Finite point expansion and integrated remainder | `countVectorSieve_error`, `bulk_sieve_error_integral_le`, `bulk_sieve_remainder_limit` | Deterministic count inequalities and proved joint factorial moments. No exponential-moment input. |
| Vanishing remainder and bulk joint point probabilities | `limitingSieveRemainder_tendsto_zero`, [`bulk_cycle_point_probability_limit`](../Luce/Section5BulkPointProbability.lean) | Exponential-series summability, finite approximation with row limit first, and exact event-measure identification. |
| Spatial cutoff removal for full point probabilities | [`cycle_point_probability_limit`](../Luce/Section5FullPointProbability.lean) | Actual point-indicator difference bounded by endpoint cycle counts; shell expectation tightness and proved intensity convergence discharge errors. |
| Countable-space TV and weak convergence | [`CountableLaw.tendsto_probabilityTotalVariation_of_singletons`](../Luce/Section4CountableTotalVariation.lean), [`cycle_vector_totalVariation`](../Luce/Section5VectorTotalVariation.lean), [`cycle_vector_weak`](../Luce/Section5RaceConclusion.lean) | Point masses converge to a probability law; finite-core tightness derived internally. Countability is inherent in the fixed finite vector of natural counts. |
| Original TV convention preserved | `cycleVectorTotalVariation_eq_countable` | Equality of the two literal suprema proved; no norm convention change. |
| Draw/rank representation and arbitrary-law transport | [`cycleCountVector_raceDraw_eq_rank`, `luce_cycle_vector_map_eq`, `luce_cycle_test_integral_eq`](../Luce/Section5LuceTransfer.lean) | Original exact finite Luce masses. Cycle-count invariance under inverse proved; no coupling of rows assumed. |
| Complete generalized Section 5 theorem | [`section5_main_general`](../Luce/Section5ShellMain.lean) | Exactly normalization, original profile, raw shell, and the original finite model/law. |
| Frozen closed Section 5 check | [`section5_contractCheck`](../Luce/Section5ShellContractCheck.lean) | No external explicit, implicit, or instance parameters. Main theorem discharges the unchanged closed contract. |

No mathematical obligation in the requested Section 4/5 migration is left as an assumed helper input. Consult the final report for the actual build, complete theorem types, and transitive axiom outputs.

## Historical progress ledger (superseded above)

The frozen target is `ShellMigrationContract.section4` in `Luce/Section4ShellMigrationContract.lean`.
The generalized Section 4 theorem `Luce.Shell.section4_main_poisson_general` and the closed `section4_contractCheck` are proved.
No unproved entry below is an allowed input to that target. The Section 4 milestone is established; Section 5 obligations remain separately tracked.

| Required fact | Implementation / proof | Mathematical inputs | Discharge / status |
|---|---|---|---|
| Exact terminal indexing | `terminalDepth_pos`, `terminalDepth_le`, `terminalDepth_add_label`, `terminalDepth_labelOfDepth`, `terminalDepth_injective` in `EndpointShells` | A label `Fin n`, or `1 ≤ m ≤ n` | Checked. These are precisely membership in the finite row. |
| Shell floor is an attained finite minimum | `shellFloor_attained`, `shellFloor_pos`, `shellFloor_le_rate`, `le_shellFloor` | Nonempty finite shell; `Weights.positive` | Checked; nonemptiness is tested within `shellCost`, not assumed of all shells. |
| Finite support and finite row cost | `shell_index_le_row`, `shellTailCost_eq_sum`, `shellTailCost_ne_top` | Positive integer depths | Checked; no boundedness premise on the array. |
| Exact raw limit vs epsilon/eventually | `endpointShellAssumption_iff_eventually` | Raw ENNReal iterated limit | Checked in both directions. No change of quantifier order. |
| Old uniform condition implies raw shell condition | `UniformEndpointAssumption.shell` in `EndpointShellCompatibility` | Only the old uniform predicate | Checked. Used for compatibility only, never as an input to the generalized target. |
| Sharp survivor Jensen estimate | `meanSurvivors_jensen`, `NormalizedWeights.meanSurvivors_jensen` | Sum of rates equals n / separate normalization | Checked. The exponent is exactly `-t`. |
| Q positivity, monotonicity and bounds | `EndpointCapacityAnalytic` | Positive argument; explicit scalar exponential bound for upper estimate | Checked. The scalar smallness premise is discharged in `bufferedShellQCost_le_of_small_tail` and `EndpointShellAssumption.buffered_Q`. |
| Exact Q tail integral and integrability | `integral_capacity_kernel`, `integrable_capacity_kernel` | `a > 0`, cutoff `s > 0` | Checked; instantiated in `endpoint_integral_bound` and the concrete block theorem. |
| Rate monotonicity of capacity kernel | `capacity_kernel_antitone` | Positive time, positive compared rates | Checked; minimum-rate comparison discharged by `capacity_block_density_le`. |
| Exact Bernoulli Chernoff block exponent | `bernoulli_block_lower_tail` in `EndpointCapacityChernoff` | Independent measurable 0/1 variables, `0 ≤ r < B`, B below true mean | Checked. `capacity_other_survivor_early` supplies the actual clock indicators, independence, and survivor mean bound. |
| Candidate removal probability comparison | `removed_survivor_probability_le` | Canonical independent exponentials; `t ≥ 0` | Checked. Used in `capacity_candidate_density_le`; independence proved from the clock model. |
| Full block capacity estimate | `exponentialRace_block_capacity`, `block_endpoint_capacity` | Nonempty depth block, its minimum a and maximum r, `t > 0`, `B=S(t)-1 > r` | Checked. The expectation identity is `block_fixedPoint_expectation_eq`; a and r are constructed as literal finite extrema. No tail hypothesis is assumed. The eventual cutoff inequality is now discharged in `shell_block_expectation_le`. |
| Square-root shell buffer | `EndpointShellAssumption.buffered_Q_raw` | Only raw shell condition and positive rates | Checked. Both limits retain their order, the coefficient of j is one, and removing the finite cutoff is proved. |
| Fixed-point endpoint expectation tightness | `EndpointShellAssumption.expectation_shells`, `spatial_tail_expectation_le_shells`, `EndpointShellAssumption.expectation_tightness` | Normalization + raw shell condition | Checked. `shell_survivor_buffer` discharges the cutoff from sharp Jensen; `shell_block_expectation_le` bounds the early term by the summable universal envelope `exp(1-sqrt(j))`. `terminalShell_cover` and `spatial_tail_log_lower` discharge the integer/spatial cutoff comparison. |
| Endpoint probability tightness | `EndpointShellAssumption.probability_tightness` | Normalization + raw shell condition | Checked. Calls the proved expectation tightness, tail-count monotonicity, and the existing Markov bound. No tightness premise is carried into the main theorem. |
| Full finite diagonal intensity | `Luce.Shell.intensity_test_bound`, `section4_interior_intensity_bounded`, `section4_full_intensity_finite`, `section4_profileDiagonal_integrable` | Normalization + profile + raw shell | Checked. The eventual expectation bound discharges Fatou's boundedness input. Interior exhaustion proves finite mass; the literal density is then proved integrable. |
| Spatial Poisson and count total variation | `Luce.Shell.section4_full_laplace`, `section4_full_poisson_general`, `section4_count_poisson_general`, `section4_main_poisson_general` | Normalization + profile + raw shell; exact finite Luce law on arbitrary probability spaces | Checked. New proved probability tightness and finite intensity discharge terminal approximation. Interior convergence and discrete TV helpers retain their original meanings. |
| Closed Section 4 contract check | `section4_contractCheck : ShellMigrationContract.section4` in `Luce/Section4ShellContractCheck.lean` | None outside the universally quantified closed contract | Checked. No additional explicit, implicit, or instance parameters. Included in the default build through `Luce/Sections1To7.lean`. |
| Section 5 compact low-rate truncation | `ProfileLimit.interiorLowCycleExpectation_bound`, `interiorLowCycleExpectation_small`, `interior_low_rate_cycles_vanish` in `Section5InteriorLowRates` | Normalization + profile, fixed β<1 | Checked. Root interior membership is explicit. The high-rate threshold is chosen using the existing proved global high-rate theorem; the low-density bound supplies the small delta. No endpoint input or delta < gamma is used. |
| Section 5 cycle-shell tightness | `EndpointShellAssumption.cycle_shell_tightness`, `cycleShell_contractCheck` | Normalization + profile + raw shell only | Proved for the exact expected sum of all lengths 1 through L, including the iterated real limsup. Eventual upper boundedness is proved before using limsup. The independent closed intermediate contract has no external parameters. |
| Section 5 marked return and edge bound | `markedReturnWeight_sum_le`, `ghost_marked_edge_bound` in `Section5ShellMarkedEdge` | Actual ghost background, local nonnegative scalar density envelope | Checked helper. Row sums and window overlap are discharged by the existing concrete ghost lemmas. Background injectivity/nonnegativity are known almost surely; applying and integrating the scalar envelope on source shells is still unresolved. This helper is not a cycle-shell tightness theorem. |
| Section 5 closed main contract | `ShellMigrationContract.section5` in `Section5ShellContract` | Exactly normalization, profile, raw shell, and the defining finite Luce law | Defined and frozen as a closed proposition; NOT proved. No `section5_contractCheck` exists yet. |
| Literal cyclic-intensity representation | `cyclicProfileMeasure_eq_closed_cube`, `cycleTraceIntensity_eq_manuscript` | None beyond the existing definitions | Checked. Product profile measure equals Lebesgue measure on the closed unit cube, including null-boundary conventions. |
| Unique maximum-root counting | `Luce.Section5.cycleMaximum_injective`, `maximumCycleRoots_card`, `bulkCycleCount_eq_maximum_roots`, `tailCycleCount_eq_maximum_roots`, `mem_maximumCycleRoots_iff` | An actual finite permutation; arbitrary real cutoff | Checked. Each cycle has exactly one maximum root and the exact terminal count difference is the number of roots beyond the cutoff. The probabilistic restricted-cylinder bound and integration still remain. |
| Maximum-root expectation identity | `Luce.Section5.tailCycleExpectation_eq_maximum_probability_sum` | Arbitrary probability space and measurable actual permutation; fixed length and cutoff | Checked. The expected terminal cycle count equals the sum of the actual maximum-root event probabilities. No distributional or endpoint estimate is assumed in this identity. The restricted ghost-cylinder bound is now discharged by retained_maximum_cycle_probability_le_ghost. |
| Retained maximum-root cylinder bound | `retainedMaximumCycleEvent_eq_actual_roots`, `exists_retained_maximum_cycle_tail`, `retained_maximum_cycle_probability_le_ghost` | Actual canonical race with positive row rates; arbitrary retained label set | Checked. The event is proved equal to the actual maximum-root event with orbit retention. Every other cycle label remains strictly below the root throughout the cylinder sum. No bound on background rates is used. |
| Return-path reduction and integrated bound | `retained_maximum_ghost_sum_le_return`, `integrable_marked_return_product`, `retained_maximum_cycle_probability_le_return` | Actual row weights; fixed cycle length at least two; arbitrary retained set | Checked. Only internal path restrictions are dropped by nonnegativity; the marked predecessor remains retained and below the root. Integrability is proved from finite sums of actual ghost-cylinder products. Early/late time estimates still remain. |
| Joint cycle Poisson, finite cycle intensities, vector TV | Existing local limits/combinatorics plus remaining completion | Exactly revised manuscript hypotheses | Pre-existing unfinished completion plus new shell obligations; no completion claim. |
| Early ghost-window event | `early_ghost_window_implies_survivor_event`, `early_ghost_shell_probability` | Normalization; fixed cycle length; nonempty shell beyond an explicit length-dependent cutoff | Checked. Clock independence and survivor means come from the canonical race; sharp Jensen supplies the mean lower bound. The finite window-width offset is absorbed by a cutoff depending only on length. |
| Literal early/late edge split | `ghostEntry_eq_early_add_late`, `earlyGhostKernel_ae_eq_count` | Actual positive row rates; injective/nonnegative backgrounds for the representation identity | Checked. The two literal time restrictions partition the original window. Background conditions are proved almost surely by existing race lemmas; the count representation proves measurability, not a change of probability law. |
| Actual early expectation and shell tail | `integrable_early_return_product`, `early_ghost_return_expectation`, `earlyCycleShellTail_limit` | Normalization only; fixed length | Checked. Early integrability is dominated by the proved full marked-return product. The rare-event estimate bounds the actual early contribution by `(2*l+1)^(l-1) exp(1-sqrt(j))`; proved summability gives the tail limit. No shell assumption is needed for this early part. |
| Predecessor shell/time order | `predecessor_shell_index_le`, `predecessor_shell_buffer_le` | Actual predecessor `u < v` and memberships in the two shells | Checked. This discharges the time-range comparison before the predecessor restriction is dropped. |
| Eventual late-density threshold | `EndpointShellAssumption.eventually_buffered_floor_gt_one` | Raw shell condition only | Checked. The proved buffered exponential tail implies `b*(r-sqrt(r)) > 1` for all sufficiently deep nonempty shells, eventually in n. The shell cutoff is chosen before n; no fixed rate floor is asserted. |
| Integrated late marked edge | `late_marked_edge_integral`, `late_source_shell_marked_integral` | Generic helper: positive lower rate b and local threshold b*s >= 1; source-shell instance: actual shell minimum and its threshold | Checked. Integrability is proved by domination. The shell minimum discharges the rate bound and the preceding raw-shell theorem discharges the threshold eventually. The ordered-pair comparison and finite source summation are now proved by the late decomposition results below; their final cycle-count application remains unresolved. |
| Literal late density and finite interchange | `ghost_subwindow_density`, `lateGhostEntry_eq_window_integral`, `late_return_sum_eq_integral` | Actual row weights and ghost windows; nonnegative injective background | Checked. The late probability is the density integral over the original window above the cutoff. Every finite sum/integral interchange has proved integrability. Background restrictions are discharged almost surely in `late_retained_expectation_bound`. |
| Ordered late pair comparison | `late_ordered_pairs_le_common_cutoff`, `late_terminal_source_bound`, `late_interior_source_bound` | Local source set and target cutoffs | Checked. The source-shell cutoff follows from `source_shell_time_le_target` while `u<v` is still retained. The interior cutoff follows from `cutoff_le_terminalShellTime`. Only afterwards is the ordered pair sum enlarged. |
| Combined late expectation | `finite_cover_sum_le`, `late_retained_pairs_bound`, `late_retained_expectation_bound` | Explicit finite retained/interior sets, their geometric cover, interior lower cutoff, and source floor thresholds | Checked local bounds, not a generalized main theorem. `lateGhostKernel_eq_entry` proves the complementary kernel has the literal late-window meaning. `integrable_late_return_product` derives integrability by subtraction of the proved full and early products. The model supplies background hypotheses almost surely. The retained sets still have to be selected and connected to the actual discarded-cycle counts in the final application. |
| Fixed spatial cutoff and interior late decay | `eventually_exterior_shellNumber`, `eventually_interior_density_cutoff`, `tendsto_interior_late_error` | Integer cutoff J0; then fixed positive delta | Checked. The spatial cutoff is `1-exp(-J0)/2`, strictly below one. The row limit is taken after this choice. For fixed delta the buffered time diverges, so the local density threshold holds eventually in J and its exponential error vanishes. No uniform rate of the raw shell condition is used. |
| Full retained probability time split | `retained_maximum_probability_sum_le_time_split`, `retained_deep_probability_bound` | Actual row weights, retained set and deep target set; local cutoff premises in the finite upper bound | Checked. The probability sum is for the original maximum-cycle event. Early and late integrals use the same target-dependent cutoff and their proven integrability. |
| Deep-target early reduction | `early_deep_expectation_le_shell_sum`, `early_finite_shell_sum_le_tail`, `buffered_finite_shell_sum_le_tail` | Positive row weights, positive integer shell cutoff (at least two for buffered costs) | Checked. The shell-number identity identifies each target cutoff with its original shell. Finite real sums are bounded by the exact extended-nonnegative tail sums, preserving empty shells and the order of limits. |
| Concrete retained sets | `not_mem_retainedCycleLabels`, `retainedInteriorLabels_rate`, `eventually_retained_labels_cover` | Literal high-rate cutoff M, fixed interior beta, and low-rate cutoff delta | Checked. Excluded labels are exactly high rates globally or low rates in the fixed interior. These are finite truncation sets, not restrictions on the model type. |
| Retained cycle tightness | `EndpointShellAssumption.retained_maximum_tightness`, `retainedDeepCycleCount_expectation`, `EndpointShellAssumption.retained_expectation_tightness` | Normalization and raw shell condition only; fixed length and error tolerance | Checked. Source cutoff J0 is chosen before fixed delta; target cutoff J is chosen afterwards and before n. All local floor, cover, rate, integrability, and scalar-error premises are discharged. No tightness predicate or extra model hypothesis is an input. The excluded-cycle contribution is not included in this retained result. |

## Representation obligation

`ShellMigrationContract.old_fullIntensity_underlying` is checked. It proves the underlying measure of the historical `fullIntensity` is definitionally the measure fixed by the frozen contract. This is a representation identity under the historical constructor's parameters, not a shell theorem or a substitute contract check.

## Representation and audit completion

`Luce.Shell.fullIntensity_eq_uniform` proves that the new constructor gives the same finite intensity as the historical constructor in the stronger uniform case. The contract check fixes its underlying measure to the raw projected density by definitional equality. Neither constructor changes the meaning of the intensity.

The completed main theorem and contract check are printed with all implicit and instance parameters in `audit/shell-statements-and-axioms.log`. Actual transitive axiom output is also recorded there. Build commands and outcomes are in `audit/shell-build-results.md`.

## Section 5 cycle-shell milestone and remaining gaps

The previous first unresolved proposition (actual endpoint expectation tightness) is now `EndpointShellAssumption.expectation_tightness`, proved under exactly normalization and the raw shell condition. No Section 4 obligation remains.

Compact low-rate truncation and full short-cycle shell tightness are now proved. The length-one bridge uses Section 4, while all longer cycles use the retained shell argument followed by both finite truncations. Their finite sum gives the exact manuscript proposition under normalization, profile convergence, and the raw shell condition.

Using the existing exact cycle objects, the proved proposition (also the closed `ShellMigrationContract.cycleShell`) is:

```lean
∀ (w : Luce.WeightArray) (f : ℝ → ℝ),
  Luce.NormalizedWeights w → Luce.ProfileLimit w f →
  Luce.EndpointShellAssumption w → ∀ L : ℕ,
  Filter.Tendsto (fun α : ℝ => Filter.limsup (fun n : ℕ =>
    ∫ e, (∑ k : Fin L,
      (Luce.Section5.cycleCount (Luce.raceRankPermutation e) k.val -
       Luce.Section5.bulkCycleCount (Luce.raceRankPermutation e) α k.val : ℕ) : ℝ)
      ∂Luce.exponentialRace (w n)) Filter.atTop)
    (𝓝[<] (1 : ℝ)) (𝓝 (0 : ℝ))
```

Here k indexes cycle length k+1, and natural subtraction is the exact difference between all cycles and cycles contained in the interior cutoff. Neither count was changed. The maximum-root, ghost-cylinder, early/late, source/interior, and truncation obligations are now discharged in the order M, J0/beta, delta, J. No source contradiction was found in this endpoint argument.

`deep_roots_le_retained_add_excluded` charges discarded roots to excluded short-cycle vertices with a fixed length factor. `excluded_short_vertices_le_high_add_interior_low` separates those vertices into the two manuscript truncations. `tail_cycle_expectation_le_retained_and_truncations` proves integrability and the expectation inequality. `cycle_tail_small_succ` makes all three terms small in the required order; `singleton_cycle_tail_le_fixed` handles length one almost surely under the canonical race. `short_cycle_tail_small` chooses a common cutoff for finitely many lengths, and `cycle_shell_tightness` proves the full limit. None of these estimates is an extra assumption of the result.

The historical global low-rate truncation was not used in this proof. Its replacement used fixed-interior low-rate truncation first. The full `ShellMigrationContract.section5` is not proved and has no claimed contract check. No missing Section 5 result has been introduced as a hypothesis or axiom.

## Factorial expectation and local-limit assembly

| Intermediate fact | Completed theorem | Hypotheses used | Discharge / remaining work |
|---|---|---|---|
| Original block indicator integrates to the rank cylinder probability | `integral_cycleBlockIndicator_eq_rank_probability` | Positive finite row rates | Injective clocks hold almost surely under the original exponential race; cylinder measurability is proved. |
| Actual joint falling-factorial expectation is the injection sum divided by the rotational multiplicity | `bulk_factorial_expectation_eq_rank_sum` | Positive finite row rates; arbitrary L, m, alpha | Existing exact deterministic counting identity; finite permutation statistics are proved integrable, not assumed. |
| Reindexing the block vertices preserves cutoff and rank event | `bulk_factorial_expectation_eq_cyclicRaceSum` | An equivalence of finite indexing sets | Canonical `Fintype.equivFin` supplies the equivalence in the next theorem. No model restriction. |
| Bulk moments converge to the literal block cyclic integral | `bulk_factorial_tendsto_canonical_block_integral` | Normalization and original profile convergence; alpha < 1; positive moment order | Apply checked `section5_cyclic_local` to the constant-one test. The positive-size condition selects the moment index and is not a rate assumption. |
| Zero-order moment equals one | `bulk_factorial_expectation_zero` | Positive finite row rates | Exact identity for every n, including zero. |
| Block integral factors as product of individual truncated cycle intensities to powers m | `canonical_block_integral_eq_intensity_product` | Arbitrary density f; finite multiplicities | PROVED. `measurePreserving_block_uncurry` proves regrouping by blocks preserves the product measure; `integral_block_product` applies Fubini; `cyclic_integral_reindex` preserves the original permutation and density. The original rotation divisor gives exactly the intensity product. |
| Every bulk joint factorial moment, including zero order, converges to the intensity product | `bulk_joint_factorial_moments` | Normalization, original profile convergence; alpha < 1 | PROVED. Positive order uses the canonical block limit and factorization. Zero order is discharged internally, with no indexing or representation witness as an input. |
| Bulk first-moment convergence, integrability of its literal truncated density, and nonnegativity of its intensity | `bulk_cycle_first_moment`, `bulk_cycle_trace_integrable`, `bulk_cycle_intensity_nonneg` | Normalization/profile for the limit and sign; profile alone for integrability; alpha < 1 | PROVED. First moment specializes the full multiplicity vector to one cycle; nonnegativity follows from nonnegative count expectations. |
| From joint point-mass convergence to weak/TV vector convergence | UNRESOLVED | Now-proved actual joint point-probability limits to a probability law | The factorial-moment passage to joint point probabilities is complete. The countable-space total-variation and bounded-test passage still require kernel-checked proofs. |
| Finite point-probability expansion and factorial remainder | `countSieve_eq_factorial_sum`, `countSieve_error_factorial`, `countVectorSieve_error` | Natural counts, target counts, and finite truncation order only | PROVED pointwise. Inclusion-exclusion gives an error bounded by the next factorial term. A product estimate gives the joint-vector remainder using only higher mixed factorial moments. |
| Expectations of finite factorial polynomials and the actual count-vector approximation converge | `bulk_factorial_polynomial_limit`, `bulk_count_sieve_expectation_limit` | Normalization and original profile convergence; alpha < 1 | PROVED. Finite sum/product expansion reduces to already-proved joint moments; finite-row integrability is discharged. Signed coefficients introduce no new assumption. |
| Integrate the point-probability remainder and compute its row limit | `bulk_sieve_error_integral_le`, `bulk_sieve_remainder_limit` | Actual finite row; normalization/profile for the limit | PROVED. All finite-row integrability is discharged. Two-term factorial polynomials compute the exact limiting error bound. |
| Remove the point-probability expansion cutoff | `factorial_remainder_tendsto_zero`, `limitingSieveRemainder_tendsto_zero`, `count_vector_sieve_series_limit`, `bulk_cycle_point_indicator_limit` | Real intensity values; normalization/profile for the concrete cycle limit | PROVED. Summability of the exponential series gives vanishing factorial remainders. `tendsto_of_finite_approximation` preserves the order of limits and all its premises are discharged. |
| Actual joint bulk point probabilities converge to the product Poisson law | `bulk_cycle_point_probability_limit` | Normalization, original profile convergence; alpha < 1 | PROVED. `measurable_race_permutation_statistic` and `bulk_cycle_point_indicator_integral` identify the actual event probabilities. `bulkCycleVectorPoissonLaw_real_singleton` identifies the limit with the product measure, using proved nonnegative intensities. |
| Uniform Cauchy bound and boundedness of truncated intensities | `EndpointShellAssumption.bulk_intensity_tail_small`, `EndpointShellAssumption.bulk_intensity_bounded` | Normalization, original profile convergence, raw shell condition | PROVED. Difference of bulk expectations is bounded by the actual endpoint expectation. Both bulk expectations converge; the shell-derived cutoff is chosen before beta and the row limit. |
| Literal full cyclic-density integrability and nonnegative full intensity | `EndpointShellAssumption.cycle_trace_integrable`, `cycle_intensity_nonneg` | Normalization/profile/raw shell for integrability; profile alone for sign | PROVED. `cyclic_cube_aecover` covers almost every point by interior cubes. Proved local integrability, nonnegativity, and uniform integral bounds discharge all premises of the improper-integral theorem. No finite-intensity input is used. |
| Truncated intensity tends to the unchanged full intensity | `EndpointShellAssumption.bulk_intensity_tendsto` | Normalization, profile, raw shell | PROVED. The real cutoff tends to one from below. `cyclicProfileMeasure_restrict_closed_bulk` proves the exact measure representation, and full integrability justifies the integral limit. |
| Cutoff removal for count-vector laws, joint weak and vector-TV convergence, and transport to arbitrary finite Luce laws | UNRESOLVED | Proved first moments, full intensity limits, and full cycle-shell expectation tightness; truncated vector convergence still needed | Original closed main Section 5 contract remains unchanged and unproved. |

The revised manuscript's final argument uses the cyclic local limit and factorial identity directly. Historical references to a separate remaining “truncated cyclic sum/late-order estimate” are not treated as additional required assumptions. The endpoint late-order estimates, block-integral factorization, and full intensity finiteness are now proved. The current missing steps are distributional completion and endpoint cutoff removal for the count-vector laws.

Validation: `lake build` passed (3873 jobs); `lake env lean audit/Section5FactorialLocal.lean` passed; `python audit/validate_section5_factorial_local.py` passed. Seven transitive axiom reports contain only propext, Classical.choice, and Quot.sound. All three semantic snapshots (162, 193, and 1 files) are unchanged. Full types and actual outputs are in `audit/section5-factorial-local-audit.log` and `audit/section5-factorial-local-axioms.txt`.

Latest validation, after completing factorization and all bulk joint moments: `lake build` passed (3876 jobs); `lake env lean audit/Section5FactorialMoments.lean` passed; `python audit/validate_section5_factorial_moments.py` passed. All nine new transitive reports use only the three allowed foundational axioms, and all three frozen snapshots remain unchanged. See `audit/section5-factorial-moments-audit.log` and `audit/section5-factorial-moments-axioms.txt` for actual fully elaborated statements and outputs. These audits cover the proved moment milestone; no full Section 5 contract-checking theorem is claimed.

Full-intensity validation: `lake build` passed (3878 jobs); `lake env lean audit/Section5IntensityFinite.lean` passed; `python audit/validate_section5_intensity_finite.py` passed. All eight transitive reports contain only the three allowed foundational axioms, with the 162/193/1-file snapshots unchanged. Exact types and output are in `audit/section5-intensity-finite-audit.log` and `audit/section5-intensity-finite-axioms.txt`. Full density integrability and intensity cutoff convergence are proved; the main vector-law contract remains incomplete.

Finite point-probability expansion validation: `lake build` passed (3880 jobs); `lake env lean audit/Section5FactorialSieve.lean` and `python audit/validate_section5_factorial_sieve.py` passed. All eleven transitive reports contain only the three allowed foundational axioms; all three semantic snapshots are unchanged. The actual types and outputs are in `audit/section5-factorial-sieve-audit.log` and `audit/section5-factorial-sieve-axioms.txt`. The deterministic joint remainder and convergence of the actual finite approximation are proved; removal of its expansion cutoff is still unresolved.

Actual bulk joint point-probability validation: `lake build` passed (3884 jobs); `lake env lean audit/Section5BulkPointProbability.lean` and `python audit/validate_section5_bulk_point.py` passed. All twelve transitive reports contain only the three allowed foundational axioms, with all frozen snapshots unchanged. Exact types and output are in `audit/section5-bulk-point-audit.log` and `audit/section5-bulk-point-axioms.txt`. The expansion cutoff is now removed and actual bulk point probabilities converge to the product Poisson law. Spatial cutoff removal, weak/TV vector convergence, arbitrary-law transport, and the full closed contract check remain incomplete.

