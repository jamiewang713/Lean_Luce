# Section 6: checked population-estimate components

Latest completion: **Lemma 6.12 is proved in full** by `lemma612`, with a
separate check against the original three interior-grid limit contracts.
The new `section6` theorem also closes the existing full Section 6 target on
both grids. Full build: 4567 jobs passed; all four new theorem types and
transitive-axiom checks passed using only the standard foundational axioms.
Original Lean inputs and toolchain hashes match. A separate manuscript edit
during the run is recorded without resetting the baseline; the statement of
Lemma 6.12 is unchanged. See `section6-lemma612.md` and
`audit/section6-lemma612-validation.json`.

Latest completion: **Section 6.6 is proved in full** by `critical` and the
independent `critical_contractCheck`, including both sampling grids. The result
contains the fixed-point Gaussian limit, its mean asymptotic, and uniformly
bounded means for every longer cycle. The full build passed (4564 jobs), and all
122 new declarations passed the transitive-axiom audit with only the three
standard foundational axioms. The original contract/profile definitions are
unchanged. See `section6-critical.md` and `audit/section66-validation.json`.
Earlier statements that the critical application remains open are historical.

Latest completed result: **Proposition 6.11 is proved in full** by `proposition611`
and `proposition611_contractCheck`, imported through `Luce.Section6`. The proof
uses compensated characteristic functions, predictable deletion, and Levy
convergence. It covers varying finite row lengths and probability spaces, the
full bounded-continuous-test conclusion, and the separate expectation assertion.
No independence or extra moment assumptions are added. See
`section6-proposition611.md` and `audit/section6-proposition611-validation.json`.
Verification: the full default build passed (4466 jobs); all 55 new theorems
passed the dedicated type and transitive-axiom audit, with only `propext`,
`Classical.choice`, and `Quot.sound`. The closed contract hash is unchanged.
The model-specific critical-pole application and other main CLTs remain open.

Latest completed result: **Proposition 6.10 is proved in full** by `proposition610` and the separate `proposition610_contractCheck`, imported through `Luce.Section6`. The actual mixed factorial moments are compared with the exact ideal category means, retaining all root restrictions and rotational factors. Mixed corners use the same exponential race. Collision and nonmoderate-edge errors are derived from the proved matrix bounds, and the full local law handles the remaining assignments. All cutoffs and constants are constructed from the original profile and sampling assumptions. See `section6-proposition610.md`.

Verification: full build passed (4412 jobs), and all 87 new theorem declarations in 38 modules passed the dedicated audit using only the three permitted foundational axioms. All prior Proposition 6.5 proof component hashes match. The regenerated whole-Section-6 audit has 1070 successful reports out of 1075; the five missing names are caused by its pre-existing omission of the `TraceDensity68` namespace. The failed run and historical baselines are preserved. The new 17-file snapshot matches all 16 non-manuscript files; an external manuscript edit after the snapshot is recorded without changing or resetting the baseline. This proof task did not edit the manuscript. Results are in `audit/section6-proposition610-validation.json`. Subsequent CLTs remain separate obligations.

Latest completed result: **Lemma 6.8 is proved in full** by `lemma68` and the independent `lemma68_contractCheck`, imported through `Luce.Section6`. The total trace retains a common boundary constant, and exact nested-interval subtraction gives the spatial estimate with K = 1 and kappa = 1. All hypotheses in the closed statement are discharged from the original profile and active-corner assumptions. Full build passed (4374 jobs); the dedicated transitive axiom audit passed with only `propext`, `Classical.choice`, and `Quot.sound`. The manuscript is unchanged. See `section6-lemma68.md` and `audit/Section6Lemma68.lean`. The broader-audit failures recorded below are historical runs before these modules were imported.

Latest completed result: **Lemma 6.9 is proved in full**, including overlaps, nearby actual labels, and the union over all pairs of distinct slots. The proof handles same-cycle and different-cycle pairs, either or both corners, and repeated vertices. Its mixed-corner bound needs no additional core-separation assumption. `lemma69` and the independent `lemma69_contractCheck` are imported through `Luce.Section6`. See `section6-lemma69.md` for the proof and exact scope.

Verification: full build passed (4342 jobs); all 34 new theorem declarations in 11 modules passed their type and transitive axiom audit, using only `propext`, `Classical.choice`, and `Quot.sound`. All 14 files in the new contract snapshot match, including the manuscript and toolchain. The regenerated all-source audit snapshot contains 950 declarations; unrelated unimported concurrent Lemma 6.8 declarations cause it to reach its existing error limit after 531 successful reports. That broader audit is reported as failed, separately from the passing Lemma 6.9 audit. Historical hash baselines were preserved. Exact evidence: `audit/section6-lemma69-validation.json`.

Latest completed result: **Proposition 6.5 is proved in full** by the normalized-gap proof. The exact closed local-law contract, its interval consequence, and the global matrix conclusion are exposed by `proposition65_localLaw`, `proposition65`, and the separate `proposition65_contractCheck`, imported through `Luce.Section6`. No added model assumptions, axioms, or frozen-definition changes are used. See `section6-proposition65-normalized-gaps.md`.

Verification for this completion: full `lake build` passed (4331 jobs); the dedicated audit checks all 63 new theorem declarations in 23 modules, including the elaborated contracts, and permits only `propext`, `Classical.choice`, and `Quot.sound`. The regenerated whole-Section-6 audit snapshot contains 847 source declarations and 825 successful axiom reports; 22 declarations in unrelated concurrent increment-density, convolution, and continuous-trace files are absent from the aggregate imports. That broader audit and the historical validator therefore do not pass. All reported axioms are permitted. The Proposition 6.5 snapshot matches 46 of 47 files: the manuscript mismatch was present at the start of this run. The manuscript, historical baselines, and checkers were not edited. Exact results: `audit/section6-proposition65-validation.json`.

The entries below are historical checkpoints. Their claims that Proposition 6.5 remains open are superseded by the completed proof above; later Section 6 CLTs remain outside this completion.

Latest Proposition 6.5 work: nine additional results derive remaining-weight concentration at the profile scale and connect actual gap rates to the surviving-weight path at random times. Fixed-time bounds and the deterministic connection are proved; the joint probability/error assembly remains open. Concurrent Lemma 6.6 files are present in the worktree and are not attributed to this work. Detailed verification results are in the Proposition 6.5 audit.

Verification: full build passed (4291 jobs); a separate audit checks exactly the nine new types and their permitted axioms. The full generated audit currently fails on eight concurrent Lemma 6.6 declarations absent from its imports, so no whole-project passing audit is claimed. The 47-file frozen contract passes unchanged. Exact evidence: `section6-proposition65-weight-scale-evidence.md`.

Latest Proposition 6.5 work: 711 source declarations. Both actual marked-gap displacement bounds are proved. An alternative remaining-weight argument now proves its exact mean, a variance bound by the deleted second-rate-moment population, and Chebyshev concentration, with no normalization or uniform rate hypothesis. Its endpoint-scale/random-time and weighted-error assembly remain unproved, as does Proposition 6.5. The proposed polynomial-concentration route preserves the frozen conclusion.

Verification: full build passed (4289 jobs); all 711 current type/axiom reports passed using only permitted foundational axioms; the 47-file frozen contract is unchanged. The older manuscript-hash validator remains failed.

Latest Proposition 6.5 work: 704 source declarations. Left and right gap-quantile window bounds now derive their deleted-mean separation from the original profile. Six new results account for finite deletion, convert additive separation to relative thresholds, and apply the actual gap-time tails. The final rank-displacement/cutoff conditions and uniform exponent estimates remain to be discharged; remaining-weight concentration and Proposition 6.5 remain unproved. Details are in the primary audit.

Verification: full build passed (4287 jobs), all 704 current source/type/axiom reports passed with permitted axioms only, and the 47-file contract snapshot is unchanged. The older manuscript-hash validator still fails.

Latest Proposition 6.5 work: 698 source declarations. Seven new gap-time results transfer the relative count tails to early and late actual deleted gap starts, using both arrival and survivor means. The proof handles the sentinel gap and exact undeleted cardinality. Numerical mean-separation premises are explicit helper premises; their profile discharge, the full quantile/skeleton concentration theorem, and Proposition 6.5 remain unproved. See the primary audit for verification results.

Final verification: full build passed (4284 jobs), all 698 type/axiom reports passed with permitted foundational axioms only, and the 47-file Proposition 6.5 freeze passed unchanged. The older manuscript-hash validator remains failed.

Latest Proposition 6.5 work: five new relative-count results, bringing the source declarations to 691. Both upper and lower tails of actual deleted arrivals and survivors now have the bound exp(-eps^2*m/4) for 0<=eps<=1, using their exact deleted-population means. This supplies shrinking-scale count concentration. Quantile bracketing, remaining-weight concentration, the full joint skeleton estimate and the final local approximation remain unproved. Verification details are recorded in `section6-proposition65-audit.md`.

Verification: full build passed (4283 jobs), complete 691-declaration type/axiom check passed with permitted axioms only, and the 47-file Proposition 6.5 freeze is unchanged. The older manuscript-hash validator remains failed; no baseline was reset.

Current Proposition 6.5 milestone: the exact actual-cylinder skeleton formula is proved in `Section6CylinderSkeleton`, using proved selected-coordinate Fubini and marked-gap reindexing. The literal later-rate sum and chronological skeleton-displacement bound are also proved. This closes the exact integration assembly identified at the 680-declaration milestone. Joint concentration and the full local approximation remain unproved. The full final build passed (4281 jobs). See `section6-proposition65-audit.md` for the final audit results and remaining obligations.

Latest Proposition 6.5 milestone: 680 checked declarations. Eight new supporting results prove actual deleted-order transfer, the separated-cylinder order sum, the endpoint buffer needed for finite gap indices, and normalized-product integrability. Full build passed (4277 jobs); all 680 type/axiom reports passed with permitted axioms only; the 47-file Proposition 6.5 freeze is unchanged. The older validator still fails on its preserved manuscript hash. Proposition 6.5 itself remains unproved. See `section6-proposition65-audit.md` for the remaining obligations and `section6-proposition65-transfer-evidence.md` for exact new types and axiom output.

Full Section 6 formalization is **incomplete**, but **the full Lemma 6.4 is now proved with a separate closed contract check**. Later lemmas and the main CLT contracts remain unproved. This does not claim completion of the earlier Sections 4/5 shell migration.

Read: the complete Section 6 of `fixed_points_sampled_profile.tex`, its introductory assumptions and theorem statements, and the relevant existing finite-model, cycle, maximum-root, profile-kernel, and deleted-race definitions/proofs. The iid scalar CLT in the pinned mathlib was inspected and cannot supply the required dependent CLTs directly.

## Completed work

Proposition 6.5 selected-coordinate milestone (672 declarations): the complete pointwise insertion product is factored into skeleton survival and selected-gap corrections with literal later-mark rate sums. `selected_coordinate_integral` now proves the exact integration over selected coordinates for fixed actual chain/unselected coordinates, including integrability. Every ordered-rate positivity and later-rate nonnegativity premise is discharged internally. A recursive simplification was replaced by explicit finite-product conversions; no checking settings changed. Dependency branch passed (3589 jobs), full build passed (4274 jobs), and complete type/axiom audit exited 0 for 672 declarations with permitted axioms only. New Proposition 6.5 freeze unchanged; old validator still fails at manuscript hash after declaration/axiom checks. Remaining: actual deleted-race transport, joint concentration, weighted errors and final local-law contract check. The full proposition is not claimed complete.

Proposition 6.5 integration milestone (663 declarations): exact scalar and finite-product selected-spacing integrals are proved, with integrability and the full later-mark survival factor retained. The new skeleton start is proved to equal the actual previous ordered clock minus the selected earlier raw gaps, and to depend only on unselected coordinates. These are concrete progress toward the conditional formula, not the full joint local law. Branch builds passed (2781 and 3586 jobs); full build passed (4272 jobs); complete elaborated-type/axiom audit exited 0 for 663 source declarations with only permitted axioms. The Proposition 6.5 47-file freeze remains unchanged. The original full validator still fails on its preserved manuscript hash after declaration/axiom checks. Remaining: reorganize and integrate the actual selected survival product, transport through the eliminated-race law, prove joint skeleton concentration and weighted errors, and establish the full frozen contract and separate check. See `docs/section6-proposition65-audit.md`.

Proposition 6.5 first milestone (654 declarations): the actual next result is the joint local insertion law, not the integrable-density lemma (the earlier numbered conversational summary skipped this proposition). Its full quantitative mixed-corner contract, exact K/H kernels, [A,B] consequence and global fallback are defined and compiled, independently of implementation. The new 47-file snapshot is frozen and unchanged. Four supporting proofs establish the selected-gap rational correction, rank separation implying distinct background gaps, and the exact separated-cylinder expectation of a product. The main local approximation and separate Proposition 6.5 contract check remain UNPROVED. Sequential branch builds and the final default build passed (4270 jobs); full elaborated-type/axiom audit passed for 654 declarations with permitted axioms only. The original validator still fails on its old manuscript hash. The manuscript also now differs from the Lemma 6.4 45-file snapshot after an external edit; the current lemma/proposition statements were reread and match, but no old full-file equality is claimed. Details and exact remaining obligations: `docs/section6-proposition65-audit.md`. The completed Lean Lemma 6.4 theorem remains unchanged.

Completion milestone (650 declarations; supersedes historical entries below): `lemma64_matrix : Lemma64Contract.matrix` proves every frozen matrix clause with no additional inputs. The construction uses the actual matrix at order r+1; obtains endpoint cutoffs; intersects them before applying right outside-corner and interior bounds; constructs the common weighted exponent; and adds all finite constants. `lemma64 : Lemma64Contract.lemma64` combines it with the checked cycle theorem. `lemma64_contractCheck` is a separate closed theorem importing the implementation and frozen contract. Both declarations have no explicit, implicit or instance parameters. The contract, model/count definitions and 45-file snapshot remain unchanged. The dedicated contract branch passed (4064 jobs), default build passed (4266 jobs), and the complete Lean elaborated-type/transitive-axiom audit exited 0 for 650 declarations, with only propext, Classical.choice and Quot.sound. The old global validator still fails at its preserved manuscript hash after declaration/axiom checks; it was not altered or bypassed. Detailed completion evidence, exact closed contract and actual elaborated output: `docs/section6-lemma64-completion.md`. The full Lemma 6.4 objective is achieved; no main CLT is claimed.

Latest milestone (645 declarations; supersedes historical entries below): `PowerProfile.active_matrix_target_columns` constructs one C and delta for both active endpoint target maxima and column sums. The two endpoint case-analysis helpers derive the estimates from the original profile, with inactive endpoints vacuous. All rows, sources and source subsets are covered. The initial automatic positivity failure was resolved by explicitly composing the four proved positive constants, without changing the statement. Default build passed (4262 jobs); complete Lean type/axiom audit exited 0 for 645 declarations, only permitted axioms. The 45-file freeze passes; full validator passes declaration/axiom checks then fails on the preserved older manuscript hash. Full matrix contract and full Lemma 6.4 contract check remain unproved; the next assembly must intersect these cutoffs with the envelopes before obtaining interior and outside-right estimates.

Latest milestone (642 declarations; supersedes historical entries below): `PowerProfile.common_weighted_exponent` constructs a positive exponent compatible with both active endpoints. `PowerProfile.domination_matrix_common_weighted_rows` then proves both actual global weighted rows with one C and that exponent, with all endpoint exponent premises discharged internally. Inactive endpoint clauses are vacuous. The module compiled; default build passed (4261 jobs); the complete Lean type/axiom audit exited 0 for all 642 source declarations with only permitted foundational axioms. The 45-file freeze passes; the complete validator passes declaration and axiom checks then fails on the preserved older manuscript hash. Full matrix assembly and the full Lemma 6.4 contract check remain unproved. The failed initial dependent rewrite was fixed by substituting endpoint equalities, without changing the statement.

Latest milestone (640 declarations; supersedes historical entries below): both endpoint envelope/fixed-target assemblies are proved. `PowerProfile.domination_matrix_right_envelope_assembly` returns a positive cutoff delta0 and integer H, then for any final 0<delta<=delta0 constructs one C,d,nu for the exact corner envelope, fixed-target source-depth decay and outside-corner decay in n. `PowerProfile.domination_matrix_left_envelope_assembly` uses one C and integer H for the exact corner envelope and fixed-left C*theta_i/n^alpha bound. Only original profile/sampling inputs and fixed numerical budgets occur; no intermediate estimate is assumed. Right and left branch builds passed (3829 and 3825 jobs), default build passed (4260 jobs), and the complete Lean type/axiom audit exited 0 for 640 source declarations. Both new reports contain only propext, Classical.choice and Quot.sound. The 45-file freeze passes; the full validator passes declaration/axiom checks then fails on the preserved older manuscript hash. Remaining: assemble endpoint target/column and weighted rows with these envelopes and interior/cylinder bounds into the complete matrix contract, then prove the full closed contract check. Lemma 6.4 remains incomplete.

Latest milestone (638 declarations; supersedes historical entries below): the complete CLOSED cycle component `lemma64_cycles : Lemma64Contract.cycles` and separate `lemma64_cycles_contractCheck` are now included in the full build and axiom audit. They construct one C and delta for interior root expectations, both active endpoint root expectations, and real-cutoff discarded-cycle expectations. No helper estimate is an input. New decay compatibility proofs construct common parameters; `PowerProfile.domination_matrix_fixed_right_common` proves source-depth and outside-corner decay with the SAME d,nu below any positive envelope parameters, using only the original profile/sampling for the actual matrix estimate. Default build passed (4258 jobs); the full Lean type/axiom audit exited 0 for all 638 source declarations, using only propext, Classical.choice and Quot.sound. The 45-file full-lemma freeze passes. The complete validator still fails at the older manuscript hash, after passing declaration and axiom checks. Full Lemma 6.4 remains incomplete: assemble the complete matrix contract with common C,delta,kappa and endpoint parameters, then prove the separate full conjunction contract check. No full lemma or CLT is claimed.

Latest milestone (627 declarations; supersedes historical entries below): all-n middle matrix target maxima and columns are proved, and the exact fixed-left rate bound C*theta_i/n^alpha now holds for all rows. Its small-row constant is constructed from finitely many samples on both grids. The independent CLOSED full target `Luce.Section6.Lemma64Contract.lemma64` is defined and compiled, with no implementation theorem or proof-input assumption. Its entire 45-file dependency/source/configuration closure is frozen in a NEW baseline, and the detailed audit is in `docs/section6-lemma64-contract.md`. This does not reset or fix the older manuscript-hash mismatch. The new freeze check passes; the old full validator still fails on the manuscript after passing declaration/axiom checks. Separate branches passed (3818 and 3824 jobs), default build passed (4253 jobs), and the complete Lean type/axiom audit exited 0 for 627 declarations with only permitted axioms. The full contract-checking theorem remains unproved; next work is constructing the shared matrix constants/cutoffs and assembling its complete clauses and cycle-count conclusions.

Latest milestone (623 declarations; supersedes historical entries below): the full O(1) discarded-cycle expectation is now proved at BOTH active endpoints for REAL cutoffs. `PowerProfile.right_discarded_expectation` and `left_discarded_expectation` in `Section6DiscardedExpectation` have only original profile/sampling inputs and B/n<=delta; C and delta are constructed uniformly in n,A,B. The new `intervalDiscardedCycleCount` is the literal cardinality of unrooted cycles whose largest-label roots lie in [A,B] and which have a vertex outside [A,B]. Exact rounding, including empty intervals, preserves that count. No old count, model or probability-law definition changed. The two integer assembly branch builds passed (4034 jobs each), then the default build passed (4249 jobs). The full Lean type/axiom audit exited 0 for 623 declarations, with only permitted axioms. The validator still fails at the manuscript hash after declaration/axiom checks. The current manuscript's assumption and Lemma 6.4 statement were reread (now lines 385 and 2141); the excursion statement matches this component. Full Lemma 6.4 remains incomplete: finish the uniform matrix statement (including small-row middle bounds), audit all bounds against the complete statement, and prove the closed combined contract check. No main CLT is claimed.

Latest milestone (615 declarations; supersedes historical entries below): both literal excursion kernel sums are bounded by 1+1/kappa for every kappa>0 and positive integer cutoffs 1<=A<=B. `Section6ExcursionDepthSums` proves the integral comparison, both power-sum bounds, and exact conversion to the manuscript summands. `Section6FilteredCycleCount` proves that filtering actual unrooted cycles is exactly filtering their unique maximum roots, and that its expectation equals the finite sum of actual root-event probabilities. No measurability or integrability of the filtering predicate is assumed; the finite permutation space supplies these facts. Both modules compiled; separate branch builds passed (3405 and 3977 jobs), followed by the default build (4244 jobs). The full Lean type/axiom audit exited 0 for 615 declarations with only permitted axioms. The validator still fails at the preserved manuscript hash after declaration/axiom checks. Remaining: combine the actual discarded-cycle predicate, endpoint probability bounds and finite depth sums; handle cutoff representation explicitly without restricting the manuscript statement; finish the full Lemma 6.4 contract check.

Latest milestone (607 declarations; supersedes historical entries below): actual escaping-cycle probabilities now have the endpoint bounds (K/m)*(m/B)^kappa on the right and (K/m)*(A/m)^kappa on the left. `Section6EscapingCycleCylinder` proves the escaping orbit-to-tuple correspondence, actual finite cylinder union bound, and closing-edge/path comparison. `Section6EndpointExcursionProbability` discharges the path and target premises and constructs kappa=beta/2 or alpha/2 internally. `Section6MaximumRootExcursion` proves both exact largest-root interval-exit equivalences. No profile assumption, model, probability law or count definition was changed. All seven new declarations compiled; default build passed (4242 jobs), and the full Lean type/axiom audit exited 0 for 607 declarations, with only permitted axioms. The validator still fails only at the preserved manuscript hash check after declaration/axiom checks. Remaining: literal discarded-count expectation assembly, uniform summation over root depths, and the complete Lemma 6.4 contract check.

Latest milestone (600 declarations; supersedes historical entries below): weighted escaping-path estimates are proved on both sides, using the actual global domination matrix. `Section6WeightedPaths` bounds the weighted occupation sum along an unrestricted path and hence paths reaching a potential threshold. `PowerProfile.right_escaping_path_bound` and `left_escaping_path_bound` discharge the generic row premises from original profile/sampling inputs, giving k*C^k*(m/B)^kappa and k*C^k*(A/m)^kappa, respectively. The chosen exponent is an intermediate parameter in the proved manuscript range, not a new profile assumption. Conversion to actual discarded-cycle expectations, the uniform root-depth sum, and the final combined lemma/contract check remain unresolved. The first combined build ran out of memory on parallel endpoint compilation; separate endpoint builds passed (3825 jobs each), followed by the default build (4239 jobs). The full Lean type/axiom audit exited 0 for 600 declarations with only permitted axioms. The full validator still fails on the preserved manuscript hash after passing declaration and axiom checks.

Latest milestone (596 declarations; supersedes historical entries below): interior cycle-through-vertex probabilities are bounded by C/n for every row size. Both active-endpoint and interior largest-root expectations are proved in `Section6MaximumRootExpectation`. The exact finite count of unrooted cycles with a prescribed maximum equals the root indicator (`cycle_count_at_maximum_eq_indicator`), preserving the counting meaning. Inputs remain the original profile and exact sampling, with the stated endpoint/interior regions. Default build passed (4236 jobs); the full Lean type/axiom audit exited 0. The validator passed declaration-set and permitted-axiom checks, then failed on the preserved manuscript hash; the other 12 frozen files remain unchanged. Uniform excursion loss and full combined Lemma 6.4 contract verification remain unresolved. No main CLT is proved.

Latest milestone: actual cycle-through-vertex probabilities are <=C/m at either active endpoint for every fixed cycle length, including length one, under original profile/sampling inputs. The generic closing-edge/path argument has every matrix premise discharged in the two concrete endpoint theorems. Default build passed (4234 jobs); the full Lean type/axiom audit completed for 590 declarations with only permitted axioms, including the reused cycle/path dependencies. The validator still fails the preserved manuscript hash after declaration/axiom checks. Largest-root expectation conversion, interior bounds, excursion loss and final contract verification remain unresolved.

Latest milestone: complete active-right columns are proved in `Section6DominationMatrixFixedRight`, including fixed terminal depths and final infinite insertion gaps. Uniform source decay is obtained from the checked moment bound, with small sources/rows absorbed by constructed constants, then summed. Both endpoint column results now have no lower-depth premise. Default build passed (4232 jobs); the full Lean type/axiom audit completed for 587 declarations with only permitted axioms. The validator still fails the preserved manuscript hash after declaration/axiom checks. Rooted-cycle expectations, excursion loss and final combined theorem/contract verification remain unresolved.

Latest milestone: `fixed_right_kernel_moment` proves stretched-exponential moments for fixed terminal targets, including the infinite final gap, for all sufficiently deep sources without a source-corner restriction. Source-depth and row cutoffs are derived from the original profile. Default build passed (4231 jobs); the full Lean type/axiom audit completed for 584 declarations with only permitted axioms. The validator still fails the preserved manuscript hash after declaration/axiom checks. Matrix-norm conversion, bounded-source/row cases, fixed-right columns, rooted expectations and excursions remain unresolved.

Latest milestone: complete active-left columns are proved in `Section6DominationMatrixFixedLeft`, including all fixed initial target depths. The source-decay bound C*(i+1)^(-alpha) follows from the existing earliest-gap and weighted-rate estimates; alpha>1 supplies summability. The final `domination_matrix_left_column` has no lower target-depth premise. Default build passed (4230 jobs); the full Lean type/axiom audit completed for 583 declarations with only permitted axioms. The validator still fails the preserved manuscript hash after passing declaration/axiom checks. Right fixed-depth columns, rooted-cycle expectations and excursion loss remain unresolved.

Latest milestone: both endpoint column bounds now cover every source and every row size above derived fixed target-depth cutoffs. The new right assembly, outside-right matrix target bound and outside-right column bound are in `Section6DominationMatrixOutsideRight`. Default build passed (4229 jobs); the full Lean type/axiom audit completed for 580 declarations with only permitted axioms. The validator passes declaration/axiom checks but still fails the preserved manuscript hash check. Fixed-depth column bounds, rooted-cycle expectations and excursion loss remain unresolved; Lemma 6.4 is incomplete.

Latest milestone: the actual outside-right insertion norm now has the sharper C/n estimate, from the original profile and sampling (`right_outside_insertion_inverse_row`). A numerical absorption lemma has its regional rate-floor premise discharged by the proved profile lower bound. Default build passed (4228 jobs); the full Lean type/axiom audit completed for 577 declarations with only permitted axioms. The validator passes declaration/axiom checks but still fails the preserved manuscript hash check. Matrix transfer/right-column assembly, fixed-depth columns, rooted expectations and excursion loss remain unresolved.

Latest milestone: left-endpoint columns now include every source above a derived fixed target-depth cutoff (`domination_matrix_left_column_above_cutoff`). Two supporting concrete matrix theorems prove the outside-left C/n target bound and its uniform column sum. Default build passed (4226 jobs); the full Lean type/axiom audit completed for 575 declarations with only permitted axioms. The validator passed declaration/axiom checks and again failed the preserved manuscript hash check. No frozen baseline was changed. Fixed initial target depths, full right-column assembly, rooted expectations and excursion loss remain unresolved.

Latest milestone: four additional proofs give both complete numerical envelope column sums and both same-corner matrix column contributions, retaining explicit source-region and lower target-depth restrictions. Default build passed (4225 jobs), and the Lean type/axiom audit completed for 572 declarations with only permitted axioms. HOWEVER, the full validator failed at the frozen-file check: `fixed_points_sampled_profile.tex` changed during this turn, after the initial 13-file check passed. This turn did not edit the manuscript or baseline. The current Lemma 6.4 statement was inspected and matches the previously read statement, but a full manuscript comparison is unavailable because the manuscript is untracked and the freeze stores hashes only. The old validation JSON is historical, not the result of this turn. Outside-source columns, fixed-depth bounds, rooted expectations and excursion losses remain unresolved.

Latest milestone: both same-corner ordinary-plus-exceptional matrix envelopes are checked in `Section6DominationMatrixCornerEnvelope`. They apply to the unchanged matrix, with derived finite target-depth cutoffs, non-strict corner boundaries, unrestricted positive source depths and the exact right/left exceptional orientations. The default build passed (4223 jobs); all 568 source theorem types and axiom reports passed the audit and validator, with only permitted foundational axioms and all 13 frozen files unchanged. The sharper excluded-depth estimates, endpoint columns, rooted-cycle expectations and excursion loss remain unresolved. Lemma 6.4 is incomplete; historical scope below is superseded by this entry.

Latest milestone: both full active-endpoint matrix target maxima are proved. The new `PowerProfile.domination_matrix_left_target` covers all sources, every row size, fixed initial depths and the non-strict boundary depth<=delta*n, with only the original profile and exact sampling. The default build passed (4222 jobs), and the complete type/axiom audit and validator passed for 566 source theorems; only propext, Classical.choice and Quot.sound occur, and all 13 frozen files remain unchanged. Endpoint columns, same-corner matrix envelopes, rooted cycle expectations and excursion losses remain unresolved. Historical milestone scope below is superseded by this entry.

Latest milestone superseding the historical entries below: the full right-endpoint matrix target maximum is proved by `PowerProfile.domination_matrix_right_target`, for every source and row size, including fixed terminal depths and the non-strict boundary depth<=delta*n. Constants and finite-gap membership are derived from the original profile and sampling. The default build passed (4221 jobs); the full type/axiom audit and validator passed for 565 source theorems, with only the permitted foundational axioms and all 13 frozen files unchanged. Left-endpoint matrix maxima, endpoint columns, same-corner matrix envelopes, rooted cycle expectations and excursion losses remain unresolved.

Latest checked milestone: 564 source theorems, default build passed (4220 jobs), full elaborated-type and transitive-axiom audit passed with only propext, Classical.choice and Quot.sound; all 13 frozen files unchanged. Both weighted matrix rows now cover every n. Same-corner inverse target-depth insertion bounds are proved, and `PowerProfile.right_outside_insertion_target_bound` now proves C/h for outside-right sources by absorbing the stretched exponential remainder into C/n<=C/h. These remain regional insertion helpers: fixed target depths, full endpoint matrix assembly and columns, same-corner matrix packaging, rooted cycle expectations and O(1) excursion loss are still unresolved. Lemma 6.4 is incomplete. Earlier milestones below retain their historical counts and remaining-obligation lists.

Current Lemma 6.4 milestone: joint cylinder domination is now checked for one fixed matrix with deletion budget and norm exponent r, uniformly over every k<=r, including k=0. Both ENNReal and real probability statements are proved, with product constant one. The finite Luce weights, injective marked labels and ranks, and k<=r are the only inputs; no profile or moment premise is added. The previous host-memory block is resolved for this verification run. Three further statements prove matrix entries<=1, row sums<=n, and the unweighted profile matrix row bound for every n, absorbing small rows into the constant without changing the matrix.

All six declarations compiled individually; the cylinder and small-row branches passed (3705 and 3816 jobs), followed by the default build (4216 jobs). The full elaborated-type/axiom audit and validator passed for all 555 source theorems, with only propext, Classical.choice and Quot.sound and all 13 frozen files unchanged. The complete elaborated types of the two cylinder theorems and all-n row theorem were inspected. Remaining Lemma 6.4 obligations: active-corner target maxima and columns, same-corner matrix-envelope packaging, all-n weighted packaging, rooted cycle expectations and O(1) excursion loss. Lemma 6.4 and the main CLTs are not yet complete.

Previous milestone: `insertionDominationMatrix` is an explicit deterministic real matrix, formed from the finite maximum of actual insertion p-norms over every bounded deletion and demanded-gap shift. Its nonnegativity, finiteness, exact real/ENNReal representation, domination of each permitted norm, and attainment are proved. The three concrete matrix row theorems prove unweighted and both manuscript-weighted sums, with no matrix estimate assumed. Interior entries are bounded by C/n, and interior column sums are bounded for arbitrary source subsets. These bounds hold for all sufficiently large rows, on both grids, from the original profile and sampling assumptions. No frozen production definition changed.

Thirteen new statements compiled individually. One entrywise proof initially hit the elaborator's heartbeat limit; spelling out finite-sup membership resolved it without changing checking settings or the theorem statement. A branch build then failed to read an existing compiled dependency; separate builds of the matrix definition, rows and interior bounds passed (3699, 3815 and 3713 jobs), followed by the default build (4214 jobs). The expanded audit prints the three new definitions and all 549 theorem types/axiom dependencies. It and the validator passed, with only propext, Classical.choice and Quot.sound and all 13 frozen files unchanged. The exact matrix definitions and complete elaborated types of its five concrete row/interior bounds were inspected. The full domination lemma remains unfinished: active endpoint columns and target maxima, same-corner matrix estimates, joint cylinder domination, excursion counting, and small-row packaging remain obligations, followed by joint asymptotics and the main CLTs/contracts.

Previous milestone: the actual insertion norms now have a uniform unweighted row and both global weighted rows, covering every source and every target for all sufficiently large rows. The new theorems are `PowerProfile.global_uniform_insertion_row`, `global_right_weighted_insertion_row`, and `global_left_weighted_insertion_row`. Constants and row-size thresholds are constructed from the original profile and fixed deletion/moment orders; both grids and target-dependent deletions/shifts are covered. The right and left weights have exactly 0<=kappa<beta and 0<=kappa<alpha, respectively. Three supporting statements assemble interior and both permitted endpoint behaviors. No global rate, integrability, summability or matrix hypothesis is introduced.

All six statements compiled individually. A combined branch build encountered compiled-file read failures; sequential endpoint and interior branch builds passed (3805 and 3711 jobs), followed by the unchanged default build (4211 jobs). The regenerated statement/axiom audit and validator passed for all 536 source theorems, with only propext, Classical.choice and Quot.sound and all 13 frozen files unchanged. The complete elaborated types of all three global row theorems were inspected, including their implicit parameters, original profile, exact sampling, constructed thresholds, unrestricted source/target sets and actual insertion norms. The failed concurrent attempt is retained in audit/section6-global-rows-branch-build.log. The remaining matrix construction, columns, target maxima, cylinder and excursion estimates, joint asymptotics and every main CLT/contract check remain unresolved. These row estimates alone do not prove the full domination lemma.

Earlier milestone: `PowerProfile.right_source_full_weighted_insertion_row` covers every target for a source in a derived right block, including paths crossing the middle and the left endpoint. Five new statements establish an interior O(theta_i) row, extend it to all targets outside any fixed terminal block, absorb the factor (n/a)^kappa using the derived right power rate bound, transfer to the exact target-dependent weight, and combine with the right-endpoint contribution. This is the manuscript's cross-region absorption argument, not a proof of the full matrix or main CLTs. The remaining outside-source case needs the uniform unweighted row over all targets.

Verification of this milestone: all five statements compiled individually, the dependency branch passed with 3767 jobs, and the default build passed with 4207 jobs. The regenerated elaborated-type and transitive-axiom audit passed for all 530 source theorems; only propext, Classical.choice and Quot.sound occur, and all 13 frozen files remain unchanged. The complete elaborated full-target right-source theorem was inspected, including its constructed source block, unrestricted target subset, natural gap indices and actual norm conclusion. The initial interior proof failed to normalize a division algebraically; an explicit ring identity resolved it without changing the statement. The source scan found no sorry, admit, axiom, unsafe or checking-bypass declarations (exit 1, no matches).

Previous milestone: `PowerProfile.right_endpoint_weighted_insertion_row` now proves the right-endpoint target contribution for every source, on both sampling grids, with 0<=kappa<beta. Four new proofs bound the outside-right kernel sum, transfer it to actual insertion norms, include fixed terminal targets, and combine outside and inside sources. Natural gap indices include the infinite final gap. Constants and all analytic estimates are derived from the original profile and exact sampling. This does not finish the global weighted row: targets outside the right block, columns, target maxima and the complete matrix remain obligations.

Validation of this milestone: individual compilations passed; the dependency branch passed (3750 jobs); the default build passed (4203 jobs); the regenerated elaborated-type and transitive-axiom audit passed for all 525 source theorems. The validator confirms only propext, Classical.choice and Quot.sound and all 13 frozen files unchanged. The complete elaborated right-endpoint theorem type was inspected, including its unrestricted source, constructed target block and large-row cutoff, bounded shifts/deletions and actual weighted insertion norm conclusion. The initial numerical compilation failed on a misnamed interval-cardinality lemma; replacing it with the pinned library's Nat.card_Ico resolved it. No mathematical statement or frozen definition was changed by that correction.

- Exact positive continuous sampled-profile assumptions, with separate power-law and critical regimes, midpoint and interior grids, explicit relative big-O expansions, and inactive finite positive limits. No normalization or endpoint-shell input.
- Independent closed power-law, spatial/localization, and critical targets for both grids in `Luce/Section6Contract.lean`. The exact Gamma/convolution coefficients, actual cycle counts, largest-label roots, deterministic centerings, and independent Gaussian laws are specified. The contract and production dependency closure plus manuscript/configuration were frozen in `audit/section6-contract-freeze.json` (13 files).
- Five hundred and fifty-five kernel-checked helper theorems. The initial nineteen cover sampling positivity/reflection, relative-error convergence and two-sided endpoint comparisons, literal finite transforms, sharp deletion bounds, and exact finite-transform derivatives.
- Thirty further theorems prove compact/profile bounds away from active endpoints (including global inactive-endpoint bounds), absolute power errors and uniform-in-time perturbation bounds retaining exponential decay, the exact Gamma power integral and its integrability for either sign of the power, antitone-envelope quadrature for both grids, and a complete quantitative slow-power prototype estimate.
- In particular, `slow_prototype_population_error` has the literal leading term `(1/r)^(1/beta) * Gamma(1+1/beta)` and error at most `1/n + exp(-r/2) * (1/(r/2))^(1/beta) * Gamma(1+1/beta)`. This is the exact power-envelope model, not yet the actual perturbed profile. Monotonicity is required only of comparison envelopes, never added to the profile assumptions.
- Seventeen further theorems now supply increasing/unimodal quadrature, the weighted-kernel error `2*exp(-1)/(n*t)` for both grids, weighted Gamma integrals and integrability, exponential decay above a lower rate, polynomial absorption, and uniform right-endpoint power-envelope sums. All peak locations and envelope monotonicity hypotheses are proved for the weighted kernel. No time-scale loss is introduced.
- Four further theorems in `Section6RightPopulationComparison.lean` and `Section6RightPopulationError.lean` now give the actual sampled-profile right-survivor comparison and its Gamma leading term. `PowerProfile.right_populationH_error` derives positive C,d,eps from the original profile, uniformly over both grids, every n>0, and every t>0. Its input is the original PowerProfile and exact SampledRates, with no additional estimates assumed. It combines the near-endpoint perturbation bound, the uniform envelope sum, the off-endpoint exponential terms, and the prototype quadrature/tail terms.
- Default build imports `Luce.Section6`, so these definitions and proofs are actually checked.

Six new theorems complete the right-survivor part of `lem:sp-populations`, including its bounded-deletion claim. `PowerProfile.right_populationH_power_error` gives the additive error K*(t^(-(1+eta)/beta)+1/n). `PowerProfile.right_populationH_relative_error` converts this into the exact Gamma-normalized relative error with saving eta/beta. `PowerProfile.right_deletedH_relative_error` proves the same estimate after deleting at most any fixed r clocks, uniformly in their labels and retaining divisor n. Three supporting algebra/decay theorems construct and simplify the required constants. All estimates use the original profile and exact sampling only, on both grids.

Twelve further theorems complete the right weighted-population estimate, including bounded deletions. `PowerProfile.right_populationD_relative_error` proves the exact leading coefficient A/beta and time power t^(-1-1/beta). Its relative error is K*(t^(-eta/beta)+1/(n*t^(-1/beta))). The proof constructs the scaled t*D comparison with the actual profile, proves and applies the weighted prototype integral and tail estimate, and retains the sharp O(1/(n*t)) discretization error. `PowerProfile.right_deletedD_relative_error` proves uniformity in any set of at most r deleted labels from the sharp existing deletion bound. No normalization, shell condition, monotonicity of the profile, or global upper rate bound is used.

Seventeen further theorems establish the fast-endpoint prototypes and begin their actual-profile comparison. The fast-arrival Gamma integral is proved by integration by parts, after proving integrability and both boundary terms. Its sampled error on either grid is ≤1/n+r/(alpha-1). The weighted fast prototype has the exact coefficient Gamma(1-1/alpha)*c^(1/alpha)/alpha and error ≤2*exp(-1)/(n*t)+c/(alpha-1). For arrival quadrature only, an auxiliary value 1 is used at zero; the proof explicitly establishes that the sample values and integral agree with the original expression. No model or contract definition changes.

`PowerExpansion.exists_fast_error_exponent` constructs a smaller positive exponent below both eta and alpha-1 from the original expansion, with no extra regularity hypothesis. `PowerProfile.left_arrival_difference_bound` uses it to prove the actual profile's pointwise envelope plus an O(t) off-endpoint term.

Sixteen further theorems complete the left-population estimates and combine both endpoint pairs. The fast-envelope sum is bounded by a weighted prototype with exponent alpha-eta'>1. This gives the positive relative saving zeta=1/(alpha-eta')-1/alpha. The earlier proposed sharper envelope rate t^((1+eta')/alpha) is not proved or used; the manuscript only requires some positive saving. This proof choice changes no mathematical input, leading coefficient, model definition, or frozen conclusion.

The complete population-transform milestone is `Section6PopulationEstimates.lean`. `PowerProfile.left_deleted_populations_relative_error` supplies both fast formulas with one positive error exponent and one constant. `PowerProfile.right_deleted_populations_relative_error` supplies both slow formulas with the common eta/beta saving. Both are uniform over the two sampling grids, all positive rows and all sets of at most any fixed r deleted labels. The actual undeleted formulas are also proved separately. No intermediate population, tightness, moment, or regularity assumption is inserted.

The full assumption expansion is in [section6-assumption-audit.md](section6-assumption-audit.md). Every outstanding main and intermediate obligation is in [section6-obligation-ledger.md](section6-obligation-ledger.md).

## Actual verification

Commands run from the repository root using the existing pinned toolchain:

```powershell
& "$env:USERPROFILE/.elan/bin/lake.exe" build *> audit/section6-progress-build.log
python audit/generate_section6_current_audit.py
& "$env:USERPROFILE/.elan/bin/lake.exe" env lean audit/Section6Current.lean *> audit/section6-current-audit.log
python audit/freeze_section6_contract.py
python audit/validate_section6_progress.py
```

The current default build exited 0: `Build completed successfully (4216 jobs).` The current statement/axiom audit exited 0 and contains all 555 Section 6 source theorem reports, each with only `propext`, `Classical.choice`, and `Quot.sound`. The validator checks the exact set of source theorem names, the axiom whitelist, successful build output, and all frozen file hashes; it exited 0. All 13 frozen files are unchanged. Compiler output is in [section6-current-audit.log](../audit/section6-current-audit.log); build output is in [section6-progress-build.log](../audit/section6-progress-build.log). The Section 6 source scan found no sorry, admit, axiom, unsafe, or checking-bypass declarations (rg exit 1, no matches). Earlier build attempts encountered compiled-file read failures and an out-of-memory panic; building the two remaining branches sequentially, then rerunning the full default build, succeeded without changing sources, dependencies, checking settings or build configuration. Those failure logs are retained in audit/section6-build-read-failure.log, audit/section6-build-memory-failure.log and audit/section6-build-second-read-failure.log. An audit attempted before the successful rebuild saw the old root module and failed on the new names; audit/section6-audit-before-rebuild.log records that attempt. The current audit was rerun after the full successful build.

## Next unresolved mathematics

The next manuscript result is `lem:sp-quantiles`. Finite quantile existence, uniqueness, positivity, and exact inverse equations are now proved for 0<m<n. The second-moment inequality D2(t) <= (2/(e*t))*D1(t/2) and a finite-time D1 variation bound are proved without endpoint assumptions. The right deterministic remaining-rate floor is also proved for every surviving set, on both grids: sum of surviving rates >= C*n^(-beta)*h^(beta+1). These 29 new helpers are imported by the default build.

Seven further helpers prove the left initial-block bound, its remaining-rate floor C*n^alpha*h^(1-alpha), and exact two-sided finite mean separation. The rate floor counts survivors among the first 3h labels after at most 2h removals. Its bounded-budget corollary uses removed.card <= h+r, 4r+4 <= h, and h <= eps*n/4; a later gap application must discharge the total removal budget. Both grids are covered, with constants derived from the original expansion and no profile monotonicity assumption.

Five further theorems prove quantitative right-quantile inversion. `PowerProfile.right_quantile_relative_error` constructs positive C,delta,M from the original profile such that, for either sampling grid and any exact sampled array, 0<m<n, M<=m, and m/n<delta imply

```
|rightQuantileTime (w n) m / (Aminus*n/m)^beta - 1|
  <= C*((m/n)^eta + 1/m),
Aminus = Gamma(1+1/beta)*c^(-1/beta).
```

The proof does not assume that the unknown time is large: it derives a lower time cutoff by evaluating the actual population error at a fixed S and applying strict antitonicity. The same error proves that the normalized power is between 1/2 and 2, where an explicitly proved derivative bound gives linear stability of inversion. All helper smallness and error premises are discharged in the concrete theorem. No frozen definition changes.

Four further theorems prove positive-power inversion and the actual left-quantile error. `PowerProfile.left_quantile_relative_error` constructs xi,C,delta,M>0 with the same grid/row/rank uniformity:

```
|leftQuantileTime (w n) m / (m/(Aplus*n))^alpha - 1|
  <= C*((m/n)^xi + 1/m),
Aplus = Gamma(1-1/alpha)*c^(1/alpha).
```

The positive exponent is derived from the original expansion via the already-proved population saving, not supplied by a new assumption. The proof constructs a small positive reference time S and proves that the actual quantile lies below it by monotonicity and the population estimate. Both endpoint time asymptotics are now proved; the rest of the quantile lemma is not yet complete.

Eleven further theorems prove sampling errors and both rate-time products. The sample/nominal-location ratio is in [1/2,1], and its relative error is <=1/m on either grid. Applying the proved power derivative bound and the original expansion gives the actual sampled-rate relative error C*((m/n)^eta+1/m). Right reflection uses terminal depth n-i.val; the left uses label i.val+1.

`PowerProfile.right_rate_time_relative_error` and `left_rate_time_relative_error` prove

```
|rate[n,n-m+1]*rightQuantileTime(n,m)/qR - 1|
  <= C*((m/n)^eta + 1/m),
|rate[n,m]*leftQuantileTime(n,m)/qL - 1|
  <= C*((m/n)^xi + 1/m),
qR = Gamma(1+1/beta)^beta,
qL = Gamma(1-1/alpha)^(-alpha).
```

All constants and the positive left saving xi are constructed from the original profile. Both grids are covered. The product helper's e<=2 premise is proved from the rank range; the coefficient cancellation is checked by separate real-power identities. These estimates are not new inputs to a main theorem.

Five further theorems establish cutoffs and actual time comparability. `joint_power_error_small` constructs independent small-x and large-m cutoffs making C*(x^a+1/m) as small as required. Applying it to both proved time errors gives `PowerProfile.right_quantile_comparable` and `left_quantile_comparable`: each actual time lies between half and twice its exact leading term, with constants chosen from the original profile. `relative_quotient_error` proves division stability and denominator positivity under a one-half error bound; both concrete hazard applications are completed below.

Four further theorems prove the right weighted population at its actual quantile and the right hazard. `antitone_quantile_smallness` derives the necessary large-time and small-error bounds from the survivor estimate. `negative_power_weighted_comparison` compares the two additive power estimates at the exact inverse. Applying both to the original H and scaled-D theorems gives

```
|beta*t*D1(t)/(m/n)-1| <= C*((m/n)^eta+1/m),
t = rightQuantileTime(n,m).
```

`PowerProfile.right_quantile_hazard_relative_error` then proves the manuscript's exact relative estimate for theta[n,n-m+1]/(n*D1(t)), with leading term beta*Gamma(1+1/beta)^beta/m and the same error order. Denominator positivity/smallness is proved, not an assumed field. Both grids are covered under the original profile and sampling assumptions.

Five further theorems prove the left weighted-population estimate and hazard. `PowerProfile.left_populations_power_error` combines the two proved positive savings by their minimum, proving the common additive bounds for G and t*D. The monotone quantile-control and positive-power comparison lemmas discharge small-time, discretization, and comparison bounds at the exact arrival inverse. Consequently

```
|alpha*t*D1(t)/(m/n)-1| <= C*((m/n)^xi+1/m),
t = leftQuantileTime(n,m), xi>0.
```

`PowerProfile.left_quantile_hazard_relative_error` combines this with the actual rate-time estimate and proves the precise leading term alpha*Gamma(1-1/alpha)^(-alpha)/m. A common positive saving and cutoffs are constructed; denominator positivity and smallness are proved. Both grids use the original profile and sampling only.

Four further theorems construct uniform weighted-population regions and cover the enlarged right quantile window. The two endpoint relative estimates yield half-to-twice power bounds once time and effective-population size enter their derived regions. `negative_power_window_region` proves region inclusion throughout T/16<=s<=16T from joint cutoffs. `PowerProfile.right_populationD_quantile_window` applies it to the actual quantile, yielding the exact power bounds for every t/8<=s<=8t, without an additional evaluation-region hypothesis in the concrete theorem.

Six further theorems establish the left window and both complete comparable-time moment estimates. `positive_power_window_region` and the concrete left-window theorem derive region inclusion from the original joint regime. `power_window_comparison` compares all window values with the center, and `population_moments_from_power_window` combines this with the center normalization and exact second-moment inequality. Both concrete endpoint applications discharge every helper premise.

`PowerProfile.right_quantile_moments` and `left_quantile_moments` construct positive a,C,delta,M such that, for the actual respective quantile t and all t/4<=s<=4t,

```
a*m/t <= n*D1(s) <= C*m/t,
n*D2(s) <= C*m/t^2.
```

These are the manuscript's eq:sp-rough-moments, uniformly on both grids and with only the original profile/sampling inputs. Quantile-scale mean separation, relative weight stability, and uniform interior quantile bounds remain unproved. The full quantile lemma and Section 6 CLTs remain incomplete. No additional quantile or weight-floor assumptions are permitted.

Representation note: the new finite quantile is `Function.invFunOn` of the existing finite arrival transform on nonnegative time. Its inverse identities are proved. The survivor quantile uses the proved equality H=1-G. No frozen model or contract definition was changed. Terminal-depth arithmetic is reused from `EndpointShells`, but no endpoint-shell predicate is an input to these Section 6 results.

The right-survivor estimate is now proved: for any permitted profile with active right exponent beta, coefficient c and error exponent eta, there exists K>0 such that for every positive row n and every t>=1,

```
|populationH (w n) t / (A*t^(-1/beta)) - 1|
  <= K * (t^(-eta/beta) + 1/(n*t^(-1/beta))),
A = Gamma(1+1/beta)*c^(-1/beta).
```

The corresponding weighted estimate is also proved:

```
|populationD (w n) 1 t / ((A/beta)*t^(-1-1/beta)) - 1|
  <= K * (t^(-eta/beta) + 1/(n*t^(-1/beta))).
```

The left estimates are now proved as well, for 0<t≤1, with Aplus=Gamma(1-1/alpha)*c^(1/alpha) and a constructed zeta>0:

```
|populationG (w n) t / (Aplus*t^(1/alpha)) - 1|
  <= K * (t^zeta + 1/(n*t^(1/alpha))),
|populationD (w n) 1 t / ((Aplus/alpha)*t^(1/alpha-1)) - 1|
  <= K * (t^zeta + 1/(n*t^(1/alpha))).
```

These are proved theorems, not assumed fields or inputs. Both endpoint pairs have corresponding bounded-deletion theorems, with K chosen before the removal set and the divisor n retained. All later obligations remain in the full ledger.

After that remain the quantitative quantile estimates and their gap applications, insertion estimates and global domination, traces/factorial moments, Gaussian convergence and localization, critical-pole martingale/longer-cycle arguments, grid extension, Sukhatme specialization, and the final independent contract checks. None has been inserted as a main-theorem assumption.

Four further theorems in `Section6QuantileMeanSeparation` prove both-sided mean separation at each actual endpoint quantile. For 0<u<=1/2 the survivor or arrival mean changes by at least a*u*m. The concrete theorems require only the original PowerProfile and exact SampledRates, uniformly for both grids; the generic derivative-window premise is discharged by the proved quantile moments. Relative D stability, compact-interior estimates, and the full insertion/CLT arguments remain unresolved. The current build and all 205 transitive axiom checks pass; all 13 frozen hashes are unchanged.

Four further theorems in `Section6QuantileWeightStability` prove relative weight stability: abs(D1((1±u)*t)/D1(t)-1)<=K*u for 0<u<=1/2 at either actual endpoint quantile. The generic variation proof uses the exact derivative of n*D1, the proved second-moment upper bound, and the positive center lower bound. Both concrete PowerProfile theorems discharge all moment premises using `quantile_moments`, uniformly over both grids, without normalization or shell assumptions. All 209 type/axiom reports pass, the full build succeeds with 4055 jobs, and all 13 frozen hashes are unchanged. Compact-interior estimates and the full insertion, Gaussian, and critical martingale arguments remain unresolved.

Three further theorems in `Section6InteriorRemainingRate` establish compact-interior sampled-rate bounds and the deterministic remaining-rate part of the quantile lemma. Interior label rates are bounded above and below from continuity/positivity alone. Under the original PowerProfile and exact sampling, every remaining set with card>=eps*n has total rate >=C*n. This persists after at most r arbitrary deletions for 2*r<=eps*n, with the remaining cardinality estimate proved explicitly. Both grids and both allowed right endpoint behaviors are covered; no shell or normalization premise is used. The complete 212-theorem audit and 4039-job build pass, with all 13 frozen files unchanged. Compact-interior quantile times and n*D comparability still require proof; main CLTs remain incomplete.

Seven new results in `Section6SmallTimePopulation` and `Section6LargeTimePopulation` prove uniform small-time arrival and large-time survivor control, then both interior inverse time bounds. `PowerProfile.interior_quantile_time_bounds` constructs s>0,T>=1,N>0 so both inverse times for x in [eps,1-eps] lie in [s,T], uniformly both grids and n>=N. Active endpoint cases use the proved population errors; inactive cases derive global rate bounds and exact exponential estimates. The theorem takes only the original PowerProfile, exact SampledRates, and the interior interval parameter. The full build succeeds with 4055 jobs; all 219 source theorem types and transitive axiom reports pass, and all 13 frozen hashes are unchanged. Interior D comparability and its bounded-deletion extensions remain open, as do insertion and main CLT arguments.

Six new results in `Section6InteriorPopulationD` and `Section6InteriorDeletedD` complete interior weighted-population bounds. A concrete interior label block has at least n/8 labels with both normalized depths >=1/4. Its rates supply D>=d*exp(-T*M)/8 on any fixed positive compact time interval; the exact kernel bound supplies the upper bound. The proved r/(e*n*t) deletion error preserves positive lower and finite upper bounds beyond a constructed row cutoff. `PowerProfile.interior_quantile_populationD_bounds` proves literal a*n<=n*D<=C*n at both original interior quantiles; `interior_quantile_deletedD_bounds` includes arbitrary bounded deletions evaluated at those times. All concrete premises come from the original profile/sampling. The full 4043-job build and all 225 theorem audits pass; only permitted foundational axioms occur and all 13 frozen files are unchanged. Deleted-transform inverse applications and the full ordered-simplex/insertion/CLT arguments remain unfinished.

Ten new results in `Section6OrderedInsertion` and `Section6OrderedInsertionExpectation` establish the exact ordered conditional insertion identity for arbitrary distinct marked labels and target ranks, including adjacent targets with repeated gap indices. `OrderedMarkedInsertion` is the literal conjunction of strict marked-time order and the existing actual count-gap membership; `orderedInsertionKernel` is its probability under the unchanged exponential race. The sorted concrete theorem derives its indexing by the existing sort permutation and q+a=j identity, with no rank separation premise. Resampling and Tonelli prove equality of the cylinder probability to the kernel expectation. Measurability, probability <=1, real integrability, and the finite real-expectation identity are proved; dropping the order gives the actual single-gap product upper bound. All 235 theorem audits and the 4045-job build pass, only permitted foundational axioms occur, and all 13 frozen hashes are unchanged. Factorization into one ordered simplex density integral per repeated gap (including the terminal gap) remains unresolved, along with insertion estimates and the main CLTs.

Eight new results in `Section6GapOrderPartition` prove the deterministic decomposition of the global ordered insertion region into the occupied-gap regions. Sorted demanded gaps are nondecreasing by a finite interval-cardinality proof. Different gap blocks have disjoint marked labels, and each event depends only on its block; between-gap order follows from actual before-count monotonicity. Per-gap events and masses are measurable and bounded by one. The conditional kernel equals the single-gap product when gaps are distinct. Full repeated-gap probability factorization and the simplex density-integral bridge remain unresolved; the special distinct-gap result does not impose a separation input on the arbitrary-rank theorem. The 4046-job build and all 243 type/axiom audits pass, with permitted axioms only and all 13 frozen hashes unchanged. Main CLTs remain incomplete.

Six new results in `Section6DisjointClockEvents` and `Section6GapProbabilityProduct` prove the full repeated-gap probability factorization. Disjoint-coordinate event independence is derived from the actual exponential law using measurable restriction/extension maps; finite induction handles the family of blocks. All helper support and measurability premises are discharged by the existing gap partition proofs. `markedRankCylinder_real_probability_eq_gap_product` gives the exact expectation of the product of ordered per-gap masses for arbitrary distinct marked labels and targets, with no rank separation. Product integrability and finite real conversion are proved. The full build succeeds with 4055 jobs and all 249 type/axiom audits pass, using only permitted axioms; all 13 frozen files remain unchanged. Identification of each per-gap mass with the manuscript ordered-simplex density integral, insertion estimates, and main CLTs remain unresolved.

Ten new results in `Section6GapCoordinates`, `Section6ExponentialProductDensity`, and `Section6SimplexDensityIntegral` prove the density representation on exact count-gap regions. Each gap mass is reduced by a proved marginal-law identity to exactly its assigned marked coordinates. The finite product exponential density theorem follows from integrability of each density, product integration, and rectangle measure uniqueness. The real literal product of rates times exponential factors is integrable on each ordered region and equals the per-gap mass; the complete cylinder equals the expectation of their product, whose integrability is also proved. The 4051-job build and all 259 theorem type/axiom audits pass; only permitted axioms occur and all 13 frozen hashes are unchanged. The remaining representation obligation is equality modulo null boundaries with the manuscript open order-statistic interval simplex, including terminal gaps. Insertion estimates and main CLTs remain unfinished.

Ten new results in `Section6OpenGaps`, `Section6OpenSimplex`, and `Section6OpenSimplexCylinder` complete the open order-statistic simplex representation. Nonterminal gaps use the existing exact order/count equivalence; the terminal gap is (T_N,infinity), including T_0=0 for empty backgrounds. Product Lebesgue coordinate hyperplanes are proved null, so the count-gap and open-gap simplex regions agree almost everywhere, preserving their literal density integrals and integrability. `markedRankCylinder_real_probability_eq_openSimplex_product` gives the concrete manuscript open-simplex expectation identity for arbitrary distinct labels and target ranks, including adjacent ranks and terminal gaps. All background injectivity/nonnegativity requirements are discharged almost surely from the original exponential law. The 4054-job build and all 269 theorem type/axiom audits pass; permitted axioms only and all 13 frozen files unchanged. Insertion moments, their concentration/tail estimates, and main CLTs remain unfinished.

Five new results in `Section6OneGapMoment` prove the exact survival factor and the integrated one-spacing factorial bound. For fixed a>0,W>0,s and natural p, the actual exponential gap mass with length xi/W is integrable under xi~Exp(1), with moment <=p!*(a/W)^p*exp(-p*a*s). The full starting-time decay is retained. The existing full order-conditioned normalized-gap law was inspected: it supplies a joint measure identity, but transfer to the random start still requires reconstruction from preceding spacings and integration with respect to that joint law. This fixed-parameter estimate is not a completed eq:sp-insertion-moment-reduction. The full 4055-job build and all 274 theorem type/axiom audits pass, using only permitted axioms; all 13 frozen hashes remain unchanged. Random-start transfer, endpoint concentration/extreme estimates, and main CLTs remain unfinished.

## Starting times and coordinate integration

Seven new results in `Section6GapStart` reconstruct the actual preceding clock as the sum of earlier normalized spacings divided by the actual remaining rates, including the initial zero sentinel. The identity holds for every real clock vector. Continuity, nonnegativity for nonnegative preceding coordinates, and invariance under updating the current spacing are proved. `gap_moment_coordinate_slice` integrates that literal updated coordinate and retains p!*(a/W)^p*exp(-p*a*s), with positivity of W discharged from the finite weights. Applying the order-conditioned product law to integrate the slice, transferring to deleted clocks, and obtaining the full endpoint moment reduction remain unresolved; no main CLT is claimed.

## Joint normalized-spacing moment reduction

Four new results in `Section6GapStartIndependence` and `Section6JointGapMoment` prove independence of the reconstructed starting time and current normalized spacing under the actual product gap law, exact survival-weighted factorial factorization, almost-sure nonnegative coordinates, and the integrated random-start moment bound. `joint_gap_moment_bound` proves both integrability and E[X^p] <= p!*(a/W)^p*E[exp(-p*a*S)] for every fixed elimination chain, positive marked rate, and natural moment. All independence and integrability premises are discharged. The next required step is transport through the order-conditioned race measure identity to actual deleted gaps, followed by summing chains and applying the endpoint deterministic bounds. The full manuscript insertion moment reduction and all main CLTs remain incomplete.

## Actual deleted insertion kernels on order events

Five new results in `Section6OrderedGapTransfer` and `Section6DeletedOrderMoment` transfer the joint moment bound to the original race and its actual deleted insertion kernel. The existing restricted-race map identity supplies the real integral and integrability transfer. Compaction transports the restricted law back to the original probability space. `deleted_order_kernel_moment_bound` proves the factorial moment inequality for the literal deletedGapKernel on every fixed compacted order event, retaining the full starting-time exponential factor. Almost-sure injectivity and nonnegative clocks are discharged from the original race law. No independence, integrability, or remaining-rate floor is added as a premise. Summing these order-event bounds and applying the endpoint deterministic bounds remain required before the full manuscript moment reduction is complete; the main CLTs remain unproved.

## Unconditional moments and endpoint floor discharges

Eight further results sum over all compacted elimination orders, prove kernel-power and actual survival-factor integrability, and identify the summed survival contribution with its full expectation. The generic finite moment helper has an explicit uniform remaining-rate floor premise. Both concrete endpoint theorems discharge it from the original PowerProfile and SampledRates: the left theorem uses the bounded-displacement prefix-cardinality bound, and the right theorem counts remaining labels. They retain the actual marked rate divided by C*n^alpha*q^(1-alpha) on the left or C*n^(-beta)*h^(beta+1) on the right. Neither theorem assumes normalization, a shell condition, or a rate floor. Substitution of the sampled marked-rate estimates and reconciliation of rank offsets are still needed for the exact manuscript x/h moment reduction. The main CLTs remain incomplete.

## Marked-rate substitution and x/h cancellation

Seven new results prove uniform sampled-rate upper envelopes from the existing relative expansions, exact real-power cancellation of the population size, and both concrete insertion moment reductions in actual gap coordinates. The factors are ((q/(i.val+1))^alpha/q) on the left and ((terminalDepth(i)/h)^beta/h) on the right. The right parameter h is a positive lower bound on the number of surviving labels; the left parameter is the actual gap index. The constant is explicitly p!*B^p and the complete starting-time survival expectation is retained. No new model hypothesis is added. Alignment with bounded demanded-rank/deletion offsets remains a separate obligation before declaring the full indexed manuscript equation complete. Concentration and the main CLTs remain unproved.

## Shifted full-rank insertion moments

Nine new results align actual gap indices with full-rank depths. Left rank h has gap h-1, and right terminal depth h has gap n-h. Bounded displacement and deletion give factor-two comparisons beyond h>=8r+8; the endpoint ranges exclude terminal gaps. The exact sorted demanded-gap definition satisfies the shift condition. Fixed-factor real-power comparisons then prove `PowerProfile.left_shifted_insertion_moment` and `PowerProfile.right_shifted_insertion_moment`: E[X^p] <= p!*(B*x_sigma(a,h)/h)^p*E[exp(-p*theta*S)], with the original deleted kernel and survival time, both grids, and all rate/independence/integrability premises discharged. The next unresolved mathematical step is the order-statistic lower-tail estimate, followed by moderate/extreme insertion estimates and restricted sums. Main CLTs remain incomplete.

## Arrival concentration and the actual gap-start event

Eleven new results prove the Bernoulli upper-tail constant log(2)-1/2 is positive, bound the MGF for independent nonidentical Bernoulli variables, and specialize the resulting concentration to actual deleted arrivals. The new arrival indicator is exactly one minus the existing survival indicator, so measurability, independence, and its integral are proved from the original clock law. Strict before-count is dominated by arrivals-by-t, avoiding any hidden equality-at-t condition. An early actual gap start implies at least q earlier clocks. Consequently `deleted_gap_start_tail_of_arrival_mean` bounds its probability by exp(-(log(2)-1/2)*q), provided the numerical mean inequality n*deletedG(t)<=q/2 holds. Proving that inequality at the left profile scale and completing the right survivor lower-tail argument remain required; the full manuscript order-statistic tail and main CLTs are not claimed complete.

## Concrete left endpoint order-statistic tail

Four further results discharge the left arrival-mean obligation. The sharp existing population estimate yields G_n(t)<=M*(t^(1/alpha)+1/n) for 0<t<=1, and deletion only decreases this mean. Choosing u=min(1,1/(8M)), k=u^alpha, and H=8M proves n*deletedG(k*(h/n)^alpha)<=h/4 for h>=H. The shifted gap inequality h<=2q then discharges the finite Chernoff helper. `PowerProfile.left_gap_start_lower_tail` proves the actual deleted-start bound exp(-rho*h), with rho=(log(2)-1/2)/2>0, both grids, and no added mean or rate hypothesis. The right survivor-tail argument remains unresolved, as do the later insertion estimates and main CLTs.

## Concrete right endpoint order-statistic tail

Six new results complete the right survivor argument. Strict before-count plus strict survivor-count is bounded by the unmarked population size, without an assumption excluding ties. The survivor mean is exactly n*deletedH. The existing Bernoulli lower-tail bound yields a finite gap-start estimate. The original deleted-population relative error then gives a uniform lower-envelope region. Choosing u=min(1,A/16), k=u^beta, and profile-derived depth cutoffs proves the deleted survivor mean at k*(n/h)^beta is at least 8h. `PowerProfile.right_gap_start_lower_tail` discharges the numerical mean premise and proves exp(-rho*h) for shifted demanded gaps. Both endpoint order-statistic lower tails are now proved from original profile/sampling assumptions. Moderate/extreme insertion estimates, restricted sums, joint/local bounds, and main CLTs remain unfinished.

Fifteen further theorems derive both survival-expectation bounds and explicit insertion moments. `PowerProfile.left_survival_envelope` and `PowerProfile.right_survival_envelope` prove E[exp(-p*theta*S)] <= exp(-d*x)+exp(-d*h), using the original gap start and probability law, for all p>=1. Exact rate-time cancellation and the already proved endpoint tails discharge every analytic premise. `PowerProfile.left_insertion_exponential_moment` and its right counterpart substitute this into the actual insertion moment reduction. The two concrete moderate insertion theorems give E[X^p] <= 2*p!*(B*x/h)^p*exp(-d*x) on x<=min(a,h)^nu, nu<=1. Both grids and bounded deletions/full-rank shifts are retained. Uniform Lp conversion, the different right extreme time scale, stretched-exponential bounds and restricted sums, and all main CLTs remain open.

Fourteen further theorems complete the moderate-region Lp conversion and the right extreme Laplace estimate. The actual kernel has MemLp at every exponent by its probability bound, and its natural-order norm equals the literal moment root. The finite-p0 algebra gives uniform constants, proving `PowerProfile.left_moderate_insertion_Lp` and `PowerProfile.right_moderate_insertion_Lp` for both grids. On the right, a real comparison-depth mean and a sharp survivor-mean tail retain the stronger probability decay needed at y=a^(beta/(beta+1)). `PowerProfile.right_extreme_survival` combines that different-time estimate with the typical estimate to prove eq:sp-right-laplace-extreme with factor 2. No population, tail, rate-floor, moment, or norm premise is added to these concrete profile theorems. Polynomial absorption, the extreme insertion Lp bounds and restricted sums, joint/local insertion analysis, and the main CLTs remain unresolved.

Five further theorems complete the separate extreme-region insertion Lp estimates. A numerical ratio lemma proves the manuscript's power lower bounds on the complement of the moderate set. Uniform polynomial absorption uses the existing exponential absorption theorem, bounds factorials and powers over 1<=p<=p0, and retains a positive decay coefficient. `PowerProfile.right_extreme_insertion_Lp` derives C*exp(-d*a^nu) from the different-time survival estimate. `PowerProfile.left_extreme_insertion_Lp` derives C*exp(-d*h^nu) from the original left survival estimate. Both use the actual deleted kernel and standard eLpNorm on the original race, on both sampling grids; no helper comparison or moment premise is left as a model input. Common constants for the combined piecewise envelope, restricted sums, joint/local insertion arguments, and the main CLTs remain open.

Twelve further theorems combine the insertion bounds and prove the exceptional restricted sums. `PowerProfile.left_insertion_Lp_envelope` and `PowerProfile.right_insertion_Lp_envelope` use common constants on both regions, with the exact exceptional indicator and actual eLpNorm. Polynomially weighted stretched exponentials are proved summable and have uniform stretched-exponential tails. The literal exceptional kernel has triangular support; this bounds its rows, while summable exponential domination bounds its columns. `exceptionalEnvelope_restricted_sums` and `exceptionalEnvelope_transpose_restricted_sums` prove the manuscript's sum of restricted suprema for right and left exceptional kernels. No summability, tail, or supremum-bound premise is assumed. The typical-kernel restricted-sum assertion, joint/local insertion analysis, and the main CLTs remain open.

Nine further theorems prove the typical-kernel restricted sums on the extreme region. The literal typical kernel with physical denominator is dominated by a constant times an exceptional kernel with exponent b*v/(b+v). Restricted exceptional rows/columns are proved summable, and a generic transfer theorem derives the two real supremum bounds from a pointwise majorant. `right_typical_extreme_restricted_sums` and `left_typical_extreme_restricted_sums` discharge that majorant and prove C*exp(-(d/4)*A^(b*v/(b+v))). The left denominator remains full-rank h through the transposition argument. All model and frozen contract definitions remain unchanged. Packaging the insertion lemma with common constants across its conclusions, global domination, joint/local insertion analysis, and the main CLTs remain open.

Seven further theorems package the one-insertion estimates and begin the global cylinder argument. The two `PowerProfile.*_insertion_moments` theorems give the actual Lp bound and both restricted-sum bounds with common coefficients and explicit tail slack. The positive tail exponent is at most the kernel exponent, and the kernel is unchanged. Closed endpoint-block cutoffs are included. The existing ordered-product inclusion is reused after inspection; a real product expectation bound and finite Holder now give `markedRankCylinder_probability_le_Lp_product` for distinct labels/ranks, including adjacent demands and repeated gap indices. No independence between insertion factors is assumed. Opposite-corner/middle estimates, boundary gaps, the full global matrix bounds and excursions, local joint insertion asymptotics, and all main CLTs remain open.


The latest ten helper theorems complete middle-rank gap geometry, the small-time gap-tail and survival bounds, actual insertion moments and their finite-range Lp conversion, plus a global polynomial sampled-rate bound and its absorption into a source-uniform exponential remainder. The latter gives `(theta_i/n)*exp(-d*n) <= C*exp(-(d/2)*n)` for all sampled source labels, deriving the polynomial bound from the original profile. The eight new modules are on the default build path. The detailed discharge chain is recorded in the obligation ledger. No full global domination matrix or main CLT is claimed.


Five further theorems assemble the ordinary middle kernel with a source-uniform exponential remainder, prove its C/n pointwise bound and constant row/column sum bound, derive the actual insertion-factor Lp target bound, and prove the actual joint middle-rank cylinder probability is at most ofReal(C/n)^r. The latter allows adjacent distinct demanded ranks and proves the deleted-gap range conditions internally; it has only the original profile/sampling model inputs. The full build and all 412 theorem-type/axiom reports pass, with 13 frozen files unchanged. The global matrix across all regions and the main CLTs are still incomplete.


Five further helpers derive upper sampled rates outside a fixed left source block and lower sampled rates outside a fixed right source block, a bounded left rate-time scale with a common cutoff, a scale below one with separate source/target cutoffs, and restoration of the exponential factor at constant cost from a bounded scale. The manuscript proof sentence claiming a scale below one after shrinking a common cutoff is not generally valid when the pole coefficient exceeds one; the assumption audit records the example and the proved bounded-scale alternative. The actual outside-block insertion estimates remain to be assembled. The latest build has 4150 jobs and the complete current audit checks 417 source theorem types and axiom reports, with all 13 frozen files unchanged. Main Section 6 CLTs and contract checks remain unresolved.


Four further helpers prove the actual deleted survival expectation is at most one, derive a source-unrestricted left insertion norm bound from the actual remaining-rate floor, establish the exact rate-floor power identity, and assemble `PowerProfile.left_outside_insertion_Lp`. The last theorem gives the ordinary exponential kernel at the actual deleted-gap index q for sources outside a fixed left block. Its finite scale bound and every moment/survival premise are discharged from the original profile/sampling. The uniform demanded-rank shift conversion, right-outside and boundary cases, full global domination, and main CLTs remain open. Current verification: 4128 build jobs passed; 421 theorem-type/axiom reports checked; all 13 frozen files unchanged.


Five further helpers close the outside-left demanded-rank conversion: power comparison under doubling, the exponential kernel comparison, the concrete norm bound in h with all bounded shifts, the numerical bounded-source inverse-row inequality, and its concrete C/n norm corollary. The latter derives the rate upper bound from the original profile and exact sampling. The cutoff is halved and the exponent coefficient is 1/2^alpha; the actual random gap/kernel remains unchanged. The outside-left insertion region is now handled away from the fixed earliest gaps. Current full build: 4150 jobs, exit 0; all 426 source theorem types/axioms checked with only permitted foundational axioms; 13 frozen files unchanged. Right-outside estimates, boundary gaps, the global weighted matrix and cycle excursions, and all main CLTs/contract checks remain open.


Four further helpers prove a right comparison-depth survival estimate retaining the actual source rate, derive the eventual range for sublinear comparison depths, establish the outside-right survival bound with remainder exp(-d*n^(beta/(beta+1))), and prove source-unrestricted right shifted insertion moments with the actual rate in the prefactor. The small-depth branch uses the different balancing time and a profile-derived outside-source lower rate; the large-depth branch uses the usual comparison depth. All required means, range bounds and rate floors are proved. Their Lp assembly and polynomial-prefactor absorption remain unresolved. Current verification: full build 4150 jobs, exit 0; 430 source theorem types/axioms checked; all 13 frozen files unchanged. No main CLT or Section 6 contract check is claimed.


Three further theorems complete outside-right finite-range Lp assembly, derive polynomial-prefactor absorption from the original sampled profile, and combine these into `PowerProfile.right_outside_insertion_envelope`. The actual norm is bounded by the ordinary rate-time kernel plus a source-uniform exp(-d*n^(beta/(beta+1))) remainder, with no assumed rate upper bound or analytic proof input. Bounded shifts/deletions are retained. Current full build: 4150 jobs, exit 0; all 433 source theorem types/axioms checked; 13 frozen files unchanged. Fixed boundary gaps, the global matrix and weighted/excursion estimates, local joint asymptotics, and all main CLTs/contract checks remain incomplete.


Three further theorems handle earliest active-left gaps: a fixed-removal rate floor of order n^alpha, a generic kernel norm conversion whose rate-floor premise is explicit, and the concrete early_left_insertion_Lp theorem that fully discharges that premise. The result includes q=0 and every fixed finite gap q<=Q, yielding C*theta_i/n^alpha for actual insertion norms. No positive lower rank is assumed. Current full build: 4150 jobs, exit 0; all 436 theorem types/axioms checked; 13 frozen files unchanged. Latest/infinite gaps, inactive boundary regions, full matrix estimates and all main CLTs/contracts remain open.


Five further helpers establish the infinite-final-gap reduction: arrival count implies passage of a finite reference gap start; every later exact gap is contained in the corresponding survival tail; the actual race supplies almost-sure hypotheses; arbitrary later-gap moments are bounded by the reference survival moment; and a finite reference gap is constructed before a fixed terminal window. The natural later gap index includes the infinite final gap without changing its definition. Endpoint-specific reference selection and terminal norm decay still require proof. Current full build: 4150 jobs, exit 0; all 441 theorem types/axioms checked; 13 frozen files unchanged. The global matrix and main CLTs/contracts remain incomplete.


Two further helpers complete the outside-right terminal-window norm estimate, including the infinite final gap, and establish the universal insertion norm bound by one. terminal_outside_right_Lp constructs the reference gap and sublinear comparison depth internally, derives every rate/survival premise, and gives C*exp(-d*n^(beta/(beta+1))) uniformly over the fixed terminal window and finite moment range. Same-corner source-depth terminal decay and inactive-endpoint boundary regions remain open. Current full build: 4150 jobs, exit 0; all 443 theorem types/axioms checked; all 13 frozen files unchanged. Global matrix estimates and all main CLTs/contracts remain incomplete.


Two further concrete boundary theorems are complete: terminal_right_depth_Lp gives C*exp(-d*a^(beta/(beta+1))) for every source in a fixed terminal window, including the infinite final gap and bounded source depths; inactive_left_insertion_Lp gives C/n for the initial-quarter actual gaps, including q=0. All reference gaps, cutoffs, rate floors and source bounds are derived from the original profile/sampling. Current full build: 4150 jobs, exit 0; all 445 source theorem types/axioms checked; 13 frozen files unchanged. Inactive-right terminal estimates, demanded-rank packaging where needed, global matrix/weighted estimates, excursions and all main CLTs/contracts remain open.


Four further helpers prove the deleted interior survivor reservoir, the exact logarithmic square-root identity, the actual survivor mean lower bound sqrt(n*m)/16 at c*log(n/m) with M*c<1/2, and the actual logarithmic order-time tail exp(-rho*sqrt(n*h)). The reservoir uses only interior continuity/positivity and exact sampling, and all Chernoff threshold premises are discharged from the stated deletion/shift/depth ranges. Current full build: 4150 jobs, exit 0; all 449 theorem types/axioms checked; 13 frozen files unchanged. Inactive-right moment/norm assembly, final-gap specialization, global matrix estimates and all main CLTs/contracts remain open.


## Inactive-right logarithmic kernel assembly

Five further theorems complete the finite shifted-gap inactive-right moment/norm argument. `inactive_right_shifted_moment` derives the all-order remaining-rate floor from `global_lower_of_right_finite` and the actual surviving-label cardinality. `logarithmic_survival_envelope` combines the previously proved logarithmic Chernoff tail with the exact survival split. `inactive_right_insertion_Lp` converts the resulting moments to the actual insertion norm, uniformly for 1<=p<=p0. `logarithmic_rate_remainder` absorbs the polynomial sampled-rate prefactor while preserving sqrt(n*h): n<=sqrt(n*h)^2 for h>=1 permits direct polynomial absorption at that square-root scale. `inactive_right_insertion_envelope` combines these results into C*((theta/h)*exp(-d*theta*log(n/h))+exp(-d*sqrt(n*h))). The ordinary exponential equals (h/n)^(d*theta) mathematically; no Lean representation identity is claimed newly checked here.

These helpers cover n>=8, 16r<=n, 8r+8<=h, 16384h<=n, at most r deleted labels and |q-(n-h)|<=r+1, with q a finite background gap. These explicit helper ranges still need to be discharged in the global matrix construction. Fixed final inactive-right gaps remain unresolved. No main Section 6 CLT or closed contract check is proved.

Validation for this assembly: all five new individual module checks exited 0; the full default build exited 0 with 4155 jobs; the regenerated 454-theorem statement/axiom audit exited 0; the validator exited 0 with only the three allowed foundational axioms and all 13 frozen files unchanged. Section 6 source scan found no placeholders or checking bypasses. Main CLTs remain incomplete.


## Inactive-right final gaps and complete endpoint-region kernel

Six further statements close the inactive-right final-gap obligation and combine all positive target depths. `terminal_logarithmic_insertion_Lp` constructs a finite reference gap and proves a norm bound for every later natural gap index, including the actual infinite final gap. `bounded_terminal_logarithmic_kernel` proves the numerical comparison when m<=H and H^2<=n. `terminal_inactive_right_envelope` chooses H=8r+Q+8 and N=16384H+H^2, derives the positive source-rate floor from the finite-right profile, and proves the manuscript-shaped kernel throughout a fixed terminal window.

`inactive_right_all_gaps_envelope` combines this with the finite shifted-gap theorem. For h>=8r+8 it proves that the natural gap index is nonterminal before constructing its finite index. For smaller h it proves membership in the terminal window Q=10r+10. Thus the resulting bound applies uniformly to every 1<=h with 16384h<=n and every natural gap q satisfying |q-(n-h)|<=r+1, for all sufficiently large n and at most r deletions. There is no missing fixed-depth case. `exp_neg_rate_log_ratio` checks the exact real-power identity, and `inactive_right_all_gaps_power_kernel` states the literal kernel C*((theta/h)*(h/n)^(d*theta)+exp(-d*sqrt(n*h))).

The all-gap theorem uses only the original finite-right PowerProfile, exact SampledRates, and the explicitly stated helper region/order bounds. No endpoint floor, reference witness, survival bound or moment bound is an assumed input. Global domination matrix construction, its row/weighted estimates, cycle excursions and the main Section 6 CLTs/contracts remain unresolved.

Validation for the all-gap inactive-right assembly: five new modules (six named theorems) compiled individually with exit 0. The default build exited 0 with 4160 jobs. The regenerated audit of all 460 source theorem types and transitive axiom sets exited 0; the validator exited 0 with only propext, Classical.choice and Quot.sound and all 13 frozen files unchanged. The source scan again found no sorry/admit/axiom/unsafe/checking-bypass declarations (exit 1, no matches). No Section 6 main theorem or main contract check exists yet.


## Rate-preserving early-target rows

Six further statements prepare the weighted global row argument. `early_insertion_rate_Lp` derives C*theta_i/n from the actual remaining-rate floor in the initial quarter, without requiring an inactive left endpoint. `early_shifted_rate_Lp` proves nonterminal membership and the quarter-range condition for all bounded demanded-rank shifts with 8h<=n and n>=8r+8, including h=1 and q=0. `inactive_left_shifted_Lp` then derives C/n from the finite-left source-rate bound.

`early_insertion_rate_row` sums actual ENNReal Lp norms over any early-target subset and proves a bound C*theta_i. Each target may have its own deletion set and gap index; their bounded-deletion/shift premises are explicit and sufficient for the concrete norm theorem. No independence between insertion factors is assumed. `reciprocal_depth_power_product_le_one` and `early_right_weighted_row` combine that row estimate with the sampled right-power rate upper bound to prove the early-target contribution to the right weighted row bound, with weight (n/a)^kappa and kappa<=beta. This includes the manuscript's 0<kappa<beta range and retains the rate until the power estimate is applied.

The inactive-right ordinary kernel row sum, the remaining global matrix row/column/weighted estimates, the matrix construction and partition, cycle excursions and all main Section 6 CLTs/contracts remain unresolved. These partial row results are not a global matrix theorem.

Validation for the early-target row work: four new modules containing six named statements compiled individually with exit 0. The default build exited 0 with 4164 jobs. The regenerated audit of all 466 source theorem types and transitive axiom dependencies exited 0. The validator exited 0: only the three allowed foundational axioms, all 13 frozen files unchanged, and main_CLTs_complete=false. The source scan found no placeholders or checking bypasses (exit 1, no matches). The inactive-right ordinary row sum and the remaining global matrix pieces are still open.


## Inactive-right row sums

Nine further statements prove the inactive-right contribution to the global row bound. `positive_power_sum_upper` compares the discrete sum of m^z with its integral and both endpoint terms for every z>-1. Its proof splits the sign of z and uses monotone/antitone integral comparison on [1,M], so it handles the integrable singularity at zero without assuming boundedness there. `terminal_power_kernel_factor`, `terminal_power_sum_upper` and `terminal_power_sum_half_bound` derive the discrete bound theta/t+2*theta*(1/2)^t for target depths M<=n/2. With t=d*theta, `rate_times_half_power_bound` absorbs both endpoint corrections uniformly in theta, and `inactive_terminal_ordinary_row_bound` gives a uniform ordinary-kernel sum with no upper-rate hypothesis.

`inactive_terminal_kernel_row_bound` adds the sqrt(n*h) exponential correction using the proved stretched-exponential summability theorem. `inactive_terminal_kernel_subset_bound` injects arbitrary target labels into the positive depth range, proving the same bound on every subset of the inactive endpoint region. `inactive_right_insertion_row` transfers it to the actual sum of ENNReal insertion Lp norms, uniformly over all source labels, target-dependent deletion sets and bounded rank shifts, including final gaps. Every numerical and probabilistic premise is discharged from the original finite-right PowerProfile, exact sampling and the explicit helper geometry.

The inactive-right row-sum obligation is now complete in the endpoint region. Construction of one global matrix, the remaining row/column/weighted pieces and region partition, cycle excursions, local joint asymptotics, and all main Section 6 CLTs/contracts remain unresolved.

Validation for inactive-right rows: six new modules containing nine named statements compiled individually with exit 0. The full default build exited 0 with 4170 jobs. The regenerated audit of all 475 theorem types and transitive axiom dependencies exited 0; the validator exited 0 with only propext, Classical.choice and Quot.sound, all 13 frozen files unchanged and main_CLTs_complete=false. The source scan found no placeholders or checking bypasses (exit 1, no matches). The inactive-right row contribution is proved; the single global matrix and main Section 6 theorems remain incomplete.


## Ordinary active-endpoint row sums

Nine further statements establish uniform finite-depth sums of the ordinary active kernels. `comparable_depth_rpow_bounds` treats every real exponent on comparable positive depths. `power_density_cell_bound` bounds each sampled integer-depth density by an integral comparison on the following unit cell, reducing the comparison envelope's positive decay constant by 2^abs(p). `power_density_integrable` and `integral_power_density` derive the exact full integral 1/(d*abs(p)) for A*x^(p-1)*exp(-d*A*x^p), for either sign of p. The coefficient A is arbitrary positive and cancels from the integral.

`power_density_positive_sum_bound` combines those cell comparisons with the proved full integral to bound every finite positive-integer sum uniformly in A. This supplies the manuscript's ordinary row estimates without needing a new unimodality hypothesis. The exact identities `scaled_power_density_factor` and `reverse_ratio_power` preserve the original formulas. `scaled_power_kernel_row_bound` gives the forward-ratio kernel theta*(h/n)^p/h*exp(-d*theta*(h/n)^p) for every p!=0, including p=alpha>1. `inverse_power_kernel_row_bound` gives the original active-right kernel theta*(n/h)^beta/h*exp(-d*theta*(n/h)^beta), beta>0. Both bounds are uniform in all positive row scales and source rates, and in the finite depth cutoff.

These are ordinary-kernel row components. The single global domination matrix, its complete boundary/exceptional assembly and region partition, remaining column/weighted estimates, cycle excursions and main Section 6 CLTs/contracts are still unresolved.

Validation for ordinary active rows: five new modules containing nine named statements compiled individually with exit 0. The full default build exited 0 with 4175 jobs. The regenerated audit of all 484 theorem types and transitive axiom dependencies exited 0; the validator exited 0 with only propext, Classical.choice and Quot.sound, all 13 frozen files unchanged and main_CLTs_complete=false. The source scan found no placeholders or checking bypasses (exit 1, no matches). Full actual active rows, the single global matrix and all main Section 6 CLTs remain incomplete.


## Ordinary active columns and the right weighted row

Nine further statements extend the unit-cell argument to arbitrary polynomial weights. `power_weighted_cell_bound` allows a power a independent of the exponential power p. `power_weighted_density_integrable` and `integral_power_weighted_density` prove integrability and the exact coefficient/scaling C*A^(1-(a+1)/p), under precisely (a+1)/p>0. `power_weighted_positive_sum_bound` retains this scaling in the finite integer-depth sum, uniformly in A>0 and the cutoff.

`ordinary_power_column_bound` instantiates the weight a=p and scale A=t^(-p), proving a numerator column bound C*t. `right_ordinary_column_bound` discharges the integrability condition from beta>0; `left_ordinary_column_bound` discharges it from alpha>1 and proves the original inverse-ratio formula. These are numerical numerator columns for the same-corner ordinary kernels, not complete columns of a constructed global matrix.

`weighted_inverse_density_identity` proves the exact weighted right-kernel representation. `right_ordinary_weighted_row_bound` uses weight exponent kappa-beta-1 and exponential exponent -beta; its Gamma integrability condition follows from kappa<beta, and its scale factor cancels against a^(-kappa). Thus the same-corner ordinary right row with weight (h/a)^kappa has a uniform bound in source depth a and finite target cutoff. It assumes no extra rate regularity, moment or matrix estimate.

Remaining: full global matrix construction and region partition; assembly with actual-profile comparisons, boundary and exceptional edges; weighted contributions between different regions; root/excursion and collision estimates, joint insertion asymptotics and all main Section 6 CLTs/contracts.

The matching ordinary left weighted-row argument is in `Section6LeftOrdinaryWeightedRow`: `weighted_forward_density_identity` preserves the original (a/h)^kappa weight and forward-ratio kernel, and `left_ordinary_weighted_row_bound` derives integrability from kappa<alpha. The generic numerical theorem only needs alpha>0, so the manuscript alpha>1 is sufficient. This closes the local ordinary left weighted-row component; complete actual/global weighted rows still require outside-corner, boundary and exceptional assembly.

Validation for these eleven statements: the full default build exited 0 with 4181 jobs; the regenerated 495-theorem elaborated-type and transitive-axiom audit exited 0; the validator exited 0 with only the three permitted foundational axioms and all 13 frozen files unchanged. No Section 6 main CLT or closed contract-check theorem is proved. Earlier portions of this file are a chronological record; later completed milestones supersede their lists of then-unresolved helper estimates.

## Outside-left actual weighted rows

Five further statements prove the contribution from sources outside the left corner to moderate left targets. `outside_left_weighted_power_identity` preserves the literal kernel and changes the weighted power to alpha-kappa. `outside_left_weighted_sum_bound` applies the positive-power sum comparison at that positive exponent, retains the source-rate prefactor, and includes the empty target range. `outside_left_weighted_subset_bound` transfers it to arbitrary target subsets in the early half by an injective label map.

`PowerProfile.outside_left_weighted_insertion_row` applies these estimates to the actual ENNReal insertion norms, deriving the outside-source rate bound from the original profile. It permits target-dependent deletion sets and natural gap indices and proves finite-gap membership internally from h>=8r+8, the chosen endpoint range, and bounded deletion/rank shifts. `PowerProfile.outside_left_manuscript_weighted_row` gives exactly the manuscript weight (d_L(i)/d_L(j))^kappa for 0<=kappa<alpha. It proves d_L(i)<=n and compares weights; it does not assume a weighted matrix estimate or an extra bounded-rate condition.

The fixed earliest-target weighted contribution, other global regions, exceptional contributions and the single global matrix still require assembly. Joint insertion asymptotics, cycle estimates and all main CLTs/contracts remain unresolved. These new regional helpers do not alter the frozen theorem contracts or model definitions.

Validation for outside-left weighted rows: all five new statements compiled individually; the full default build exited 0 with 4184 jobs; the regenerated type and axiom audit exited 0 for all 500 source theorems. The validator exited 0 with only the three permitted foundational axioms and all 13 frozen files unchanged. The source scan found no placeholders or checking bypasses. Main CLTs and closed contract checks remain incomplete.

## Earliest left weighted rows and outside-left assembly

Four further statements complete the fixed earliest-target weighted contribution and combine the outside-left branches. `PowerProfile.global_left_sampled_power_upper` extends the original left power upper estimate to all source labels, using the derived bound away from the left endpoint. `PowerProfile.left_weighted_rate_bound` then proves theta_i*d_L(i)^kappa/n^alpha<=K, uniformly over all sources and kappa<=alpha, by exact cancellation of the power envelope.

`PowerProfile.early_left_weighted_insertion_row` applies the actual earliest-gap norm estimate, including gap zero, to arbitrary target subsets with h<=H. It derives q<=H+r from the bounded rank shift, constructs the large-row threshold giving finite background-gap membership, bounds each weighted factor by a common constant, and proves that there are at most H targets. Deletion sets and natural gap indices may vary with the target. The source is unrestricted.

`PowerProfile.outside_left_all_weighted_row` combines this result with the moderate outside-left weighted row using a proved partition at h=8r+8. Its resulting endpoint region has no lower-depth exclusion: all positive target depths in the chosen block are covered. The original profile and sampling discharge the analytic inputs; no model or frozen contract definition was changed.

The same-corner actual weighted row, other global regions and exceptional terms still require assembly. The single global matrix, joint insertion asymptotics, cycle estimates, and every main Section 6 CLT and closed contract check remain unresolved.

Validation for earliest left weighted rows and outside-left assembly: all four new statements compiled individually; the full default build exited 0 with 4187 jobs; the regenerated elaborated-type and transitive-axiom audit exited 0 for all 504 source theorems. The validator exited 0 with only propext, Classical.choice and Quot.sound, all 13 frozen files unchanged and main_CLTs_complete=false. The complete elaborated type of outside_left_all_weighted_row was inspected. The source scan found no placeholders or checking bypasses.

## Sampled-rate ordinary weighted kernels

Six further statements transfer the numerical weighted sums to the original sampled rates. `weighted_rate_kernel_comparison` proves the generic comparison using an upper rate-time bound for the prefactor and a lower bound for exponential decay. `PowerProfile.left_sampled_ordinary_weighted_row` and `PowerProfile.right_sampled_ordinary_weighted_row` derive both bounds from the original profile and sampling, using the exact power-time identities, and discharge them in the comparison helper. They retain the ordinary kernel's theta_i*tau_h/h factor and the manuscript's left or right weight. The required summability restrictions are kappa<alpha and kappa<beta respectively.

`positive_depth_subset_sum_le` proves an injective positive-depth comparison with the finite interval 1..n. `PowerProfile.left_sampled_weighted_subset` and `PowerProfile.right_sampled_weighted_subset` apply it to the actual zero-based label and terminal-depth conventions, proving injectivity, range and nonnegativity. These results hold for arbitrary target subsets, making the ordinary bounds available for the global region partition.

These are ordinary kernel contributions, not a completed insertion-norm row or global domination matrix. The complete same-corner insertion bound must also handle its exceptional and boundary components and discharge its validity ranges. Joint insertion asymptotics, cycle estimates, the critical regime and all Section 6 main CLTs/contracts remain unfinished.

Validation for sampled-rate weighted kernels: all six new statements compiled individually. After building the left branch separately, the full default build exited 0 with 4191 jobs. The regenerated elaborated-type and transitive-axiom audit exited 0 for all 510 source theorems. The validator confirms only the three permitted axioms and all 13 frozen files unchanged. Both sampled subset theorem types were inspected, including implicit parameters and their original rate expressions. The source scan found no placeholders or checking bypasses. No main CLT or closed contract check is claimed.

## Exceptional weighted sums and actual left-corner rows

Six further statements supply the exceptional weighted contributions and the actual left-corner weighted insertion row. `exceptional_weighted_factor_le` proves that the manuscript weight is at most one on the exceptional support; the zero-envelope case is separate. `right_exceptional_weighted_row_bound` and `left_exceptional_weighted_row_bound` derive uniform finite-depth weighted sums from the proved exceptional summability and tail estimates. The left kernel is the exact transpose. `left_envelope_weighted_sum_bound` adds the ordinary and exceptional contributions.

`PowerProfile.left_corner_weighted_insertion_row` applies this complete envelope bound to actual insertion norms with target-dependent deletions and natural shifted gaps. It constructs the endpoint cutoffs, chooses the permitted envelope exponent v=1 internally, proves nonterminal gap membership, and discharges the finite target-sum comparison. Its initial statement applies when both source and target depths exceed the derived H.

`PowerProfile.left_corner_all_targets_weighted_row` then removes the target cutoff by a proved partition and the existing earliest-target weighted row. It constructs an integer cutoff and large-row threshold for that branch. All positive target depths in the chosen left block are covered, including the earliest gaps.

The source-depth restriction d_L(i)>=H remains explicit. Fixed small source depths must still be proved; this regional theorem is not the full matrix result. Complete right actual weighted rows, other global contributions, the single global matrix, joint insertion asymptotics and all main Section 6 CLTs/contracts remain unresolved.

Validation for exceptional weighted rows and left-corner assembly: all six new statements compiled individually; the full default build exited 0 with 4195 jobs; the regenerated elaborated-type and transitive-axiom audit exited 0 for all 516 source theorems. The validator confirms only propext, Classical.choice and Quot.sound, with all 13 frozen files unchanged. The complete elaborated type of left_corner_all_targets_weighted_row was inspected, including its explicit source cutoff. The source scan found no placeholders or checking bypasses. All main CLTs and closed contract checks remain incomplete.

## Left-endpoint weighted rows for all sources

The apparent fixed small-source gap was an unnecessary restriction in the existing left extreme helper. Its proof used source depth>=H only to prove source depth>=1. Replacing that use with the elementary fact for every Fin label proves the stronger extreme statement; the underlying exponential moment already has no lower source cutoff. The moderate helper likewise has no such cutoff. The envelope and weighted-row proofs now carry this strengthening through. The all-target left-corner theorem has no lower source-depth restriction and no output H. No frozen definition or main contract has changed.

`PowerProfile.left_endpoint_weighted_insertion_row` combines the strengthened same-corner all-target theorem with the outside-left all-target theorem. It constructs a common target block and large-row threshold, splits sources at the local source cutoff, and discharges both branches. The resulting weighted insertion sum applies to every source label and every positive target depth in the derived left block, including fixed earliest targets and exceptional edges. Deletion sets and natural gap indices may vary with targets.

This completes the left-endpoint target contribution to the weighted row. The full global matrix still requires the other endpoint and middle/cross-region contributions, as well as its complete column and target bounds. Joint insertion asymptotics, cycle estimates, critical-regime arguments and all main CLTs/contracts remain incomplete.

Validation for removal of the left-source cutoff: the strengthened proof chain and the insertion-moments compatibility rebuild passed. The new left_endpoint_weighted_insertion_row compiled individually. A parallel full-build attempt failed to read an existing compiled dependency; audit/section6-source-cutoff-read-failure.log preserves that failure. After building the affected branch separately, the full default build passed with 4196 jobs. The regenerated elaborated-type and transitive-axiom audit passed for all 517 source theorems; the validator confirms only propext, Classical.choice and Quot.sound and all 13 frozen files unchanged. The complete new theorem type was inspected and has no source restriction. Main CLTs and closed contract checks remain unproved.

## Right-corner weighted rows including terminal targets

The right extreme proof no longer assumes a lower source cutoff: its extreme-support inequality gives source depth>target depth, so the target cutoff supplies the needed source cutoff for the survival estimate. Source positivity is proved from the label. The right envelope is strengthened accordingly; the existing insertion-moments statement is preserved and its proof adapted.

`right_envelope_weighted_sum_bound` combines the ordinary and exceptional right weighted sums. `PowerProfile.right_corner_weighted_insertion_row` applies it to actual insertion norms for moderate target depths, deriving nonterminal membership and the terminal-depth rank identity internally. It covers every positive source depth in the derived right block.

`right_fixed_targets_weighted_row` handles the remaining fixed terminal targets using the proved universal norm bound by one and the bounded right weight, including arbitrary natural gap indices and the infinite final gap. It proves the finite target cardinality bound. This is sufficient for the weighted row contribution but does not replace the stronger decay estimates needed for columns or target maxima.

`PowerProfile.right_corner_all_targets_weighted_row` partitions targets and combines both branches, removing the lower target cutoff. All source and target depths in the derived right block are covered. Outside-right sources, other global contributions, the full matrix and all main CLTs/contracts remain incomplete.

Validation for right-corner weighted assembly: the strengthened proof chain and insertion-moments compatibility rebuild passed, and all four new statements compiled individually. Concurrent compilation encountered compiled-file read failures, including the full-build attempt preserved in audit/section6-right-assembly-read-failure.log. Sequential branch builds followed by the full default build succeeded (4200 jobs). The regenerated type and transitive-axiom audit passed for all 521 source theorems; the validator confirms only propext, Classical.choice and Quot.sound and all 13 frozen files unchanged. The full elaborated right_corner_all_targets_weighted_row type was inspected: it retains only the right source-block condition, with no lower source or target cutoff, and accepts natural gap indices. Main CLTs and closed contract checks remain unproved.
