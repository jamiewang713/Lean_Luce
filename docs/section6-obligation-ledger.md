# Section 6 obligation ledger

### Lemma 6.12 completed: interior-grid sampling and the full Section 6 contract

`lemma612 : Lemma612Contract.lemma612` proves all three interior-grid
conclusions by specializing the completed `powerLaw65`, `spatial65`, and
`critical` theorems. `lemma612_contractCheck` checks the conjunction directly
against the three original contracts. `section6` and `section6_contractCheck`
also prove the unchanged complete `SampledProfileContract.section6`, covering
both grids. All four are imported by the default `Luce` library.

Verification: full build passed (4567 jobs); all four type and transitive-axiom
checks passed with only `propext`, `Classical.choice`, and `Quot.sound`.
The original Lean inputs and toolchain are unchanged. A separate manuscript
edit during the run caused a whole-file snapshot mismatch; Lemma 6.12's
statement was re-read and is unchanged. This task made no TeX edits and
preserved the baseline. See `section6-lemma612.md` and
`audit/section6-lemma612-validation.json` for the exact scope and evidence.

### Section 6.5 completed: power-law CLTs and localization

The exact closed `powerLaw65 : SampledProfileContract.powerLaw grid` and
`spatial65 : SampledProfileContract.spatial grid` are proved, with independent
contract checks for both grids and the combined `section65` theorem. They are
imported through `Luce.Section6`. The conclusions include coefficient positivity,
total and spatial mean asymptotics, all stated joint Gaussian limits, and
localization for every positive excursion threshold. Arbitrary probability-space
Luce realizations, empty coordinate sets, fixed points, mixed active corners,
the original root convention, and the exact rounded cutoffs are covered.
No additional model assumptions or axioms are introduced.

The proof retains the exact ideal means in Proposition 6.10, uses finite
polynomial expansions and the centered Poisson recurrence, and proves the
moment-to-Gaussian passage by finite characteristic-function Taylor estimates.
Lemma 6.8 supplies mean normalization; Lemma 6.7 gives the essential logarithmic
expectation bound for removal of the core and the spatial excursion estimate.
The exact finite law transfers the resulting race-statistic limits to the
closed contracts. See `section65-power-law-clts.md` for details and the module map.

Verification: the full build passed (4564 jobs), including the preserved
concurrent critical-profile import. All 192 new declarations (161 theorems in
51 modules) passed the dedicated type and transitive-axiom audit, using only
`propext`, `Classical.choice`, and `Quot.sound`. All 720 files in this task's
initial frozen snapshot remain unchanged. Four older manuscript hash
mismatches remain recorded without resetting their baselines. The unchanged
whole-Section-6 generator produced 1328 reports out of 1337 names; its nine
namespace failures are retained separately, and all nine declarations pass
the supplement using their actual qualified names. Exact evidence is in
`audit/section65-validation.json`. This entry supersedes the historical open
statuses for the Section 6.5 CLTs, means, and localization below; the separate
critical-profile work is described in its own entry.

### Section 6.6 completed: critical pole

`critical : SampledProfileContract.critical grid` and its independent checks
prove the fixed-point Gaussian limit, the `log (log n)` mean asymptotic, and the
uniform bound for every longer cycle, on both grids. The probability estimates
and all analytic bounds follow from the original profile and sampling inputs.
Full build: 4564 jobs passed. Dedicated type/axiom audit: all 122 declarations
passed, using only `propext`, `Classical.choice`, and `Quot.sound`. Original
contract/profile hashes are unchanged. See `section6-critical.md` for the proof
and evidence. This discharges the critical application described as open below.

### Proposition 6.11 completed: adapted Bernoulli central limit criterion

The closed `proposition611 : Proposition611Contract.proposition611` and independent
`proposition611_contractCheck` are proved and imported through `Luce.Section6`.
The exact two convergence-in-probability assumptions yield the Gaussian weak
limit for arbitrary adapted finite Bernoulli rows. Predictable caps, integrability,
and all complex exponential estimates are derived internally. The expectation
assertion follows from the proved equality of expected count and compensator.
See `section6-proposition611.md` for the proof and validation record.

The generic criterion is discharged. Its application to the critical-pole
profile, including the required compensator estimates, remains a separate
obligation. Earlier main-theorem and historical-audit limitations below remain.

### Proposition 6.10 completed: core factorial moments

The exact closed `proposition610 : Proposition610Contract.proposition610` and independent `proposition610_contractCheck` are proved and imported through `Luce.Section6`. The conclusion retains the actual mixed falling-factorial moment, the exact ideal category means, the original largest-label root convention, and the prescribed rounded cutoffs. Empty category lists, zero factorial orders, fixed points, either or both active corners, root intervals, and both sampling grids are covered. No extra model assumptions or axioms are introduced.

The proof combines exact cycle-counting equivalences, the mixed-corner Proposition 6.5 local law, collision bounds from the Lemma 6.9 argument, and weighted-path estimates for every nonmoderate edge. All matrix, kernel, cutoff, integrability, and error premises are discharged. If s is the number of proposed cycles, the proof supplies K=s+1 and a derived positive decay exponent. See `section6-proposition610.md` for the proof and module map.

Verification: full build passed (4412 jobs); all 87 new theorem declarations in 38 modules passed the dedicated type and transitive-axiom audit with only `propext`, `Classical.choice`, and `Quot.sound`. Prior Proposition 6.5 proof components match their saved hashes. The unchanged all-source generator produced 1070 successful reports out of 1075 names: its five failures omit the existing `TraceDensity68` namespace. This failed historical audit is retained separately. The 17-file Proposition 6.10 snapshot matches all 16 non-manuscript files; the manuscript changed outside this task after the snapshot. This task did not edit the manuscript or reset any baseline. Exact evidence is in `audit/section6-proposition610-validation.json`. Later CLTs remain open.

### Lemma 6.8 completed: total and spatial ideal traces

`lemma68 : Lemma68Contract.lemma68` and `lemma68_contractCheck` are proved and imported through `Luce.Section6`. The exact ideal sums include distinctness, the canonical root, and all rounded cutoffs. The quantitative logarithmic lattice comparison and collision estimate give a refined total expansion with a common boundary constant; nested-interval subtraction cancels that constant and proves the spatial bound with K = 1 and kappa = 1. No helper bounds are added to the closed theorem's hypotheses. Full build passed (4374 jobs), and the dedicated axiom audit passed using only the three permitted foundational axioms. The TeX manuscript is unchanged. See `section6-lemma68.md` for the proof and verification evidence. Broader-audit failures in older entries predate the new aggregate import.

### Lemma 6.9 completed: overlaps and nearby ranks

The exact independent `Lemma69Contract.lemma69` is proved by `lemma69`, with a separate `lemma69_contractCheck`, imported through `Luce.Section6`. Both the specified-pair estimate and the all-pairs union estimate have the stated bound `K/A * (1+log(B/A))^(s-1)`. Repetitions, empty configurations, fixed points, either corner, mixed corners, and cutoff boundaries are covered. The mixed-corner proof uses bounded nearby-label fibres and does not assume asymptotic separation of the cores. Only the matrix and joint-weight bounds stated in the manuscript are inputs; no additional model assumptions or axioms were introduced.

Verification: full build passed (4342 jobs); all 34 new theorem declarations in 11 modules passed the dedicated audit with only the three permitted foundational axioms. The new 14-file contract snapshot matches completely, including the unchanged manuscript and toolchain. The regenerated all-source audit remains affected by unrelated concurrent Lemma 6.8 declarations absent from aggregate imports and reaches its existing error limit. Historical manuscript hash mismatches remain recorded without resetting any baseline. See `section6-lemma69.md` and `audit/section6-lemma69-validation.json`.

### Proposition 6.5 completed: direct normalized-gap proof

The exact closed `proposition65_localLaw`, `proposition65`, and separate `proposition65_contractCheck` are proved. The proof includes separated moderate configurations with mixed active corners, the interval consequence with the same final constant, empty configurations, and the full global matrix conclusion for adjacent ranks and all other configurations. Both supported grids and the stated cutoff boundaries are covered. No assumptions, axioms, frozen definitions, manuscript edits, or checker changes were introduced.

The completed proof uses actual normalized gaps, polynomial time/weight concentration, off-diagonal profile asymptotics, survival-preserving Taylor bounds, exact mixed moments, Holder, and Cauchy--Schwarz. The skeleton-concentration obligations in the historical entries below are superseded by this alternative proof, not assumed. See `section6-proposition65-normalized-gaps.md` and the dedicated type/axiom audit `audit/Section6Proposition65.lean`.

Verification: full build passed (4331 jobs), and the dedicated audit passed for all 63 new theorems in 23 modules with only the three permitted foundational axioms. The regenerated Section 6 audit snapshot has 825 successful reports out of 847 declarations; the 22 missing declarations are in unrelated concurrent increment-density, convolution, and continuous-trace files outside the aggregate imports. This prevents a passing all-source audit and historical validator, but does not affect the closed Proposition 6.5 checks. See `audit/section6-proposition65-validation.json` for the exact results and missing names.

Historical audit limitation: this run's initial 47-file snapshot check already found a manuscript mismatch, with the other 46 files unchanged. The manuscript and historical baselines are preserved. Build and proof audit results are reported separately from that pre-existing hash failure. This entry supersedes earlier unresolved-status entries for Proposition 6.5; later CLT obligations remain open.

### Proposition 6.5: profile weight scale and actual random-time bridge

Completed: deletion decreases each nonnegative moment; scaled Chebyshev; both concrete profile theorems with bound C/(eps^2*m); surviving-weight path monotonicity; exact full/deleted gap-rate identity; deterministic intermediate-time transfer; and the ae deleted identity with positivity/injectivity discharged from the actual race.

Remaining: combine endpoint probability events with the random-time identity, control drift from the two deleted means to the target mean, handle selected-spacing errors, prove the joint skeleton good-event bound, and complete the weighted local approximation and closed contract check. The fixed-time and deterministic results do not by themselves close these obligations.

### Proposition 6.5: marked displacement and variance route (711 declarations)

Completed: left/right marked-gap depth errors at most r from the original source/rank injectivity and mark budget. Completed: actual survival indicators belong to L2; their variance is at most their mean; deleted weighted survival sums have exact mean n*deletedD(1,t) and variance at most n*deletedD(2,t); Chebyshev bounds deviations by that variance scale divided by the squared absolute tolerance. Independence and all integrability requirements are discharged from the original finite exponential race. No normalization, finite uniform rate cap, or extra profile condition is supplied.

Alternative route under investigation: polynomial concentration combined with the existing weighted Holder estimates, preserving the complete local error conclusion. Still required: profile-derived relative variance scaling, random-time control, selected-spacing polynomial tail bounds, joint skeleton control, and the full weighted error assembly. None is assumed. The exact main contract and its final proof check remain incomplete.

### Proposition 6.5: profile-derived quantile windows (704 declarations)

Completed: `scaled_deletion_error`; both `PowerProfile.*_deleted_quantile_separation` theorems; `relative_mean_separation`; both `PowerProfile.*_gap_quantile_window` theorems. Mean-separation premises of the four generic gap-time bounds are discharged from the original profile and exact finite deletion estimates. The slope a<=1 is constructed as an output, not imposed on a profile parameter.

Still to discharge in the final marked configuration: the effective gap-depth error at most r; 8r<=a*u*m via the chosen lower cutoff; shrinking-window domain; lower bounds on actual deleted means giving uniform exponential decay in depth. Remaining-weight concentration, joint skeleton concentration, weighted exceptional-event error, full local law and closed contract check remain unresolved. The main contract is unchanged.

### Proposition 6.5: count-to-gap-time transfer (698 declarations)

Completed in Section6GapTimeRelativeTail: the arrival-before-later-count inequality; late-gap arrival-count bound with sentinel handling; exact arrival/survivor complement identity; early and late gap-time probability bounds from both arrival and survivor relative tails. The four probability theorems discharge measurability/independence/injectivity requirements using the actual exponential race and existing probability-one event. They retain explicit numerical separation premises in terms of q, undeleted cardinality N, and exact deleted means nG/nH.

Remaining discharge: obtain these numerical inequalities at the quantile window from the original sampled-profile mean-separation estimates and bounded deletion/rank displacement. No separation inequality has been added to the frozen main theorem. Remaining-weight concentration, joint skeleton concentration, weighted error assembly, full local law and closed contract check also remain unproved.

### Proposition 6.5: relative count concentration (691 declarations)

Completed: `bernoulli_sum_exp_integrable` proves exponential integrability at every real tilt; `bernoulli_relative_upper_tail` and `bernoulli_relative_lower_tail` prove exp(-eps^2*m/4) tail bounds for any 0<=eps<=1. Generic measurability, zero-one range and independence are discharged for the actual race in `deleted_arrival_relative_tails` and `deleted_survivor_relative_tails`. Their means are exactly n*deletedG and n*deletedH, with positivity of finite weights as the only model restriction; time nonnegativity and relative-scale bounds are numerical domains.

Still unresolved: convert these fixed-time counts to quantile-window bounds using the existing mean separation; prove remaining-weight concentration; assemble joint skeleton concentration and the weighted exceptional-event estimate; prove the unchanged full local approximation and closed contract check. Fixed-time count concentration alone does not close any of these obligations. See the primary audit for actual verification results.

### Proposition 6.5: exact skeleton assembly (686 declarations)

Completed: `normalized_skeleton_integral` splits the actual normalized product law and applies Fubini with integrability proved; `marked_normalized_skeleton_integral` reindexes by the image of the injective marked-gap map; `later_selected_rate_reindex` identifies Lambda with the original later marked rates; `separated_cylinder_skeleton_formula` combines these with the actual order mixture. Every selected rate is positive by Weights, and selected-gap injectivity follows from the original rank separation. The generic terminal buffer is still discharged by the existing endpoint-block lemma when choosing h0 and delta.

Completed: `ordered_remaining_rate_antitone` and `skeleton_displacement_bound` prove chronological rate monotonicity and 0<=T-U<=(sum selected xi)/W under nonnegative selected spacings, available almost surely from standardGapLaw. This is valid across corners and supplies a deterministic step toward concentration.

Unresolved: the joint skeleton concentration bound, weighted exceptional-event bound, actual mixed-corner approximation, full local error and interval consequence, and separate closed main contract check. None is an allowed extra hypothesis. Exact types/axioms and command results are recorded in `section6-proposition65-transfer-evidence.md` and `section6-proposition65-audit.md`.

### Proposition 6.5: order transfer and integrability (680 declarations)

Completed: `deleted_order_normalized_integral`, `deleted_order_kernel_product_integral`, and `deleted_kernel_product_order_sum` transport and partition the actual deleted race, discharging concrete measurability and raw-product integrability. `separated_cylinder_order_sum` applies this to the actual cylinder. Its generic terminal buffer is derived by `endpoint_block_terminal_buffer` from selectable output constants delta<=1/2 and h0>=r+1; `sorted_gap_nonfinal_of_terminal_buffer` proves the finite-index conversion. `normalized_insertion_product_bounds` and `normalized_insertion_product_integrable` establish the [0,1] bound and integrability from positive weights and the actual standard exponential law.

Unresolved: selected-gap image reindexing and Fubini assembly with the actual order sum; joint skeleton concentration; weighted exceptional-event and mixed-corner error estimates; full frozen local-law conclusion and separate contract check. None has become a hypothesis. Full build 4277 jobs and 680-declaration type/axiom audit passed; the 47-file freeze is unchanged; older manuscript hash validation remains failed. Detailed statements and evidence are in `section6-proposition65-audit.md` and `section6-proposition65-transfer-evidence.md`.

### Proposition 6.5: pointwise skeleton product and exact selected-coordinate integral (672 declarations)

Completed in `Section6SkeletonProduct`: definition and positivity of the literal later-selected-rate sum; exact interchange of selected earlier/later pairs; weighted-start and survival-product identities; full insertion-product factorization. No probabilistic independence or asymptotic premise occurs in these identities.

Completed in `Section6SelectedCoordinateIntegral`: selectedFill leaves unselected coordinates fixed; skeleton_start_fill proves invariance; selected_coordinate_integral integrates the actual normalized-gap insertion product over the selected standard-exponential coordinates for fixed chain and unselected values. Its result is exactly skeleton survival times the rational product, and integrability is included. Positivity of remaining rates and nonnegativity of summed later rates are discharged from finite positive weights and selected marked-rate positivity. The actual marked-rate mapping and deleted-race transport remain to be constructed, not assumed.

Verification: nine new theorems compile; branch 3589 and full 4274-job builds passed; complete 672-declaration type/axiom audit passed with only permitted axioms. New 47-file freeze unchanged; old validator still fails after declaration/axiom checks at manuscript hash. The frozen main Proposition 6.5 and final contract check remain unresolved. Detailed types, restrictions and next obligations are in `docs/section6-proposition65-audit.md`.

### Proposition 6.5: exact integration and skeleton representation (663 declarations)

Completed in `Section6SelectedGapIntegral`: exponential weighted-density identity; integrability and exact Laplace transform; selected-gap integral W*theta/((W+Lambda)*(W+Lambda+theta)); finite product integral under the explicit product measure. Numerical premises W>0, theta>0, Lambda>=0 are explicit generic premises, to be discharged in the future actual-race transfer. No independence instance for insertion events is assumed.

Completed in `Section6SkeletonStart`: exact partition of reconstructed start into selected and unselected earlier spacings; invariance under arbitrary selected-coordinate changes; continuity; nonnegativity; equality to the actual start minus selected preceding raw gap lengths. The final identity is proved through the existing normalized-spacing telescope and preserves the original clock meaning.

Verification: all nine new theorems compile; branch builds 2781/3586 and default build 4272 jobs passed; full type/axiom audit for 663 declarations passed with only permitted axioms. New 47-file freeze unchanged. Old validator fails only after declaration/axiom checks, at the preserved manuscript hash. Remaining actual conditional integration, concentration, local approximation and closed main contract check are detailed in `docs/section6-proposition65-audit.md`; none has been moved into an assumption.

### Proposition 6.5 started: exact contract and four supporting proofs

Full ledger and assumption/type audit: `docs/section6-proposition65-audit.md`. The frozen closed target `Proposition65Contract.proposition65` is NOT proved. It includes the joint local law and the previously proved full global matrix conclusion. No concentration, independence or approximation input has been added.

Completed: exact numerical selected-gap factor error; separation implies injectivity of sorted background gaps; actual separated cylinder equals ONE expectation of the gap-kernel product. Remaining: selected-spacing conditional integration, skeleton joint concentration, weighted bad-event estimate, mixed-corner approximation, telescoping, interval consequence and final closed contract check. These remain obligations, not allowed hypotheses. The current audit covers 654 declarations with only permitted axioms; final build passed (4270 jobs) after sequential branch recovery from compiled-file read failures. The new 47-file freeze passes. Both older manuscript baselines now differ at the manuscript; none was reset. The existing Lemma 6.4 proof is unchanged.

### Full Lemma 6.4 completed (650-theorem milestone)

This entry supersedes all historical Lemma 6.4 unresolved-status entries below. Later Section 6 density, trace, collision and CLT obligations remain outside this completion claim. The exact final contract and actual elaborated/axiom output are in `docs/section6-lemma64-completion.md`.

| Required fact | Completed proof | Hypotheses and discharge in final theorem |
|---|---|---|
| All permitted endpoint combinations | `PowerProfile.active_left_envelope`, `active_right_envelope` | Original profile; active cases use the concrete assembly proofs, inactive output clauses are vacuous. No new model restriction. |
| One deterministic nonnegative matrix and all cylinders up to r | `lemma64_matrix`; `markedRankCylinder_real_probability_le_domination_matrix` | Choose the existing concrete matrix at deletion/norm order r+1. t<=r implies t<=r+1, including zero marks. Entry nonnegativity is proved and C>=1 absorbs the cylinder prefactor. |
| Uniform ordinary rows | `PowerProfile.domination_matrix_row_bound_all_n` | Original profile and p=r+1>0; specialize the source subset to univ and enlarge B to C. |
| Endpoint target maxima and columns | `PowerProfile.active_matrix_target_columns` | Derived under original profile/sampling. Final delta is no larger than the returned cutoff. Positive depth permits enlarging the target numerator; subset univ gives columns. |
| Interior maxima and columns at final delta | `PowerProfile.domination_matrix_interior_target_all_n`, `domination_matrix_interior_column_all_n` | Applied only AFTER deriving positive final delta. Thus no unproved monotonicity under cutoff shrinking is assumed. |
| Both weighted rows with one positive exponent | `PowerProfile.domination_matrix_common_weighted_rows` | Constructs kappa from the original active exponents; all exponent premises discharged. Enlarge common W to C. |
| Both exact corner envelopes, fixed-left rate, fixed-right depth/outside decay | `active_left_envelope`, `active_right_envelope`, backed by the concrete endpoint assemblies | Final delta intersects left, right and target/column cutoffs. Right shared decay is constructed at that final delta. All integer H, positive d,nu and v=1 are outputs with proved validity. Ordinary-plus-exceptional nonnegativity permits increasing constants. |
| One C and compatible parameters for EVERY matrix clause | `lemma64_matrix : Lemma64Contract.matrix` | C=1+L+R+T+W+B+I+J, with all terms constructed and positive. Corner-dependent d,nu,H are defined by the two returned values. No external parameter or assumed estimate occurs. |
| Literal root and discarded-cycle expectation conclusions | `lemma64_cycles : Lemma64Contract.cycles` | Original profile and exact sampling only; all count identities, endpoint expectations, depth sums and real-cutoff transfers discharged by the earlier concrete proofs. |
| Entire original frozen proposition and independent contract check | `lemma64`; `lemma64_contractCheck` | Closed types with no additional explicit, implicit or instance parameters. The theorem is the conjunction of the complete matrix and cycle proofs; the audit theorem obtains it from the implementation. |

Verification: active-envelope and matrix modules compiled; dedicated contract branch passed (4064 jobs); default build passed (4266 jobs). The generated full type/axiom audit exited 0 for all 650 source declarations. Actual main, contract-check, matrix and cycle axiom outputs are `[propext, Classical.choice.{u}, Quot.sound.{u}]`, with no sorryAx or project axioms. All 45 frozen full-lemma files remain unchanged. Original source assumption and full statement/fixed-index estimates were reread and compared to the contract. No new-module placeholders or checking bypasses were found. The old global validator still fails at its original manuscript-hash comparison after passing declaration and axiom checks; that historical mismatch is preserved and explicitly reported.

No mathematical proof obligation for the frozen Lemma 6.4 remains unresolved. The older manuscript's unavailable historical contents remain an audit limitation; no claim is made that the old global validator passed or that later Section 6 CLTs are complete.

### Common active endpoint targets and columns (645-theorem milestone)

| Intermediate fact | Completed proof | Hypotheses and discharge |
|---|---|---|
| Either active endpoint target maximum | `PowerProfile.active_matrix_target` in `Section6ActiveMatrixTargets` | Original profile, fixed budgets p>0. Case analysis uses the concrete left/right target proofs; inactive cases are vacuous. Depth casts are converted exactly. |
| Either active endpoint column bound | `PowerProfile.active_matrix_column` | Same inputs; case analysis uses complete all-n column proofs including fixed target depths. |
| Both active target and column bounds with common C,delta | `PowerProfile.active_matrix_target_columns` | Choose the four proved positive constants and cutoffs. Their minimum gives the common cutoff and their sum gives C. Target numerators are enlarged by division monotonicity; column bounds are enlarged directly. Every input estimate is constructed from the original profile. |

Verification: module compiled and default build passed (4262 jobs). Full generated Lean type/axiom audit exited 0 for 645 source declarations. New transitive axiom reports contain only propext, Classical.choice and Quot.sound. Declaration-set/whitelist checks passed; full validator then failed at line 21 on the preserved older manuscript hash. The newer 45-file freeze remains unchanged. Source scan found no proof placeholders or checking bypasses in the new module.

Remaining full matrix construction: intersect this common endpoint cutoff with the left/right envelope cutoffs; obtain the right assembled decay and interior estimates at that final cutoff; choose the already-proved common weighted exponent and enlarge all constants; verify cylinder and ordinary row clauses; then prove the full matrix proposition and separate full conjunction contract check. The cycle component remains checked. No full Lemma 6.4 theorem is claimed.

### Common weighted exponent and rows (642-theorem milestone)

| Intermediate fact | Completed proof | Hypotheses and discharge |
|---|---|---|
| Positive exponent below each active endpoint exponent | `PowerProfile.common_weighted_exponent` in `Section6CommonWeightedRows` | Original profile only. Assign each active endpoint its exponent and an inactive endpoint 1; choose half the minimum. Profile alpha>1 and beta>0 prove positivity and strict comparisons. |
| Both concrete weighted matrix rows with common C,kappa | `PowerProfile.domination_matrix_common_weighted_rows` | Original profile/sampling, fixed r and p>0. The constructed exponent discharges every premise of the proved left/right all-n weighted rows. Inactive endpoints are vacuous. Sum the two positive constants. No row estimate, compatible exponent witness or additional model restriction is an input. |

Verification: individual compilation and default build passed (4261 jobs); full generated Lean type/axiom audit exited 0 for 642 declarations. Both new axiom outputs contain only propext, Classical.choice and Quot.sound. The complete validator passed declaration-set and whitelist checks, then failed at line 21 on the preserved older manuscript hash. The 45-file full-lemma freeze remains unchanged. Source scan found no placeholders or checking bypasses in the new module.

Remaining: combine these weighted rows with the proved endpoint envelopes, endpoint target/column estimates, ordinary rows, interior estimates and cylinder bound into the full frozen matrix proposition, then prove the full conjunction contract check. The common exponent is no longer an unresolved existence obligation. No full Lemma 6.4 claim is made.

### Shared endpoint envelopes and fixed-target estimates (640-theorem milestone)

| Intermediate fact | Completed proof | Hypotheses and discharge |
|---|---|---|
| Right envelope and both fixed-target estimates with one C,d,nu,H | `PowerProfile.domination_matrix_right_envelope_assembly` in `Section6RightEnvelopeAssembly` | Original right-power profile and sampling, fixed r,p with p>0 and 0<v<=1. First derive delta0 and H=max(1,ceil(h)); for every smaller positive final delta, apply the proved common fixed-right theorem using the envelope's positive decay parameters. Ordinary and exceptional monotonicity prove the original envelope with the returned smaller parameters. C=B+F dominates both constants. No matrix estimate or decay compatibility is assumed. |
| Left envelope and exact fixed-target rate estimate with one C,H | `PowerProfile.domination_matrix_left_envelope_assembly` in `Section6LeftEnvelopeAssembly` | Original left-power profile and sampling, same numerical budgets. Derive H=max(1,ceil(h)), apply the actual all-n fixed-left rate theorem at H, and add constants. Both depth conventions and the literal exceptional orientation are preserved. |

Verification: individual compilations passed; right and left branch builds passed (3829 and 3825 jobs); default build passed (4260 jobs); complete elaborated-type/axiom audit exited 0 for all 640 source theorems. Both new axiom outputs are `[propext, Classical.choice.{u}, Quot.sound.{u}]`. The validator passes exact declaration-set and permitted-axiom checks then fails at line 21 on the old manuscript hash. The newer 45-file contract freeze remains unchanged. No placeholders or checking bypasses occur in either new module.

Remaining full matrix construction: obtain common positive weighted exponent, intersect endpoint envelope and target/column cutoffs, apply right assembly and interior bounds at that final cutoff, enlarge constants across all clauses, and provide the full closed matrix theorem and combined contract check. The cycle component is already proved. These remaining obligations are not allowed new inputs.

### Closed cycle contract and compatible decay (638-theorem milestone)

| Intermediate fact | Completed proof | Hypotheses and discharge |
|---|---|---|
| Root and discarded bounds for either active corner | `PowerProfile.active_root_bounds`, `active_discarded_bounds` | Original profile; case analysis uses the concrete left/right proofs. Inactive corners have vacuous conclusions. |
| Complete closed cycle-count contract | `lemma64_cycles`, separately `lemma64_cycles_contractCheck` | No external parameters. Original profile and exact sampling are quantified inside the frozen proposition. Choose the minimum endpoint cutoff, obtain the interior theorem at that final cutoff, and enlarge the finite collection of constants. Exact root count/indicator equality preserves the literal count. |
| Monotonicity of depth, exceptional, and ordinary envelopes | `depth_decay_mono`, `exceptional_envelope_mono_decay`, `ordinary_envelope_mono_decay` | Numerical positivity/order conditions only, proved with real-power and exponential monotonicity. |
| Positive common decay parameters | `common_decay_parameters` | Positive numerical bounds D,E,N,Z and 0<delta<=1. Construct nu=min(N,Z), b=min(D,E), d=b*delta^nu; prove positivity and every required comparison. |
| Outside-corner decay transfer | `outside_depth_decay` | Positive source depth, delta*n<=a, nonnegative parameters and ordered exponents. Raise the location inequality to nu and use the proved exponential monotonicity. |
| Actual fixed-right bounds with shared d,nu | `PowerProfile.domination_matrix_fixed_right_common` | Original profile/sampling, fixed budgets and positive numerical envelope bounds. The actual matrix bound is supplied by the proved fixed-right theorem, not an assumption. The common-parameter construction discharges all monotonicity premises and proves both depth and row-size conclusions together. |

Verification: default build passed (4258 jobs); the complete generated Lean type/axiom audit exited 0 for 638 declarations. Declaration-set and axiom-whitelist checks passed; the validator then failed at line 21 on the preserved old manuscript hash. Actual axiom reports for both closed cycle declarations and the common fixed-right theorem contain only propext, Classical.choice and Quot.sound. The newer 45-file full-lemma freeze remains unchanged. No full matrix theorem or full `lemma64_contractCheck` exists yet.

Remaining: construct the complete frozen matrix proposition, choosing final delta before obtaining interior/outside bounds, one positive weighted exponent compatible with both active endpoints, compatible endpoint decay parameters and integer cutoffs, and one C for all clauses. Then combine with the checked cycles theorem and provide the separate full closed contract check. These are unresolved proof obligations, not additional permitted hypotheses.

### All-row matrix refinements and frozen full contract (627-theorem milestone)

| Intermediate fact | Completed proof | Hypotheses and discharge |
|---|---|---|
| Middle target maximum and columns for all n | `PowerProfile.domination_matrix_interior_target_all_n`, `domination_matrix_interior_column_all_n` in `Section6DominationMatrixInteriorAll` | Original profile, exact sampling, fixed positive p, r and interior margin eps>0. The derived finite row cutoff is absorbed into C using M<=1. Summing at most n entries gives the all-row column bound. No size threshold remains in either result. |
| Constructed finite-row rate absorption constant | `finite_sampled_rate_ratio_bound` in `Section6FiniteSampledRatios` | Any f, real exponent alpha and finite row cutoff M. The constant is 1 plus the finite sum of the absolute sampled ratios on BOTH grids. Exact sampling and positive finite weights discharge division positivity; no uniform positive floor is an input. |
| Exact fixed-left rate-dependent matrix bound for all n | `PowerProfile.domination_matrix_fixed_left_rate` in `Section6DominationMatrixFixedLeftRate` | Original left-power profile and sampling, fixed r,H,p>0. Matrix attainment plus bounded shifts and deletions prove the finite gap condition for large n, then the actual early insertion Lp theorem gives C*theta_i/n^alpha. The finite-ratio lemma handles all remaining rows. |

Contract: `Luce/Section6Lemma64Contract.lean` defines closed `matrix`, `cycles`, and `lemma64 := matrix ∧ cycles` propositions. It imports mathematical definitions and earlier foundational modules but no combined implementation theorem. `matrix` has one existential matrix and shared C/delta across every clause, including both weighted rows, complete endpoint/middle target and column bounds, exact corner envelopes, fixed-left rate bounds, and fixed-right depth/outside-source decay. `cycles` uses literal maximum-root cardinalities and real-cutoff discarded counts. All model data are universally quantified and only original PowerProfile and SampledRates are antecedents. Detailed type, representation, hypothesis and source audit: `docs/section6-lemma64-contract.md`.

Freeze: the new independent specification was compiled before freezing its 45-file project/source/configuration closure in `audit/section6-lemma64-contract-freeze.json`. `python audit/freeze_section6_lemma64_contract.py` verifies this snapshot without overwriting it and currently passes with no changed files. The original 13-file baseline remains untouched and continues to fail on the manuscript. The new snapshot is explicitly a later source snapshot for the newly defined full lemma, not a resolution of the old mismatch. Do not silently change either snapshot or the contract when implementing its proof.

Verification: four new source theorems compiled; separate branches passed (3818 jobs for the interior bounds, 3824 for fixed-left rates). Default build passed (4253 jobs). The full generated elaborated-type/axiom audit exited 0 for 627 declarations and now prints all three contract definitions. They have closed type Prop. All four new theorem axiom outputs contain only propext, Classical.choice and Quot.sound; central types were inspected. The validator passes its declaration and axiom checks then fails at line 21 on the older manuscript hash. The source scan's sole hit was the English word 'admit' in a doc comment in Section6FiniteSampledRatios, not a tactic or declaration; no proof placeholder or checking bypass was found.

Remaining: prove the frozen matrix and cycles propositions with compatible constants and cutoffs; provide the concrete full theorem and a separate closed `lemma64_contractCheck` importing it and this contract. This includes proving any needed monotonicity when reconciling decay constants/exponents and outside-right decay, rather than assuming those facts or weakening the clauses. No such full theorem or contract check is claimed complete.

### Full real-cutoff discarded-cycle expectations (623-theorem milestone)

Representation: `Section6DiscardedCountDefinitions.intervalDiscardedCycleCount R side k A B` is a new literal finite count with REAL cutoffs: filter the existing `cycleOrbits R k` by A<=depth(maximum)<=B and existence of an orbit vertex whose depth is outside [A,B]. It does not redefine the earlier logarithmic excursion count or any frozen production object. The audit generator now prints this definition in full.

| Intermediate fact | Completed proof | Hypotheses and discharge |
|---|---|---|
| Both integer-cutoff discarded expectation assemblies | `PowerProfile.right_discarded_expectation_nat`, `left_discarded_expectation_nat` | Original profile and exact sampling; natural 1<=A<=B, B/n<=delta. The literal count's expectation is the sum of actual maximum-root event probabilities. Outside-root terms vanish; inside terms use the checked one-sided geometry and concrete excursion probabilities. Reindex by the injective depth map, enlarge its image to [A,B], and apply the proved kernel sum. Returns C=K*(1+1/q), with all quantities constructed. |
| Exact positive-depth interval rounding | `positive_depth_interval_rounding` | Any positive natural depth and arbitrary real A,B. Membership equals membership in [max(1,ceil A),floor B], using the nonzero-depth floor equivalence even when B is negative. |
| Literal count preserved by rounding; empty reversed intervals count zero | `interval_discarded_count_rounding`, `interval_discarded_count_eq_zero_of_lt` | Any actual permutation and either corner. Positivity of both depth conventions is proved. Both root membership and every escaping-vertex condition are preserved. |
| Generic integer-to-real expectation transfer | `discarded_expectation_real_of_nat` | Generic proved integer estimate and C>=0. Nonempty rounded intervals imply floor(B)>0, hence floor(B)<=B; the size condition is preserved. Empty rounded intervals have literal zero count. No rounding error or extra domain restriction is assumed. |
| Both full real-cutoff O(1) expectation bounds | `PowerProfile.right_discarded_expectation`, `left_discarded_expectation` | Only the original profile, exact sampling, fixed k (cycle length k+1), and B/n<=delta. C>0 and 0<delta<1 are conclusions. All integer-transfer hypotheses are discharged by the concrete integer theorems. Arbitrary A,B are supported, including empty intervals; n=0 is harmless because there are no cycles. |

Verification: all eight new declarations compiled individually and are imported by the default root. Both integer assembly branch builds passed (4034 jobs each), and the default build passed (4249 jobs). The complete generated Lean type/axiom audit exited 0 for 623 named source declarations. Both full endpoint theorem types and the new count definition were inspected. Every new axiom report contains only propext, Classical.choice and Quot.sound. The validator passes exact declaration-set and permitted-axiom checks then fails at line 21 on the preserved manuscript hash; only `fixed_points_sampled_profile.tex` differs among the original 13 frozen files. The baseline was not reset. The current manuscript's original profile assumption and full Lemma 6.4 statement were reread at lines 385 and 2141; the excursion component is unchanged relative to the previously read statement. This does not certify the full manuscript against its unavailable frozen contents. The source scan found no proof placeholders or checking bypasses.

Remaining full-lemma work: combine and audit the deterministic matrix properties with the root and discarded-count expectations under one closed, implementation-independent contract. In particular, `domination_matrix_interior_target` and `domination_matrix_interior_column` currently retain a derived large-n threshold; their finite-small-row cases must be discharged in the final uniform matrix result. Review same-corner exceptional/fixed-index bounds and constant/quantifier compatibility before claiming the complete lemma. The full combined contract check is not proved. The excursion expectation itself is no longer an unresolved mathematical estimate.

### Uniform depth sums and exact filtered-count expectations (615-theorem milestone)

| Intermediate fact | Completed proof | Hypotheses and discharge |
|---|---|---|
| Decreasing positive-depth sum <= first value + integral | `antitone_depth_sum_le` in `Section6ExcursionDepthSums` | Generic antitone function on [A,B], natural A<=B. Exact finite interval shift and the checked integral comparison. |
| Both scaled power sums <= 1+1/kappa | `left_excursion_depth_sum`, `right_excursion_depth_sum` | Only kappa>0 and positive integer cutoffs. Left uses the antitone integral of x^(-kappa-1). Right splits kappa<=1 and kappa>1, covering all positive exponents. Every integrability/monotonicity premise is proved. |
| Literal manuscript kernel sums <= 1+1/kappa | `left_excursion_kernel_sum`, `right_excursion_kernel_sum` | kappa>0 and natural 1<=A<=B. Exact real-power algebra gives summands (1/m)*(A/m)^kappa and (1/m)*(m/B)^kappa. Right drops the lower cutoff using nonnegativity. These are numeric helpers, not a restriction of the intended final theorem to integer cutoffs. |
| Actual filtered cycle count = maximum-root count = sum of indicators | `filtered_cycle_count_eq_root_count`, `filtered_cycle_count_eq_indicator_sum` in `Section6FilteredCycleCount` | Any finite permutation, cycle length k+1, and any predicate P on maximum label and literal orbit. Uses existing maximum injectivity and orbit identity. No count definition is changed. |
| Expected filtered cycle count = sum of actual root-event probabilities | `filtered_cycle_expectation_eq_probability_sum` | Positive finite weights, k, arbitrary P. Finite permutation statistics prove measurability; indicators are bounded by one, proving integrability. No measurable/integrable predicate or probability estimate is an input. P remains a freely chosen filter, to be instantiated by the actual discarded-cycle condition. |

Verification: eight new declarations compiled and are imported by the default root. Separate branch builds passed (3405 jobs for depth sums, 3977 for filtered counting), then the default build passed (4244 jobs). The full generated elaborated-type and transitive-axiom audit exited 0 for 615 named source declarations. All eight new axiom reports contain only propext, Classical.choice and Quot.sound; literal kernel and expectation types were inspected. The full validator passes declaration-set and axiom checks and fails at line 21 on the manuscript hash. The freeze still reports only `fixed_points_sampled_profile.tex` changed among 13 files; no baseline was reset. The source scan found no proof placeholders or checking bypasses. An initial indicator-integrability approach encountered a Lean norm-instance mismatch; proving bounded measurable integrability directly resolved it without a mathematical assumption change.

Remaining exact assembly: specialize the filtered expectation identity to roots in [A,B] whose orbits leave [A,B], apply the checked largest-root geometry and actual endpoint excursion probabilities, reindex the finite label sum by its injective depth map, and apply the uniform kernel sums. Empty root intervals and integer/real cutoff representation must be handled explicitly; the integer helper domain is not permission to narrow the manuscript theorem. Then prove the full combined Lemma 6.4 contract. No source obstruction is claimed; these remain implementation obligations.

### Actual excursion probabilities and largest-root geometry (607-theorem milestone)

| Intermediate fact | Completed proof | Hypotheses and discharge |
|---|---|---|
| An escaping orbit gives an escaping nonroot tuple vertex | `exists_escaping_cycle_tail` in `Section6EscapingCycleCylinder` | Any finite permutation, actual minimalPeriod=k+1, escape predicate P false at the root, and an actual orbit witness. Enumerates the canonical rooted cycle; includes k=0, where escape is impossible. |
| Actual escaping event bounded by escaping cylinders | `escaping_cycle_probability_le_cylinders` | Positive finite weights only. Uses almost-sure distinct race clocks and the exact one-based marked rank assignments. Retains injectivity, root avoidance, and the escaping predicate. |
| Actual escaping event bounded by T times escaping open paths | `escaping_cycle_probability_le_paths` | Generic closing-target premise M(i,v)<=T, T>=0, and k+1<=r. The concrete cylinder theorem supplies the product; distinctness is dropped by nonnegativity while escape is retained. |
| Right and left actual endpoint escape probabilities | `PowerProfile.right_excursion_cycle_probability`, `left_excursion_cycle_probability` in `Section6EndpointExcursionProbability` | Original profile and exact sampling, fixed cycle length k+1 and stated root/boundary locations. Every generic matrix/path premise above is discharged using existing all-n estimates. Positive kappa is returned, constructed as beta/2 or alpha/2 from the original exponent restrictions. Returned K is positive even at k=0. No intermediate estimate or exponent restriction is assumed of the model. |
| Largest-root interval exit is one-sided | `maximum_root_right_escape_iff`, `maximum_root_left_escape_iff` in `Section6MaximumRootExcursion` | Existing maximumCycleRoots membership gives every orbit vertex <= root. Right depth is therefore >= root depth and left depth <= root depth. These are exact equivalences for the literal orbit, using the appropriate root-in-interval boundary. |

Verification: all seven declarations compiled individually, are imported by the default root, and the full build passed (4242 jobs). The full Lean elaborated-type/axiom audit exited 0 for 607 declarations. Both complete concrete excursion theorem types were inspected: only original profile/sampling and the stated numerical domains occur; K, kappa and delta are existential conclusions. All seven new axiom reports contain only propext, Classical.choice and Quot.sound. The validator passes the exact declaration-set and permitted-axiom checks then fails at line 21 on the preserved manuscript hash. The freeze reports only `fixed_points_sampled_profile.tex` changed among 13 files. No baseline was reset and the earlier validation JSON is historical. The source scan found no placeholders or checking bypasses.

Remaining: identify the literal number of discarded cycles with the sum of its maximum-root indicators, pass to expectation using the checked actual probabilities and geometry, and prove uniformly bounded sums of m^(-1)*(m/B)^kappa and m^(-1)*(A/m)^kappa over A<=m<=B. The full combined Lemma 6.4 statement and contract verification are still unresolved. These are implementation obligations, not additional allowed hypotheses; no source obstruction is asserted.

### Weighted escaping paths (600-theorem milestone)

| Intermediate fact | Completed proof | Hypotheses and discharge |
|---|---|---|
| Total potential visited by a k-edge path has weight <= k*C^k*V(root) | `forward_path_occupation_bound` in `Section6WeightedPaths` | Generic nonnegative finite matrix, nonnegative V, ordinary rows <= C, and potential-weighted rows <= C*V(i). Induction splits the first vertex and uses the existing ordinary path sum bound. |
| Paths visiting V >= L have weight <= k*C^k*V(root)/L | `forward_path_escape_bound` in `Section6WeightedPaths` | Same generic premises, L>0. Every escaping path has occupation at least L; positivity allows dropping its restriction. Includes k=0. |
| Actual right paths reaching depth >= B | `PowerProfile.right_escaping_path_bound` in `Section6RightEscapingPaths` | Original right-power profile, exact sampling, 0<kappa<beta, fixed r and p>0. V(i)=rightDepth(i)^kappa. Existing all-n ordinary and weighted matrix rows provide every generic premise, using their maximum constant. Gives k*C^k*m^kappa/B^kappa without any corner restriction on paths. |
| Actual left paths reaching depth <= A | `PowerProfile.left_escaping_path_bound` in `Section6LeftEscapingPaths` | Original left-power profile, exact sampling, 0<kappa<alpha, fixed r and p>0. V(i)=1/leftDepth(i)^kappa. All generic premises are discharged as on the right. Gives k*C^k*(1/m^kappa)/(1/A^kappa), the same decay as k*C^k*(A/m)^kappa. |

Verification: all four declarations compiled individually and are imported by the default root. The initial combined build failed with out-of-memory panics in the two parallel endpoint jobs. Separate endpoint branch builds passed (3825 jobs each); the subsequent default build passed (4239 jobs). The generated full Lean type/axiom audit exited 0 for 600 declarations. All four new axiom outputs are `[propext, Classical.choice.{u}, Quot.sound.{u}]`; the endpoint elaborated types were inspected. The validator passes declaration and axiom checks then fails on the preserved manuscript hash, with only `fixed_points_sampled_profile.tex` changed among 13 frozen files. The source scan found no proof placeholders or checking bypasses. The prior validation JSON remains historical.

Still unresolved: translate the escaping-path sums to the actual number of discarded cycles, using the largest-root geometry and the closing-edge target estimate; prove the uniform root-depth sums of m^(-1)*(m/B)^kappa and m^(-1)*(A/m)^kappa; choose positive kappa from the profile internally in the combined statement; finish and check the full Lemma 6.4 contract. Existing `rootedCycle_mem_periodicOrbit_iff` in Section5CycleAssignments supplies a checked route from an escaping orbit vertex to the tuple representation, but that conversion has not yet been implemented. These remain proof obligations, not permitted hypotheses.

### Interior probabilities and exact largest-root expectations (596-theorem milestone)

`PowerProfile.interior_cycle_vertex_probability` in `Section6InteriorCycleProbability` proves C/n for every row. The actual matrix row and interior target bounds discharge the generic cycle-probability premises; probability <= 1 absorbs the finite small-row range. No bound is assumed.

`cycle_count_at_maximum_eq_indicator` in `Section6MaximumRootExpectation` proves that the literal finite number of unrooted cycles with maximum v equals its maximum-root indicator, using injectivity of the existing cycle maximum. `maximum_root_expectation_le_cycle_probability` proves the measurable-indicator integral identity and event inclusion. The three concrete `PowerProfile.right_maximum_root_expectation`, `left_maximum_root_expectation`, and `interior_maximum_root_expectation` then give C/m or C/n from original profile/sampling inputs. Cycle length is k+1, including length one. No counting definition was changed.

Verification: both modules are now imported by the default root; build passed (4236 jobs). The generated full type/axiom audit exited 0 for 596 named source declarations, all using only propext, Classical.choice and Quot.sound. The validator passed those two checks and failed at line 21 on `fixed_points_sampled_profile.tex` differing from its frozen hash. No baseline was reset; the previous validation JSON is historical. The source scan found no sorry, admit, axiom, unsafe or checking bypass. A deprecation warning for Set.mem_setOf_eq does not affect checking.

Remaining exact obligation: uniformly over active-corner intervals A <= B <= delta*n, bound by a constructed constant the expected number of cycles rooted at depths in [A,B] with some vertex outside [A,B]. Available inputs are the original profile/sampling and the proved global ordinary/weighted matrix rows and target bounds. The weighted escaping-path estimate, its root-depth summation, and final combined lemma/contract check are not yet proved. This is an implementation gap; no mathematical obstruction is claimed.

### Actual endpoint cycle-through-vertex probabilities

Validation: individual compilations and default build passed (4234 jobs). The full Lean audit exited 0 for 590 declarations; both concrete endpoint probability types and all three axiom reports were inspected. Only `[propext, Classical.choice.{u}, Quot.sound.{u}]` occur, including through the reused Section 5 cycle and path lemmas. Source scan found no placeholders/checking bypasses. The validator still fails the preserved manuscript hash after declaration/axiom checks. Full Lemma 6.4 remains incomplete.

`cycle_vertex_probability_le_matrix_rows` in `Section6CycleVertexBound.lean` is a generic finite bound T*C^k for cycles of length k+1. It uses the existing exact cycle-cylinder union bound, the concrete matrix cylinder estimate, and the checked generic `forwardPathWeight_sum_le`. Distinctness is dropped only after nonnegativity is proved. The closing edge is bounded by T; the k open edges are summed by rows. The k=0 case is included.

`PowerProfile.right_cycle_vertex_probability` and `left_cycle_vertex_probability` in `Section6EndpointCycleProbability.lean` discharge ALL generic matrix premises with the proved all-n row and active target bounds at deletion/moment order k+1. Their only inputs are the original profile and exact sampling. They prove the actual probability of minimalPeriod=k+1 is <=C/m throughout a derived endpoint block, every row size. Constants are constructed. These three declarations compiled. Largest-root expectation conversion, interior probability/expectation, excursion loss, and final combined contract verification remain unresolved; the full lemma is incomplete.

### Complete right-endpoint columns and fixed-depth source decay

Validation: individual compilations and the default build passed (4232 jobs). The full Lean audit exited 0 for 587 declarations; the complete final right-column type and all three new axiom outputs were inspected, with only `[propext, Classical.choice.{u}, Quot.sound.{u}]`. Source scan found no placeholders/checking bypasses. The validator again passes declaration/axiom checks but fails the preserved manuscript hash. Full Lemma 6.4 is not claimed complete.

`PowerProfile.domination_matrix_fixed_right_decay` in `Section6DominationMatrixFixedRight.lean` proves C*exp(-d*a^(beta/(beta+1))) for every source and row size at each bounded terminal target depth. Matrix attainment reduces to the actual fixed-terminal moment theorem; `uniform_moment_decay` and the kernel moment-to-norm theorem prove the norm bound. For bounded a or small n, M<=1 is absorbed by the constructed constant exp(e*max(A,N)^z). No moment or rate premise is assumed.

`domination_matrix_fixed_right_column` sums the decay by the proved stretched-exponential summability theorem and injective terminal-depth map. `domination_matrix_right_column` combines fixed and above-cutoff targets. Its conclusion covers every target in the derived right block, all source subsets and every row size, with no lower-depth premise. All three declarations compiled. Both complete active-endpoint column bounds are now proved. Rooted-cycle expectations, excursion loss and final common-matrix theorem packaging/contract verification remain unresolved.

### Fixed right-target survival moments, including the final gap

Validation: individual compilation and default build passed (4231 jobs). The full Lean audit exited 0 for 584 declarations; the new complete elaborated type was inspected, including natural k, unrestricted positive moment orders and the actual probability integral. Its axiom output is `[propext, Classical.choice.{u}, Quot.sound.{u}]`. Source scan found no placeholders/checking bypasses. The validator again passes declaration/axiom checks but fails the preserved manuscript hash; full Lemma 6.4 remains incomplete.

`PowerProfile.fixed_right_kernel_moment` in `Section6FixedRightSurvival.lean` proves a bound 2*exp(-d*a^(beta/(beta+1))) for any positive moment order, every target of fixed terminal depth<=H0, and every natural gap index within the permitted rank shift. Its inputs are original profile/sampling, bounded deletions, and constructed lower source-depth/row-size cutoffs. There is no source-corner restriction.

Discharge: choose L=max(8*r+8,H0+r+1); the earlier reference gap n-L is finite and below the demanded natural gap. At comparison depth y=a^(beta/(beta+1)), the existing comparison-depth survival theorem and exact power identity balance both exponents. The source-depth cutoff proves y>=max(H,L); the sublinear row cutoff proves y/n<delta uniformly for a<=n. `deleted_gap_moment_le_earlier_survival` then includes every later gap, including the infinite final gap. The theorem compiled. Conversion to matrix norms, bounded source/row cases and fixed-right column summation remain unresolved, followed by rooted expectations and excursions.

### Complete left-endpoint columns, including fixed target depths

Validation: individual compilations and final default build passed (4230 jobs). The full Lean audit exited 0 for 583 source theorems; the complete final column type and all three new axiom outputs were inspected, with only `[propext, Classical.choice.{u}, Quot.sound.{u}]`. Source scan found no placeholders or checking bypasses. The full validator passes declaration/axiom checks but again fails the preserved manuscript hash check; full audit approval is not claimed.

`PowerProfile.domination_matrix_fixed_left_power` in `Section6DominationMatrixFixedLeft.lean` transfers the earliest-gap C*theta_i/n^alpha estimate to the unchanged matrix. The rank shift supplies a bounded natural gap, the constructed large-row threshold proves finite-gap membership, and the already proved global weighted rate bound gives C*(i.val+1)^(-alpha). No rate bound is assumed.

`domination_matrix_fixed_left_column` sums that source decay using the p-series theorem with alpha>1, which is an original profile hypothesis. Finite depth-map injection transfers the source sum; small rows are absorbed via M<=1. `domination_matrix_left_column` then combines these fixed-target columns with the completed above-cutoff theorem. Its conclusion covers every target in the constructed left block, every source subset and every row size, without any lower-depth premise. All three declarations compiled. Right fixed-depth columns, rooted-cycle expectations and excursion loss remain unresolved.

### Right columns with every source and row size above the fixed cutoff

Validation: all three declarations compiled and the default build passed (4229 jobs). The full Lean audit exited 0 for 580 declarations; the combined column's complete elaborated type and all three new axiom outputs were inspected. Only `[propext, Classical.choice.{u}, Quot.sound.{u}]` occur. Source scan found no placeholders/checking bypasses. The validator again passes declaration/axiom checks but fails at the unchanged frozen baseline for the externally changed manuscript; full audit approval is not claimed.

`PowerProfile.domination_matrix_outside_right_target` in `Section6DominationMatrixOutsideRight.lean` transfers the actual inverse-row insertion estimate to the unchanged matrix using finite attainment; bounded deletion/shift arithmetic proves the finite-gap index. `domination_matrix_outside_right_column` sums at most n entries bounded by C/n. Their local source block and constructed large-row/depth cutoffs are explicit.

`PowerProfile.domination_matrix_right_column_above_cutoff` combines the outside and same-corner column contributions, partitions sources internally and absorbs small rows with M<=1 and n<N. Only original profile/sampling and fixed r,p>=1 enter; every source and every row size are covered. A derived lower target-depth cutoff remains. All three declarations compiled. Fixed terminal/initial target columns, rooted-cycle expectations and excursion losses remain unresolved; full Lemma 6.4 is incomplete.

### Outside-right inverse-row insertion estimate

Validation: both declarations compiled; the final default build passed (4228 jobs), and the regenerated Lean audit exited 0 for 577 source theorems. The full concrete insertion type was inspected; its axiom output is `[propext, Classical.choice.{u}, Quot.sound.{u}]`. Declaration and axiom checks pass before the validator again fails at the preserved manuscript hash. No placeholders or checking bypasses were found. Full Lemma 6.4 remains incomplete.

`outside_right_kernel_inverse_row` in `Section6OutsideRightInverseRow.lean` is a numerical helper: positive beta,b,d and theta>=b give (theta*(n/h)^beta/h)*exp(-d*theta*(n/h)^beta)<=C/n for n,h>0. It splits the exponential using the existing rate-kernel bound and absorbs (n/h) with the proved exponential power bound of order 1/beta. Its rate-floor premise is discharged below, not added to a concrete model theorem.

`PowerProfile.right_outside_insertion_inverse_row` in `Section6RightOutsideInverseTarget.lean` derives that floor from `sampled_lower_outside_right` under the original profile and exact sampling. It applies the numerical bound to the actual outside insertion envelope and bounds its stretched-exponential remainder by D/n using `stretched_exp_le_inverse_depth`. All constants are constructed. The regional source/target conditions, finite-gap index, deletion/shift bounds and derived cutoffs remain explicit at this helper stage. Both declarations compiled. Matrix transfer and outside-right column summation, fixed-depth columns, rooted expectations and excursion loss remain unresolved.

### Left columns with every source above the fixed cutoff

Validation: individual compilations and default build passed (4226 jobs). The regenerated Lean audit exited 0 for 575 declarations; the combined column's complete elaborated type and all three new axiom reports were inspected. Only `[propext, Classical.choice.{u}, Quot.sound.{u}]` occur. The source scan found no proof placeholders or checking bypasses. The full validator again fails solely at the frozen manuscript hash after its declaration/axiom checks; the freeze is preserved and the old validation JSON is historical. This remains an incomplete Lemma 6.4 milestone.

`PowerProfile.domination_matrix_outside_left_target` proves C/n for outside-left sources from the original profile and sampling. It attains the matrix maximum and proves the finite gap index from the target cutoff and bounded shifts/deletions. `domination_matrix_outside_left_column` sums at most n such entries; the arbitrary outside-source block is the only regional source restriction.

`PowerProfile.domination_matrix_left_column_above_cutoff` in `Section6DominationMatrixOutsideLeft.lean` combines this with the completed same-corner column theorem. It constructs the source partition, common target cutoff and fixed depth buffer internally. Its conclusion covers every source subset and every row size with no source-region premise; a derived lower target-depth cutoff remains explicit. Only original left-power profile and exact sampling, fixed r,p>=1 enter. All three declarations compiled. Fixed initial targets, full right-column assembly, rooted-cycle expectations and excursion loss remain unresolved.

### Same-corner column contributions

Verification: individual compilations and the default build passed (4225 jobs). The full Lean type/axiom audit exited 0 for 572 declarations; both concrete column types were inspected. The validator passed its declaration-set and permitted-axiom checks, then FAILED on `fixed_points_sampled_profile.tex` differing from the frozen hash. The initial freeze check this turn passed; the manuscript subsequently changed without an edit from this task. The current Lemma 6.4 statement is unchanged compared with the previously read text, now at line 2163. Do not reset the frozen hash or describe full validation as passed. Full manuscript equivalence remains unaudited; the previous validation JSON remains historical.

`right_corner_envelope_column_bound` and `left_corner_envelope_column_bound` in `Section6CornerEnvelopeColumns.lean` prove uniform finite column sums of the full ordinary-plus-exceptional kernels. The existing ordinary numerator sum is O(h), canceled by its exact denominator h. The exceptional contribution uses the proved weighted row bounds at exponent zero, exchanging right and left orientations. The right power is positive and the left power exceeds one, exactly as supplied by the profile; no summability is assumed.

`PowerProfile.domination_matrix_right_corner_column` and `domination_matrix_left_corner_column` in `Section6DominationMatrixCornerColumns.lean` apply these numerical bounds to the unchanged matrix. Constants and cutoffs come from the original profile and sampling. Fix v=1 internally, apply the proved concrete matrix envelope pointwise, and transfer any source subset by its injective depth map into the positive finite depth interval. No column estimate is an input. Both actual column results retain their same-corner source restriction and derived lower target-depth cutoff. Outside-source contributions, the excluded fixed depths, rooted-cycle expectations and excursion loss remain unresolved. These four checked declarations do not complete Lemma 6.4.

### Same-corner concrete matrix envelopes

Validation: both individual compilations and the default build passed (4223 jobs). The regenerated audit/validator passed for all 568 source theorems; both complete elaborated types were inspected, and each has axiom output `[propext, Classical.choice.{u}, Quot.sound.{u}]`. All 13 frozen files are unchanged. The source scan found no sorry, admit, axiom, unsafe or checking-bypass declarations.

`PowerProfile.domination_matrix_right_corner_envelope` and `domination_matrix_left_corner_envelope` in `Section6DominationMatrixCornerEnvelope.lean` prove the literal ordinary-plus-exceptional bounds for the existing concrete matrix. Both use original profile/sampling inputs, fixed r,p>=1 and a freely chosen 0<v<=1. All constants, corner cutoff and lower target-depth cutoff are constructed. No source lower-depth condition is imposed. The right exceptional term is E(a,h); the left is E(h,a), with exactly the existing definition.

Discharge: attain the finite matrix maximum, construct its finite gap index with the proved shift/deletion arithmetic, and apply the completed actual insertion envelope. The cutoff is halved to cover non-strict source and target boundaries; in the left case it also proves 2*h<=n. Nonnegativity justifies the real/ENNReal conversion. Both declarations compiled. The finite excluded target-depth estimates needed for bounded columns and excursions are NOT discharged by these envelope theorems or by the coarser target maxima. Endpoint columns, these boundary estimates, rooted-cycle expectations and excursion losses remain unresolved.

### Full left-endpoint matrix target bound

Validation: individual compilation passed, final default build passed (4222 jobs), and the regenerated audit/validator passed for all 566 source theorems. The complete elaborated type and axiom output were inspected: `[propext, Classical.choice.{u}, Quot.sound.{u}]`. All 13 frozen files remain unchanged. The source scan found no sorry, admit, axiom, unsafe or checking-bypass declarations. Lemma 6.4 remains incomplete.

`PowerProfile.domination_matrix_left_target` in `Section6DominationMatrixLeftTarget.lean` proves M(i,j)<=C/(j.val+1) for every source and row size, whenever (j.val+1)/n<=delta. Only the original left-power profile, exact sampling and fixed r,p>=1 enter. The concrete matrix definition is unchanged.

Discharge: choose a cutoff at most 1/2 and strictly inside the local corner cutoff. Matrix attainment and `shifted_left_nonterminal` supply the actual finite-gap index; the latter's 2*h<=n premise follows from the chosen cutoff. The local corner insertion estimate handles inside sources. Outside sources use the original-profile C/n estimate and h<=n to obtain C/h. Below the constructed target-depth threshold, M<=1 absorbs all fixed initial depths into C. No lower-depth or source-region condition remains. The first compilation found a missing local proof binding; the corrected proof compiled without changing the mathematical statement. Endpoint columns, same-corner matrix envelopes, rooted-cycle expectations and excursion losses remain unresolved.

### Full right-endpoint matrix target bound

Validation: individual compilation passed; the final default build passed (4221 jobs), and the regenerated elaborated-type/axiom audit and validator passed for all 565 source theorems. The complete elaborated new theorem type has only the original profile, fixed r,p with p>0, sampling, labels and target region. Its axiom output is `[propext, Classical.choice.{u}, Quot.sound.{u}]`. All 13 frozen files are unchanged.

`PowerProfile.domination_matrix_right_target` in `Section6DominationMatrixRightTarget.lean` proves that there exist C>0 and 0<delta<1 such that M(i,j)<=C/terminalDepth(j) for every row n, every source i and every target with terminalDepth(j)<=delta*n (expressed by real division). The matrix is the existing concrete insertion maximum. Only the original right-power profile, exact sampling and fixed r,p>=1 enter. No asymptotic-size, lower-depth, source-region or finite-gap premise remains.

Obligation discharge: finite maximum attainment selects the actual insertion norm; `shifted_right_survivor_bounds` proves finite-gap membership from the derived depth buffer. The two concrete corner/outside target theorems handle the source partition. For fixed target depths or small rows, the proved matrix bound M<=1 applies, with their finite cutoffs absorbed in C. The returned cutoff is half the minimum of the two regional cutoffs, proving the manuscript's non-strict target boundary. Left-endpoint matrix maxima, endpoint columns, same-corner matrix envelopes, rooted-cycle expectations and excursion losses remain unresolved; this is not the complete Lemma 6.4.

### Outside-right target estimate

`PowerProfile.right_outside_insertion_target_bound` in `Section6RightOutsideTarget.lean` proves the actual insertion norm bound C/h for sources outside a fixed right block. Inputs are the original right-power profile and exact sampling, fixed deletion/moment orders, and the explicitly regional source/target and finite-gap conditions. The existing profile envelope constructs all constants. The ordinary kernel is bounded by exp(-1)/(d*h); `stretched_exp_le_inverse_depth` bounds the remainder by D/n, and the derived h<=n gives D/n<=D/h. No rate, decay or matrix estimate is assumed. Fixed target depths, natural-gap membership and full source/matrix assembly remain to discharge in the complete lemma.

Individual compilation and default build passed (4220 jobs). The regenerated audit and validator passed for all 564 source theorems, only propext, Classical.choice and Quot.sound; all 13 frozen files unchanged. The new fully elaborated theorem type was inspected. Source scan found no sorry, admit, axiom, unsafe or checking-bypass declarations. Full Lemma 6.4 remains incomplete, including endpoint columns, same-corner matrix packaging, rooted-cycle and excursion estimates.

### All-row weighted bounds and corner target maxima

| Obligation | Proof | Inputs and discharge |
|---|---|---|
| Weighted finite-row bound | `insertionDominationMatrix_weighted_row_le_size` | A weight bound B>=0 and the proved matrix bounds 0<=M<=1 imply sum(v*M)<=n*B. Generic weight premise is discharged in both concrete all-n results. |
| Both weighted matrix rows for every n | `PowerProfile.domination_matrix_right_weighted_row_all_n`, `domination_matrix_left_weighted_row_all_n` | Original profile and exact sampling, fixed r,p>=1 and the manuscript exponent ranges. For n below the previously constructed threshold, depth ratios are <=T=max(N,1), so the finite-row contribution is <=T*T^kappa. No rate or matrix hypothesis is added. |
| Stretched exponential inverse-depth bound | `stretched_exp_le_inverse_depth` | d,nu>0. The proved summability of h*exp(-d*h^nu) constructs a uniform numerator. |
| Exceptional target maxima | `exceptionalEnvelope_target_bound` | Positive b,v,d,nu. Right support h<a and monotonicity compare its decay to the target depth; the left transposed envelope already decays in target depth. |
| Full ordinary plus exceptional target maxima | `endpoint_envelope_target_bound` | Adds the exact global bound x*exp(-d*x)<=exp(-1)/d. No restriction on the ordinary argument x is assumed. |
| Actual same-corner insertion target maxima | `PowerProfile.right_corner_insertion_target_bound`, `left_corner_insertion_target_bound` | Original profile and exact sampling. Choose v=1 internally and apply the numerical bound to the completed actual insertion envelope. Constants are constructed. These are regional helpers with a derived target-depth cutoff and finite gap index, not yet the full source-uniform active-endpoint matrix maximum. |

All eight new statements compiled individually. The integrated default build passed (4219 jobs), and the full audit passed for 563 source theorems with only propext, Classical.choice and Quot.sound. All 13 frozen files were unchanged. Remaining target-maximum assembly must cover outside-corner sources, fixed target depths, natural gap membership, and transfer to the matrix. Active-endpoint columns, same-corner matrix-envelope packaging, rooted cycle estimates and O(1) excursion loss remain unresolved. Lemma 6.4 is incomplete.

### Lemma 6.4 resumed as the current objective

The complete target is the manuscript's `lem:sp-domination`: one deterministic nonnegative matrix with joint cylinder domination for at most r marks, uniform rows, active-corner and interior target maxima and columns, same-corner ordinary/exceptional envelopes, both positive-exponent weighted rows, rooted cycle expectation bounds, and O(1) expected excursion loss from [A,B]. Proving only the matrix rows does not complete this objective.

The cylinder check in audit/section6-lemma64-cylinder-check.log completed successfully (session 31957, exit 0). The earlier memory blocker is resolved for this verification run. The cylinder branch then passed with 3705 jobs, the small-row branch with 3816 jobs, and the default build with 4216 jobs. All 555 source theorem types and transitive axiom sets were audited successfully, with only propext, Classical.choice and Quot.sound; all 13 frozen files are unchanged. The exact cylinder types and all-n row type were inspected. Historical UNVERIFIED and blocked entries below record earlier attempts and are superseded by this validation.

Three checked statements in Section6DominationMatrixSmallRows.lean supply finite-row packaging: each real matrix entry<=1; every subset row sum<=n; and the concrete profile's unweighted row bound for all n, absorbing the previous finite threshold into its constant. The module is imported by the root. In Section6DominationMatrixCylinder, exponent monotonicity on the actual probability space permits one matrix at exponent r to bound every cylinder of k<=r marks; both ENNReal and real versions are proved, including k=0. All six additions are included in the 555-theorem audit. Remaining: active-corner target maxima and columns, same-corner matrix-envelope packaging, all-n weighted packaging, rooted cycle expectations, and O(1) excursion loss. No extra mathematical input was added.

### Historical cylinder verification block (resolved at the 555-theorem milestone)

Blocked audit: the third consecutive goal turn retried the import-only diagnostic. Its live session returned terminal exit 1, and audit/section6-cylinder-import-retry.log contains `INTERNAL PANIC: out of memory`. Verification cannot presently proceed without a host-resource change. The task is marked blocked, not complete. The three cylinder declarations remain unverified; the last successful checkpoint remains 549 audited statements and 4214 build jobs. Once memory is available, retry the import check, compile the cylinder module, rebuild, and run the full expanded audit before claiming the result.

Resume diagnostic: the next direct compilation failed at line 1 while reading Mathlib/MeasureTheory/Function/UniformIntegrable.olean.private; the file was confirmed present (1,319,512 bytes). The log is audit/section6-matrix-cylinder-resume.log. A separate import-only module, audit/Section6CylinderImportCheck.lean, containing the same three imports and only #check commands, also exited 1 without output (audit/section6-cylinder-import-check.log). No live Lean/Lake process remained from the direct attempt, and the import-check handle returned terminal exit 1. This confirms that verification is currently failing before the cylinder proof is tested, following the earlier explicit out-of-memory panics. No successful new compilation or axiom audit is claimed by this diagnostic. The next action is to retry the import check when host resources permit, then compile the actual theorem and run the complete audit.

`Section6DominationMatrixCylinder.lean` now contains a proposed proof that, for any positive finite row, k<=r and injective marked labels u and demanded ranks j,

`exponentialRace w {clocks | MarkedRankCylinder u j clocks} <= product_a ofReal (insertionDominationMatrix w r r (u a) (j a))`.

The module also contains the probability-space Lp exponent comparison used in the proof and a real-valued probability corollary. These THREE NEW STATEMENTS ARE UNVERIFIED. They are imported by the Section 6 root for the next integrated build. The last successful audit still covers 549 statements, not these three additions.

The argument handles k=0 directly. For positive k it applies the already proved Holder cylinder bound at exponent k, proves the sorted deletion set has size at most r, derives the demanded-gap shift directly from sortedMarkedGapIndex_add, compares exponent k to the fixed r on the actual probability space, and applies finite-maximum domination. Equiv.prod_comp removes the sorting permutation. No profile, moment, independence, matrix, or convergence premise is added.

The first compilation identified only a product-reindexing name-resolution error; it was corrected to the explicit Equiv.prod_comp. Subsequent attempts failed with out-of-memory errors, including a PowerShell out-of-memory exit. See audit/section6-matrix-cylinder-compile.log (Lean panic), audit/section6-matrix-cylinder-recompile.log (empty failed attempt), and the session output for the aborted thin-dependency attempt. An attempted reduction of the matrix foundation's imports was reverted; its previously checked source is preserved. The cylinder module retains a smaller dependency list by proving the elementary rank-shift bound locally. No checking settings, toolchain, external dependencies, or frozen definitions were changed. Remaining obligation: compile these three declarations, rebuild the root, regenerate and run the full definition/type/axiom audit, and validate all frozen hashes. The cylinder result is not yet claimed proved.

### Concrete finite-maximum domination matrix

The matrix is now explicitly defined in `Section6DominationMatrix`: `insertionChoices r j` enumerates all deletion sets of size at most r and all natural gap indices within distance r+1 of the demanded rank. Its finite encoding q<n+r+2 is proved automatic, not an additional restriction. `insertionDominationENN` is the finite supremum of the actual p-norms; `insertionDominationMatrix` is its real value. The bound by one proves finiteness, and `ofReal_insertionDominationMatrix` proves the exact representation identity. No model, rate, law or frozen contract definition changes.

| Intermediate fact | Theorem | Inputs and discharge |
|---|---|---|
| Nonempty finite choices | `insertionChoices_nonempty` | Empty deletion set and unshifted demanded gap supply a witness for every target label. |
| Finite nonnegative matrix and faithful real representation | `insertionDominationENN_le_one`, `insertionDominationMatrix_nonneg`, `ofReal_insertionDominationMatrix` | Uses the proved actual insertion norm<=1 for every natural gap, including the infinite final gap. Only positive finite Weights, no profile estimate assumed. |
| Entrywise actual insertion domination | `deleted_kernel_le_insertionDominationMatrix` | Bounded deletion and shift. Proves the finite encoding bound from Nat.dist and j<n; applies finite-sup membership. |
| Attainment and rowwise maximizing choices | `insertionDominationMatrix_attained`, `insertionDominationMatrix_row_selection` | Finite nonempty maximum and ordinary classical choice. The existing actual row theorems permit target-dependent choices, so no multiplicity factor is incurred. |
| Weighted row transfer | `insertionDominationMatrix_weighted_row_transfer` | Generic actual row premise and nonnegative weights/constant. Selects maximizing choices and proves equality of the real matrix sum with the ENNReal norm sum. The following concrete theorems discharge the premise. |
| Concrete matrix unweighted and weighted rows | `PowerProfile.domination_matrix_row_bound`, `domination_matrix_right_weighted_row`, `domination_matrix_left_weighted_row` | Original profile and exact sampling; fixed r and p>=1; weighted exponents in the proved manuscript ranges. Applies the completed global actual row bounds with moment cutoff p and all selector conditions proved internally. All sufficiently large rows; every source and arbitrary target subset. |
| Interior target maximum | `PowerProfile.domination_matrix_interior_target` | Original profile and sampling, fixed interior margin. Attainment reduces to the existing actual C/n target theorem; constructed row threshold proves natural-gap membership after shifts/deletions. |
| Interior column bound | `PowerProfile.domination_matrix_interior_column` | Sums at most n entries bounded by C/n. Same concrete matrix, original profile and sampling, no column assumption. |

All thirteen new statements compiled individually. After a compiled-dependency read failure, separate builds of the matrix definition, rows and interior bounds passed (3699, 3815 and 3713 jobs), followed by the default build (4214 jobs). The expanded audit prints the three definitions and all 549 theorem types and axiom sets; it and the validator passed with only propext, Classical.choice and Quot.sound and all 13 frozen files unchanged. The exact matrix definitions and complete elaborated types of its five concrete row/interior bounds were inspected. Active endpoint target maxima and columns, the joint cylinder application, same-corner matrix bounds, excursions, small-row packaging, joint asymptotics and all main CLTs/contracts remain unresolved. The full domination lemma is not claimed complete.

### Global actual insertion rows

| Intermediate fact | Theorem | Inputs and discharge |
|---|---|---|
| Uniform interior row | `PowerProfile.interior_insertion_uniform_row` | Original profile and exact sampling. Reuses the proved actual C/n target estimate, constructs a deletion/shift margin to prove natural-gap membership, and sums at most n targets. The earlier rate-row bound is not used as a uniform bound on source rates. |
| Uniform left endpoint row for either allowed behavior | `PowerProfile.left_endpoint_uniform_row` | Active case specializes the proved left weighted row at kappa=0. Finite case uses the early O(theta_i) row and the global upper bound derived from the finite left profile. The initial-eighth condition is discharged from the chosen ratio cutoff. |
| Uniform right endpoint row for either allowed behavior | `PowerProfile.right_endpoint_uniform_row` | Active case specializes the proved right weighted row at kappa=0. Finite case uses the proved inactive-right row, translates its natural row threshold, and discharges 16384*h<=n from the chosen cutoff. Includes terminal gaps. |
| Uniform row over every source and target | `PowerProfile.global_uniform_insertion_row` | Constructs left/right blocks, a common interior margin, and a row-size threshold absorbing the one-based rank offset. Splits arbitrary target subsets into left, right, and middle regions. All helper bounds come from the original profile and sampling. |
| Global right weighted row | `PowerProfile.global_right_weighted_insertion_row` | Original right-power profile, exact sampling, 0<=kappa<beta. Right-block sources use the proved full-target weighted row. For other sources, the weight is bounded by sigma^(-kappa), and the new global uniform row applies. No source or target region remains in the conclusion. |
| Global left weighted row | `PowerProfile.global_left_weighted_insertion_row` | Original left-power profile, exact sampling, 0<=kappa<alpha. Left-block targets use the proved endpoint weighted row. Other targets have bounded weight and use the new global uniform row. Every source and target is covered. |

All six new statements compiled individually. After compiled-file read failures in a concurrent branch build, sequential endpoint and interior branch builds passed (3805 and 3711 jobs), followed by the default build (4211 jobs). The regenerated elaborated-type and transitive-axiom audit and validator passed for all 536 source theorems, with only propext, Classical.choice and Quot.sound; all 13 frozen files are unchanged. The complete elaborated types of the three global row theorems were inspected. These are actual insertion-norm row estimates for all sufficiently large rows, uniform over target-dependent bounded deletions and shifts. Constructing one deterministic domination matrix and proving its columns, target maxima, cylinder bounds and excursions still remains; so do joint local asymptotics and all main CLTs/contract checks.

### Right-source rows crossing into the middle and left endpoint

| Intermediate fact | Theorem | Inputs and discharge |
|---|---|---|
| Interior row retaining O(theta_i) | `PowerProfile.interior_insertion_rate_row` | Original profile and sampling, fixed positive interior margin and deletion/moment orders. The concrete interior Lp envelope is bounded by twice its rate prefactor. A constructed row-size threshold proves natural-gap membership after all permitted shifts and deletions; subset cardinality discharges the row sum. |
| O(theta_i) row outside a fixed terminal block | `PowerProfile.outside_terminal_insertion_rate_row` | Original profile and sampling, positive terminal margin. Splits targets at the initial eighth, combines the existing early rate row with the new interior rate row, and proves the one-based rank offset is absorbed by a constructed threshold. No source restriction or global rate bound is imposed. |
| Absorption of the right-source weight | `PowerProfile.right_outside_targets_scaled_row` | Original right-power profile, exact sampling and kappa<=beta. Derives a right source block and theta_i<=K*(a/n)^beta from the profile. The proved reciprocal-power product bound absorbs (n/a)^kappa. The preceding rate row supplies the entire outside-target mass, including active or inactive left endpoints. |
| Exact target-dependent weight outside the right block | `PowerProfile.right_outside_targets_weighted_row` | Adds kappa>=0 as the chosen weight range, and proves (h/a)^kappa<=(n/a)^kappa from h<=n. No new model input. |
| All targets for right-corner sources | `PowerProfile.right_source_full_weighted_insertion_row` | Combines the existing right-endpoint weighted row with the new outside-target weighted row using a target partition. Constructs the source block and common row-size threshold. Only original profile and sampling inputs, 0<=kappa<beta, and fixed deletion/moment orders are used. |

All five new statements compiled individually. The dependency branch passed (3767 jobs), followed by the default build (4207 jobs). The regenerated elaborated-type and transitive-axiom audit passed for all 530 source theorems, with only propext, Classical.choice and Quot.sound; all 13 frozen files are unchanged. The complete elaborated full-target right-source theorem was inspected. This proves the manuscript's absorption argument for right sources. Sources outside the derived right block still require the full unweighted row bound for all targets. The single matrix, column and target-maximum bounds, joint asymptotics and all main CLTs/contracts remain unresolved.

### Right-endpoint contribution for every source (latest implementation)

| Intermediate fact | Theorem | Inputs and discharge |
|---|---|---|
| Outside-right ordinary kernel plus a remainder per target | `right_outside_kernel_subset_bound` in `Section6RightOutsideKernelSum` | Positive beta, d and nu. The ordinary inverse-power row bound and proved summability of n*exp(-d*n^nu) give a uniform bound for arbitrary target subsets. Terminal-depth injectivity and its range discharge the finite-sum conversion. No source-rate upper bound is assumed. |
| Actual outside-right moderate-target weighted row | `PowerProfile.outside_right_weighted_insertion_row` | Original right-power profile and exact sampling. A fixed outside-source region bounds the weight; the existing concrete envelope supplies its constants. The target cutoff and bounded deletion/shift conditions prove natural-gap membership internally. The preceding numerical bound discharges the complete kernel sum. |
| Outside-right row including every terminal target depth | `PowerProfile.outside_right_all_weighted_row` | Splits at the constructed moderate cutoff, uses the proved fixed-target row below it and the moderate theorem above it. Includes the infinite final gap; no new profile input. |
| Right-endpoint weighted insertion row for every source | `PowerProfile.right_endpoint_weighted_insertion_row` | Original profile, exact sampling, 0<=kappa<beta and fixed deletion/moment orders. Splits sources at the derived right-corner cutoff and constructs a common target block and large-row threshold. Every source is covered; all helper geometric conditions are discharged. |

These four statements compiled individually. The dependency branch passed (3750 jobs), followed by the default build (4203 jobs). The regenerated elaborated-type and transitive-axiom audit passed for all 525 source theorems, with only propext, Classical.choice and Quot.sound; all 13 frozen files are unchanged. The complete elaborated endpoint theorem type was inspected. The result bounds the right-endpoint target contribution only. Cross-region weighted contributions, columns, target maxima, the single global matrix, and all main CLTs/contract checks remain unresolved.

Status: in progress. None of the final CLTs is claimed proved. Available main inputs are exactly the sampled profile hypotheses and the defining finite Luce law in `section6-assumption-audit.md`.

| Obligation (source label) | Proof/status | Hypotheses and discharge |
|---|---|---|
| Sampling points in (0,1), valid positive rows, endpoint reflection | `Section6Sampling`: `samplePoint_mem`, `sampledWeights_sampled`, `samplePoint_rev` | Arithmetic on Fin n; profile positivity supplies row positivity. Both grids covered. |
| Relative error vanishes; two-sided endpoint comparisons | `Section6EndpointBounds`: `PowerExpansion.relative_error_tendsto`, `eventually_comparable`, specialized power/critical lemmas | Positive error exponent; coefficient positivity used only for comparison. Derived directly from stated expansion. |
| Global bounds away from active endpoints | `Section6ProfileBounds`, `Section6InactiveBounds`: `PowerProfile.bounds_off_active`, `lower_away_right`, `upper_away_left`, global inactive-endpoint bounds, `PowerExpansion.exists_comparison_neighborhood` | Derived from continuity, positivity, inactive limits and endpoint comparisons. No monotonicity or rate bounds assumed. |
| Exact finite transforms and sharp deletion bounds | `Section6PopulationFinite`: `populationG_eq_one_sub_H`, `populationH_deletion_bound`, `populationG_deletion_bound`, `populationD_one_deletion_bound` | Arbitrary positive finite rates, nonnegative time (strictly positive for weighted bound); no normalization/profile input. Deleted sums retain divisor n. |
| Exact finite-transform derivatives | `Section6PopulationDerivatives`: `populationD_hasDerivAt`, `populationH_hasDerivAt`, `populationG_hasDerivAt` | Finite sums only, all real times; H'=-D1, G'=D1, Dr'=-D(r+1). No asymptotic expression differentiated. |
| Kernel probability normalization, convolution integrability and positive coefficients | Unresolved | gamma>0, q>0; Gamma positivity must be discharged from allowed endpoint exponents. |
| Population transforms, bounded deletions (`sp-populations`) | Proved: [`Section6PopulationEstimates`](../Luce/Section6PopulationEstimates.lean), `PowerProfile.left_deleted_populations_relative_error`, `right_deleted_populations_relative_error`; undeleted statements in the endpoint asymptotic modules | Exact original PowerProfile and SampledRates only. Exact Gamma coefficients, positive error exponents, both grids, original divisor n, and constants uniform in every set of at most r deleted labels. Each endpoint pair has one common constant and exponent. No assumed population estimate. |
| Absolute power error and decaying perturbation envelopes | `Section6KernelPerturbation`: `PowerExpansion.absolute_error_bound`, `kernel_perturbation_bounds` | Literal relative expansion, positive coefficient/error exponent; constant uniform in nonnegative time. The factor exp(-t*c*s^p/2) is retained. |
| Exact power-envelope Gamma integral and integrability | `Section6PowerIntegrals`: `integral_power_exp`, `integrableOn_power_exp`, slow-survival specializations | p nonzero, r positive, (a+1)/p positive; supports both signs of p. Concrete parameters still must discharge these conditions at each use. |
| Both-grid antitone quadrature | `Section6MonotoneQuadrature`: `samplePoint_scaled_cell`, `antitone_sample_average_error` | Only the comparison envelope is antitone, not the sampled profile. Same 1/n divisor on either grid. |
| Both-grid increasing and unimodal quadrature | `Section6UnimodalQuadrature`: `monotone_sample_average_error`, `unimodal_sample_average_error` | Explicit monotonic pieces are proved for comparison kernels. The profile is not assumed monotone. |
| Right weighted-kernel quadrature | `Section6WeightedQuadrature`: `weighted_power_quadrature` | Positive beta,c,t and n>0; error ≤2*exp(-1)/(n*t). Peak location and monotonicity are proved, so there is no time-scale loss. |
| Weighted power integral, integrability, off-endpoint decay | `Section6WeightedIntegrals`: `integral_weighted_power`, `integrableOn_weighted_power`, `rateKernel_bound_above_lower` | Positive beta,c,t; decay only uses the supplied lower rate, whose concrete off-endpoint bound is already proved. No upper rate bound required. |
| Absorption and uniform right-endpoint envelope sums | `Section6EnvelopeAbsorption`, `Section6EnvelopeSums`: `power_envelope_absorption`, `power_envelope_average_bound`; `Section6ScaledEnvelopeSums`: `scaled_power_envelope_average_bound` | Constants constructed independently of n,r and grid. The weighted comparison absorbs its additional linear factor using `linear_exp_absorption`, then applies the beta+eta envelope. Positivity follows from manuscript beta,eta>0 and is discharged in the actual-profile assembly. |
| Exact slow-power prototype population error | `Section6SlowPrototype`: `slow_prototype_population_error` | beta>0, r>0, n>0; exact Gamma leading term, error ≤1/n plus explicit exponentially small tail. Applied in the actual-profile survivor estimate. |
| Actual sampled-profile right-survivor population error | `Section6RightPopulationComparison`: `populationH_eq_reflected_samples`, `PowerProfile.right_survival_difference_bound`, `right_populationH_comparison`; `Section6RightPopulationError`: `PowerProfile.right_populationH_error` | Original PowerProfile and SampledRates only. Near-endpoint, off-endpoint, envelope-sum and prototype premises all discharged internally. Constants are uniform in the choice of grid, n>0 and t>0. |
| Reduction to a positive power saving | `Section6PopulationPowerBounds`: `reciprocal_scaled_rpow`, `exponential_le_power`, `right_perturbation_power_identity`; `Section6RightPopulationAsymptotic`: `PowerProfile.right_populationH_power_error` | Positive coefficient, beta, eta from original profile. Exponential comparison constants constructed by `exists_power_exp_bound`; actual-profile error is ≤K*(t^(-(1+eta)/beta)+1/n), uniformly n>0,t≥1 and either grid. |
| Exact Gamma-normalized right-survivor relative error | [`PowerProfile.right_populationH_relative_error`](../Luce/Section6RightSurvivorRelative.lean) | Original PowerProfile and SampledRates only. A=Gamma(1+1/beta)*c^(-1/beta)>0 proved, then divide the completed additive bound. Saving zeta=eta/beta>0 follows from the profile. Bound holds for all n>0,t≥1, hence throughout the manuscript's joint asymptotic regime. |
| Right-survivor asymptotic after any bounded deletion | [`PowerProfile.right_deletedH_relative_error`](../Luce/Section6RightSurvivorDeletion.lean) | Any fixed r; the theorem quantifies every removal set of cardinality≤r after choosing K. `populationH_deletion_bound` discharges the only new estimate. Original divisor n, exact same leading constant and saving; no label-dependent constant or added profile premise. |
| Weighted power prototype with exact coefficient and decay | `Section6WeightedPrototype`: `scaled_integral_weighted_power`, `weighted_power_tail_bound`, `scaled_weighted_prototype_power_error` | Positive beta,c,eta. The exact Gamma integral and t-weighted quadrature give the manuscript coefficient and O(1/n) scaled error; the half-line tail is bounded by a proved exponential estimate. Actual-profile theorem applies these with the original endpoint parameters. |
| Actual sampled weighted-profile comparison | `Section6WeightedProfileComparison`: `linear_exp_absorption`, `populationD_one_eq_reflected_samples`, `PowerProfile.right_scaled_rate_difference_bound`; `Section6WeightedPopulationComparison`: `PowerProfile.right_scaled_populationD_comparison` | Original PowerProfile and SampledRates only. Near-endpoint kernel comparison, the scaled envelope sum and off-endpoint lower bounds are proved and discharged internally. Left poles are permitted; no global upper rate bound is used. |
| Right weighted-population relative error | [`PowerProfile.right_populationD_relative_error`](../Luce/Section6RightWeightedAsymptotic.lean), via `right_scaled_populationD_power_error` and `time_mul_weighted_power` | Exact coefficient Gamma(1+1/beta)*c^(-1/beta)/beta; relative error K*(t^(-eta/beta)+1/(n*t^(-1/beta))). All n>0,t≥1 and either grid. The scaling by t preserves the O(1/(n*t)) discretization error. |
| Right weighted asymptotic after any bounded deletion | [`PowerProfile.right_deletedD_relative_error`](../Luce/Section6RightWeightedDeletion.lean) | Any fixed r; K chosen before the removal set. `populationD_one_deletion_bound` proves the sharp r/(e*n*t) change. The leading term, time exponent, divisor n, and profile hypotheses are unchanged. |
| Fast-arrival integrability and exact Gamma integral | `Section6FastArrivalBounds`: `one_sub_exp_neg_bounds`, `integrableOn_fast_arrival`, `fast_arrival_boundary_continuous`, `fast_arrival_boundary_tendsto`; `Section6FastArrivalIntegral`: `fast_arrival_boundary_hasDerivAt`, `integral_fast_arrival` | alpha>1 and r>0. Proves integrability by boundedness near zero and the s^(-alpha) tail, proves both boundary terms, then applies FTC to s*(1-exp(-r*s^(-alpha))). Integral is r^(1/alpha)*Gamma(1-1/alpha). No assumed integral identity. |
| Fast-arrival sampling and truncation | `Section6FastArrivalPrototype`: `fast_arrival_quadrature`, `fast_arrival_tail_bound`, `fast_arrival_prototype_error` | Both grids, n>0; alpha>1,r>0 for prototype error ≤1/n+r/(alpha-1). The auxiliary quadrature function takes value 1 at zero; the proof establishes exact equality of sampled values and integrals with the original prototype. No production model definition changes. |
| Fast weighted integral, tail, and sampled prototype | `Section6FastWeightedIntegral`: `integral_fast_weighted_power`, `integrableOn_fast_weighted_power`, `fast_weighted_tail_bound`; `Section6FastWeightedPrototype`: `fast_weighted_quadrature`, `fast_weighted_prototype_error` | alpha>1,c,t>0 (quadrature only alpha>0). Exact coefficient Gamma(1-1/alpha)*c^(1/alpha)/alpha and power t^(1/alpha-1); error ≤2*exp(-1)/(n*t)+c/(alpha-1). Peak, monotonicity pieces and zero value all checked; no profile monotonicity assumed. |
| Smaller integrable fast error exponent | `Section6FastErrorExponent`: `PowerExpansion.mono_error_exponent`, `exists_fast_error_exponent` | From the original relative O(s^eta) expansion and alpha>1,eta>0, construct eta'=min(eta/2,(alpha-1)/2), prove 0<eta'<eta,alpha-1 and the weaker expansion. This is a conclusion, not an added hypothesis. |
| Actual fast-arrival pointwise perturbation | [`PowerProfile.left_arrival_difference_bound`](../Luce/Section6FastProfileComparison.lean) | Original PowerProfile only. Constructs eta',C,M; bound is C*t*s^(-alpha+eta')*exp(-t*(c/2)*s^(-alpha))+t*M. Local kernel comparison and global bound away from the left endpoint are proved from the profile and discharged internally. |
| Uniform fast perturbation-envelope sums | Proved sufficient bound: `Section6FastEnvelopeSums`, `scaled_fast_weighted_average_bound`, `fast_power_envelope_average_bound` | The bound is K*(t^(1/(alpha-eta'))+1/n), via the weighted prototype with exponent alpha-eta'>1. All parameters are discharged from the constructed smaller exponent. The earlier proposed sharper t^((1+eta')/alpha) bound remains unproved and is not claimed; it is unnecessary because the manuscript requires some positive relative saving. The proved saving 1/(alpha-eta')-1/alpha is positive, and the exact leading terms and main contracts are unchanged. |
| Actual left-arrival population asymptotic | `Section6FastPopulationComparison`: `populationG_eq_samples`, `PowerProfile.left_populationG_comparison`; `Section6LeftArrivalAsymptotic`: `left_populationG_power_error`, `left_populationG_relative_error` | Original profile constructs the smaller exponent, constants, and near/off-endpoint bounds. Proven envelope sum and exact fast-arrival prototype discharge the population error. Exact A=Gamma(1-1/alpha)*c^(1/alpha); relative error K*(t^zeta+1/(n*t^(1/alpha))), zeta>0 constructed internally. |
| Actual left-weighted population asymptotic | `Section6FastWeightedComparison`: `PowerProfile.left_scaled_rate_difference_bound`; `Section6FastWeightedPopulationComparison`: `populationD_one_eq_samples`, `left_scaled_populationD_comparison`; `Section6LeftWeightedAsymptotic`: `time_mul_fast_weighted_power`, `left_scaled_populationD_power_error`, `left_populationD_relative_error` | Original profile only. Proved exponential absorption reduces the weighted perturbation to the same envelope sum. Scaling by t preserves the sharp sampling error; exact leading term (A/alpha)*t^(1/alpha-1). No additional moment or upper-rate assumption. |
| Left bounded deletions and common endpoint error bound | `Section6LeftArrivalDeletion`: `PowerProfile.left_deletedG_relative_error`; `Section6LeftWeightedDeletion`: `left_deletedD_relative_error`; `Section6PopulationEstimates`: paired endpoint theorems | Generic r/n and r/(e*n*t) deletion bounds are discharged internally. Take the minimum of the two positive left error exponents and sum of constants; monotonicity of t^zeta for 0<t≤1 proves one common bound. Constants are selected before grid/array/n/removal set/time. |
| Quantiles, weighted moments, deterministic weight floors (`sp-quantiles`) | Partially proved; see detailed rows below | Finite inverse equations, both quantitative time inversions, rate-time products and hazards, both comparable-time moment bounds, exact derivative separation, and both endpoint remaining-rate floors proved. Quantile-scale separation/stability and interior estimates remain unresolved. |
| Exact marked cylinder and repeated-gap simplex (`sp-cylinder-simplex`) | Unresolved; audit existing finite-insertion machinery before use | Product clock law is a constructed representation of finite Luce masses. |
| Insertion moments and extreme restricted sums (`sp-insertion-moments`) | Unresolved | Deterministic floors, Chernoff, marked moment bounds. |
| Global domination, weighted rows, inactive endpoint treatment (`sp-domination`) | Unresolved | Construct matrix and prove its complete bounds; no matrix witness is allowed as a main input. |
| Marked-background concentration and joint insertion (`sp-joint-insertion`) | Unresolved | Includes mixed corners and actual dependence of selected gaps. |
| Integrable-density envelope (`sp-density-envelope`) | Proved: `Section6.lemma66`, checked against `Section6Lemma66Contract.lemma66` | Single-coordinate replacement gives `K_ell = 3^ell` for arbitrary independent clocks with densities. The count is integrable; tie exclusion and all helper hypotheses are discharged. See the [statement and audit](section6-lemma66.md). Concrete applications must still construct their envelope. |
| Root/excursion bounds (`sp-root-excursion`) | Unresolved | Global weighted rows and target bounds; no circular truncation argument. |
| Ideal traces and spatial root normalization (`sp-ideal-traces`) | Unresolved | Literal convolution constants, logarithmic-grid errors, and extremum root factor. |
| Overlap/nearby-rank sums (`sp-collisions`) | Unresolved | Matrix hypotheses discharged by constructed domination/ideal kernels. |
| All mixed core factorial moments (`sp-factorial-moments`) | Unresolved | Actual disjoint cycles, mixed-corner insertion, collision and excursion errors. |
| Gaussian method of moments and removal of core | Unresolved | Derive required moment inputs; no additional convergence hypothesis in main theorem. |
| Power-law total/spatial CLTs, means, localization | Closed targets in `Section6Contract`; proofs unresolved | Exact original power-profile and finite-law inputs only. |
| Adapted Bernoulli martingale CLT (`sp-fixed-martingale`) | Unresolved | Its generic variance/compensator hypotheses must be proved in the critical application. |
| Critical transforms, quantile hazards, compensator and cutoffs | Unresolved | Critical sampled expansion; no shell or finite-mean assumptions. |
| Critical longer-cycle uniform mean bounds | Unresolved | Density envelope, deletion bound, common background event, logarithmic-bin trace. |
| Critical full theorem | Closed target in `Section6Contract`; proof unresolved | Exact critical profile and finite law. |
| Interior-grid extension (`sp-interior-grid`) | Sampling foundations proved; analytic extension unresolved | Verify sum/integral and endpoint errors for this grid; no assumed coupling. |
| Sukhatme specialization | Unresolved | f(x)=1-x, interior grid, proved finite-law scaling; no separate CLT hypothesis. |
| Separate closed contract checks and full axiom audit | Unresolved | Add only completed proofs, with no extra parameters or sorry dependencies. |

### Quantile lemma progress

| Intermediate fact | Proved theorem | Inputs and discharge |
|---|---|---|
| Finite transforms are strictly ordered, with the required endpoint limits | `Section6FinitePopulationOrder`: `populationH_strictAnti`, `populationG_strictMono`, `populationH_tendsto_zero`, `populationG_tendsto_one` | Positive finite row, n>0 where needed; positivity is an existing `Weights` field. No profile assumption. |
| Positive unique finite quantile times for 0<m<n, satisfying the actual H/G equations | `Section6FiniteQuantiles`: `leftQuantileTime_spec`, `rightQuantileTime_spec`, inverse and order identities | Defined by `Function.invFunOn` on nonnegative time for the original finite G. Right quantiles use the proved identity H=1-G. The total definitions outside valid ranges impose no model restriction. |
| Second population moment bound and finite-time D variation | `Section6PopulationMomentBounds`: `populationD_two_le`, `populationD_one_difference_bound` | Only positive finite rates and positive times; proved from exponential kernels, exact derivatives, and monotonicity. Quantile-scale comparability still unresolved. |
| Global right pointwise rate floor on either sampling grid | `PowerProfile.right_global_lower_power`, `right_sampled_rate_floor` | Original PowerProfile and exact SampledRates. Endpoint comparability and the bound away from the endpoint are discharged by completed profile lemmas. Existing terminal-depth arithmetic is reused without any shell assumption. |
| Any h surviving labels have total rate at least C*n^(-beta)*h^(beta+1), manuscript eq:sp-right-weight-floor | `PowerProfile.right_remaining_rate_floor` in `Section6RightRemainingRate` | Original PowerProfile and SampledRates only. Distinct positive depths dominate the first h integers by `positive_natset_order_bound`; `positive_natset_rpow_sum_ge` proves the integral lower bound. All helper premises are discharged internally. Applies to every surviving set, including after bounded deletions. |
| Left initial-block rate lower bound | `PowerProfile.left_initial_block_rate_floor` in `Section6LeftRateFloor` | Original PowerProfile and exact sampling on either grid. The neighborhood is constructed from the original expansion. Monotonicity is used only for the power comparison, not for f. |
| Left remaining-rate floor, manuscript eq:sp-left-weight-floor | `PowerProfile.left_remaining_rate_floor`, `left_remaining_rate_floor_bounded_displacement` in `Section6LeftRemainingRate` | At most 2h actual removals leave at least h of the first 3h labels; `initial_block_remaining_rate` proves this counting and sum argument. Constants are constructed from the original profile. The bounded-budget form applies when removed.card <= h+r, 4r+4 <= h, and h <= eps*n/4. Later applications must discharge the actual removal count, including both deletion and rank displacement; no assumed floor is introduced. |
| Exact finite mean separation | `populationG_separation`, `populationH_separation` in `Section6PopulationSeparation` | Positive finite row, n>0, s<=t. The increment lies between (t-s)*D1(t) and (t-s)*D1(s), by exact derivatives and antitonicity. No asymptotic remainder is differentiated. |
| Power inversion with linear relative error | `rpow_relative_lipschitz`, `inverse_power_relative_error` in `Section6PowerInverseBounds`; `negative_power_inverse_estimate` | The derivative of a real power is bounded explicitly on [1/2,2]. The pointwise inverse helper has error and smallness premises; those premises are all discharged by the uniform quantile helper below, not exposed as new model assumptions. |
| Uniform antitone population inversion | `antitone_negative_power_quantile_error` in `Section6AntitoneQuantileError` | Given the proved population error, constructs C,delta,M. Evaluating the error at a fixed large time S and using monotonicity proves that the unknown quantile exceeds S. The cutoff N*x>=M proves the discretization smallness. The normalized power is then proved to lie in [1/2,2]. No smallness or tightness of the quantile is assumed. |
| Right quantile time, first part of eq:sp-right-quantile | `PowerProfile.right_quantile_relative_error` in `Section6RightQuantileAsymptotic` | Exact PowerProfile and SampledRates only, both grids. For m>=M, 0<m<n, m/n<delta, the error relative to (Aminus*n/m)^beta is <=C*((m/n)^eta+1/m). The existing population error, finite strict antitonicity, and inverse equation discharge every analytic helper premise. Exact Gamma coefficient; no normalization or shell condition. |
| Positive-power inversion and uniform arrival inversion | `positive_power_relative_error`, `positive_power_inverse_estimate`, `monotone_positive_power_quantile_error` | The derivative bound on [1/2,2] gives linear inverse stability. A fixed positive S<=1 is constructed with K*S^q<=A/4. The actual population error and monotonicity prove t<S; N*x>=M supplies the discretization smallness. Both helper smallness premises are discharged. |
| Left quantile time, first part of eq:sp-left-quantile | `PowerProfile.left_quantile_relative_error` in `Section6LeftQuantileAsymptotic` | Exact PowerProfile and SampledRates only, both grids. The proved population estimate constructs eta' and q=1/(alpha-eta')-1/alpha>0, then xi=q/(1/alpha)>0. For m>=M, 0<m<n, m/n<delta the error relative to (m/(Aplus*n))^alpha is <=C*((m/n)^xi+1/m). Exact Gamma coefficient; no assumed small-time estimate or additional model hypothesis. |
| Sampling-shift relative errors | `samplePoint_ratio_interval`, `samplePoint_relative_error`, `samplePoint_power_relative_error` in `Section6SamplingRelativeError` | On either grid, the sample/nominal-location ratio lies in [1/2,1] and differs from one by at most 1/m. The proved power Lipschitz bound gives the same order for any fixed real power. Real division and the interior n+1 denominator are handled exactly. |
| Actual sampled relative endpoint rates | `PowerExpansion.sampled_relative_error`; `PowerProfile.left_sampled_rate_relative_error`, `right_sampled_rate_relative_error` | The original big-O expansion supplies its constant and neighborhood. Combine it with the proved sampling error; right reflection uses the existing one-based terminal depth. Error <=C*((m/n)^eta+1/m), uniformly both grids, with no profile monotonicity or assumed sampling estimate. |
| Both rate-time products, second parts of eq:sp-right-quantile and eq:sp-left-quantile | `PowerProfile.right_rate_time_relative_error`, `left_rate_time_relative_error` in `Section6RightRateTime` and `Section6LeftRateTime` | Combine the proved sampled rates and quantile times using `relative_product_error`; its error bound e<=2 follows from m>=1 and 0<m/n<1. `right_rate_time_leading` and `left_rate_time_leading` prove cancellation of c, leaving exactly Gamma(1+1/beta)^beta and Gamma(1-1/alpha)^(-alpha). The left common saving is min(original eta, proved time saving)>0. No extra model assumptions. |
| Joint-error cutoffs | `joint_power_error_small` in `Section6JointErrorCutoffs` | Constructs delta,M from positive a,C,eps, so C*(x^a+1/m)<=eps whenever 0<x<delta and M<=m. No relation or prescribed convergence rate between x and m is assumed. Applied to the proved quantile errors with eps=1/2. |
| Actual quantile time comparability | `PowerProfile.right_quantile_comparable`, `left_quantile_comparable`, via `half_relative_error_bounds` in `Section6QuantileComparability` | Original profile and sampling only. Cutoffs are chosen by the previous lemma and combined with those of the quantitative time theorem. Both actual times lie between T/2 and 2T, where T is their exact manuscript leading approximation. |
| Division of relative errors | `relative_quotient_error` in `Section6JointErrorCutoffs` | Generic helper: from errors <=a*e and <=b*e with b*e<=1/2 derives quotient error <=2*(a+b)*e, proving denominator positivity internally. Both hazard applications discharge the denominator error using the concrete quantile weighted-population theorem and its smallness using `joint_power_error_small`. |
| Quantile control for a second population | `antitone_quantile_smallness` in `Section6AntitoneQuantileControl` | The original survivor error and antitonicity construct delta,M giving t>=1, K*t^(-q)<=A/4, and K/N<=x/4 at the exact quantile. No large-time or smallness premise is assumed for the concrete model. |
| Comparing two power-scale populations | `negative_power_weighted_comparison` in `Section6NegativePowerComparison` | From the two additive population errors and the proved control bounds, obtains the normalized second-population error C*(x^(q/p)+1/(N*x)). Its premises are discharged by the original H and scaled-D estimates in the next row. |
| Right weighted population at the actual quantile | `PowerProfile.right_quantile_scaledD_relative_error` in `Section6RightQuantileWeighted` | Original profile and sampling only. Proves abs(beta*t*D1(t)/(m/n)-1)<=C*((m/n)^eta+1/m), with t the actual survivor inverse. Uses the exact H(t)=m/n identity, exact Gamma coefficients, and both proved population errors. |
| Right hazard, third part of eq:sp-right-quantile | `PowerProfile.right_quantile_hazard_relative_error` in `Section6RightHazard` | Combines the proved right rate-time and weighted-population estimates; constructs cutoffs making the denominator error <=1/2. Exact leading term beta*Gamma(1+1/beta)^beta/m, relative error C*((m/n)^eta+1/m), both grids, no additional model inputs. |
| Left common additive population saving | `PowerProfile.left_populations_power_error` in `Section6LeftCommonPopulationError` | Both positive savings are derived from the original expansion. Their minimum is positive; for 0<t<=1 the two original power remainders are proved bounded by the common one. A sum of the two constructed constants works for both G and t*D. |
| Left quantile control and population comparison | `monotone_quantile_smallness`, `positive_power_weighted_comparison` | The first derives t<=1 and both smallness inequalities from G's estimate and monotonicity. The second compares the two positive-power populations. Their complete hypotheses are discharged by the common additive population theorem and the exact inverse equation. |
| Left weighted population at the actual quantile | `PowerProfile.left_quantile_scaledD_relative_error` in `Section6LeftQuantileWeighted` | Proves abs(alpha*t*D1(t)/(m/n)-1)<=C*((m/n)^xi+1/m), with constructed xi>0 and actual arrival inverse t. Original profile and sampling only, both grids. |
| Left hazard, third part of eq:sp-left-quantile | `PowerProfile.left_quantile_hazard_relative_error` in `Section6LeftHazard` | Combines the proved left rate-time and weighted-population errors using a constructed common positive saving. Cutoffs prove denominator error <=1/2. Exact leading term alpha*Gamma(1-1/alpha)^(-alpha)/m; no additional mathematical inputs. |
| Two-sided weighted-population bounds in the asymptotic regions | `PowerProfile.right_populationD_comparable`, `left_populationD_comparable` in `Section6WeightedPopulationRegions` | Construct time and effective-population cutoffs from the original relative estimates, so D1(s) is between half and twice its exact power leading term. The right region has s>=S and n*s^(-1/beta)>=M; the left has 0<s<=S and n*s^(1/alpha)>=M. These are intermediate evaluation regions, not added profile assumptions. |
| Right multiplicative-window region inclusion | `negative_power_window_region` in `Section6PowerWindowRegions` | Derives S<=s and effective-population size >=M for every T/16<=s<=16T, T=(A/(m/n))^(1/p), using constructed cutoffs on m and m/n. All constants depend only on the supplied positive power-model constants. |
| Actual weighted-population bounds over the enlarged right quantile window | `PowerProfile.right_populationD_quantile_window` in `Section6RightPopulationWindow` | Original profile and sampling only. For t/8<=s<=8t, D1(s) is between half and twice its exact power leading term. The region conditions in the first row are discharged using the proved right quantile comparability and the preceding region-inclusion theorem. The enlargement covers s/2 for every t/4<=s<=4t. |
| Left multiplicative-window inclusion and actual population bounds | `positive_power_window_region`; `PowerProfile.left_populationD_quantile_window` in `Section6LeftPopulationWindow` | Construct joint cutoffs that place T/16<=s<=16T in the left population region. Actual time comparability discharges these conditions for t/8<=s<=8t, uniformly on both grids. |
| Conversion from power windows to normalized moments | `power_window_comparison`, `population_moments_from_power_window` | The first compares D1(s) with D1(t) using explicit factors depending only on the power. The second uses the proved center normalization and D2(s)<=2*D1(s/2)/(e*s). Its window and center premises are fully discharged by both concrete theorems below. |
| Both endpoint comparable-time moments, eq:sp-rough-moments | `PowerProfile.right_quantile_moments`, `left_quantile_moments` in `Section6QuantileMoments` | Original profile and sampling only. Construct a,C,delta,M>0 such that every t/4<=s<=4t satisfies a*m/t<=n*D1(s)<=C*m/t and n*D2(s)<=C*m/t^2. Here t is the actual respective quantile, with m large and m/n small. No moment, window, or denominator estimate is a model input. |
| Quantile-scale mean separation | `PowerProfile.right_quantile_mean_separation`, `left_quantile_mean_separation` in `Section6QuantileMeanSeparation` | Original profile and sampling only, both grids. The concrete moment lower bound discharges the generic window premise. Exact finite separation proves both changes >=a*u*m for 0<u<=1/2. |
| Relative D stability | `populationD_relative_variation`, `populationD_relative_stability`, `PowerProfile.right_quantile_weight_stability`, `left_quantile_weight_stability` in `Section6QuantileWeightStability` | Exact finite derivative and the second-moment upper bound give a Lipschitz estimate for n*D1. Divide by the positive center lower bound to obtain abs(D1((1±u)t)/D1(t)-1)<=K*u. Both concrete endpoint theorems discharge every helper moment premise from `quantile_moments`, using only original profile and sampling, on both grids. |
| Interior sampled rate bounds | `interior_sampled_rate_bounds` in `Section6InteriorRemainingRate` | Continuity and positivity on (0,1) only. Both grids and reflected-label bounds place samples in [eps/2,1-eps/2]; the existing compact extreme-value theorem supplies positive lower and finite upper bounds. |
| Interior remaining rate, including bounded deletions | `PowerProfile.interior_remaining_rate`, `interior_remaining_rate_deleted` in `Section6InteriorRemainingRate` | Original profile and sampling only. Any remaining set of cardinality >=eps*n has total rate >=C*n, using the proved power floor for an active right endpoint or derived global lower bound for an inactive right endpoint. At most r deletions leave >=eps*n/2 labels when 2*r<=eps*n; this counting premise is proved. |
| Uniform small-time arrival control | `two_power_small_time`, `populationG_le_uniform_rate`, `PowerProfile.small_time_populationG` in `Section6SmallTimePopulation` | Original profile and sampling. The active left case uses the proved additive population error and constructs fixed small time plus row cutoff. The inactive case uses the derived global rate upper bound and exact 1-exp(-t*rate)<=t*rate. |
| Uniform large-time survivor control | `populationH_le_uniform_rate`, `PowerProfile.large_time_populationH` in `Section6LargeTimePopulation` | Original profile and sampling. The active right case evaluates the proved power error at reciprocal small time. The inactive case derives a global positive rate lower bound and applies proved exponential decay. All cutoff and positivity premises are discharged. |
| Uniform interior quantile times | `PowerProfile.interior_quantile_time_lower`, `interior_quantile_time_bounds` | Original profile and sampling only. For any eps>0, construct s>0,T>=1,N>0, uniformly both grids, such that every inverse at x in [eps,1-eps] lies in [s,T] for n>=N. Exact inverse equations and monotonicity discharge the finite inverse comparisons. |
| Counted interior block and compact-time D estimates | `exists_interior_label_block`, `populationD_one_upper`, `interior_populationD_bounds` in `Section6InteriorPopulationD` | A concrete shifted block has at least n/8 labels and both normalized depths >=1/4 for n>=8. Continuity/positivity yields d<=rate<=M there, hence D>=d*exp(-T*M)/8 on [s,T]. The exact single-kernel supremum gives D<=exp(-1)/s. No endpoint assumption is used. |
| Deleted compact-time D estimates | `interior_deletedD_bounds` in `Section6InteriorDeletedD` | From the proved bound and r/(e*n*t) deletion error, construct a row cutoff ensuring error<=a/2. No assumed deleted-population estimate. |
| Interior quantile D comparability | `PowerProfile.interior_quantile_deletedD_bounds`, `interior_quantile_populationD_bounds` | Original profile/sampling only. Proven compact-time bounds are evaluated at both original quantiles. Includes arbitrary sets of at most r deleted clocks evaluated at those times, and literal a*n<=n*D<=C*n for the original population. Bounds on inverses of deleted transforms, if needed by later insertion applications, still require a separate proof. |

This ledger retains the full objective. Partial compilation of definitions or helper proofs does not establish a CLT.

Both endpoint comparable-time moment estimates are now proved. The survivor/arrival mean separation >=c*u*m between t and (1±u)t is now proved. Relative stability abs(D1((1±u)t)/D1(t)-1)<=C*u for 0<u<=1/2 is also proved at both endpoints. Available inputs are the completed moment estimates, exact derivative identities and finite separation/variation bounds, and the original profile/sampling hypotheses. The compact-interior sampled-rate and remaining-rate estimates are proved, including bounded deletions. Compact-interior quantile times are now bounded above and away from zero. Interior D comparability and deleted-D estimates at the original quantile times are proved. Deleted-transform inverse applications, when needed later, still require proof. No source inconsistency is claimed.

Reuse audit: the pinned `Mathlib/Probability/CentralLimitTheorem.lean` main theorem is an iid scalar CLT. Its independence/identical-distribution inputs do not apply to the cycle counts or adapted Bernoulli increments here. It must not be used by assuming those properties; the Gaussian and martingale limit arguments still require proofs appropriate to their actual dependence.

| Ordered insertion step | Checked result | Hypotheses and remaining obligations |
|---|---|---|
| Exact rank/count/order equivalence | `markedRankCylinder_iff_ordered_counts` in `Section6OrderedInsertion` | Distinct sources, sorted targets, and exact arithmetic q+a=j. No gap separation; repeated gaps retain strict marked-time ordering. |
| Exact conditional probability | `markedRankCylinder_probability_eq_orderedInsertion`, `markedRankCylinder_probability_eq_sortedOrderedInsertion` | Finite positive Weights only. Arbitrary distinct target ranks are sorted by a proved permutation identity. Resampling preserves the original law; Tonelli integrates the literal ordered insertion event. No normalization, profile, shell, or moment hypothesis. |
| Product-box bound | `orderedInsertionKernel_le_product` | Distinct sources only. Dropping the marked time-order condition gives containment in the product of actual count gaps, whose mass factors by the existing proved independent exponential law. |
| Measurability and finite real expectation | `measurableSet_orderedMarkedInsertion`, `measurable_orderedInsertionKernel`, `orderedInsertionKernel_le_one`, `orderedInsertionKernel_integrable`, `markedRankCylinder_real_probability_eq_sortedOrderedInsertion` | All established from the literal event and probability law. No integrability premise is added. |
| Per-gap simplex factorization and density representation | Unresolved | Must identify the ordered conditional region mass with the product of ordered simplex density integrals for repeated gaps, including terminal gaps. The exact conditional identity is progress toward eq:sp-cylinder-simplex, not a claim that this factorization is proved. |

| Gap partition step | Checked result | Hypotheses and remaining obligations |
|---|---|---|
| Nondecreasing demanded gaps | `sortedMarkedGapIndex_monotone` | Injective targets only. A finite interval-cardinality comparison proves sorted j_b-b is nondecreasing, without a separation condition. |
| Exact decomposition by occupied gap | `orderedMarkedInsertion_iff_gap_partition` | Monotone gap indices; the concrete sorted indices satisfy this by the preceding theorem. Before-count monotonicity forces time order across distinct gaps, while each gap event retains strict order inside its own block. |
| Disjoint coordinate support | `gap_label_blocks_disjoint`, `gapOrderedInsertion_congr_on_block` | Distinct marked labels imply disjoint label blocks for distinct gaps. Each event is proved unchanged by altering fresh clocks outside its block. |
| Measurable per-gap masses | `measurableSet_gapOrderedInsertion`, `measurable_gapOrderedKernel`, `gapOrderedKernel_le_one` | Literal existing count-gap membership and within-gap order; the conditional mass is taken under the original independent exponential law and is <=1. |
| Equality for distinct gaps | `orderedInsertionKernel_eq_product_of_strictMono` | Under strict gap ordering, the global ordering restriction follows automatically from gap membership, so its conditional mass equals the product of actual single-gap kernels. This stronger special case is not used to assume rank separation in the arbitrary-rank theorem. |
| Full repeated-gap probability product | `orderedInsertionKernel_eq_gap_product`, `markedRankCylinder_probability_eq_gap_product`, `markedRankCylinder_real_probability_eq_gap_product` in `Section6GapProbabilityProduct` | Proved for arbitrary distinct labels and target ranks. Existing sorted-gap monotonicity and disjoint-support results discharge the generic independence premises. Each factor retains within-gap ordering. |
| Simplex density bridge on count-gap regions | `gapOrderedKernel_eq_simplex_lintegral`, `gapSimplexIntegral_integrable_and_eq`, `markedRankCylinder_real_probability_eq_simplex_product` | Proved using exactly the assigned marked coordinates and the literal product of exponential densities. Single-region and outer-product integrability are established. The count-gap region includes terminal gaps. |
| Open order-statistic interval representation | `mem_openOrderGap_iff_count`, `gapSimplexRegion_ae_eq_open`, `gapSimplexIntegral_eq_open`, `markedRankCylinder_real_probability_eq_openSimplex_product` | Proved. The terminal gap is (T_N,infinity), with T_0=0 for an empty background. Product Lebesgue coordinate hyperplanes are null, proving equality of the simplex sets almost everywhere. Tied/nonnegative-background conditions are discharged almost surely by the existing exponential law. |

| Independence step | Checked result | Hypotheses and discharge |
|---|---|---|
| Disjoint-support event independence | `exponentialRace_inter_of_disjoint_support` in `Section6DisjointClockEvents` | Uses the original exponential-coordinate independence. Restriction/extension maps identify arbitrary measurable supported events with measurable events of disjoint coordinate tuples. |
| Finite-family factorization | `exponentialRace_biInter_disjoint_support` | Induction over a finite family. A new block is disjoint from the union of previous blocks; the intersection event depends only on that union. No Countable restriction on the index type is imposed. |
| Actual gap-factor application | `orderedInsertionKernel_eq_gap_product` | All support, measurability, and disjointness premises are discharged by previously proved concrete gap-region lemmas. Monotone q is derived from injective sorted ranks in both concrete cylinder theorems. No rank separation, profile, shell, normalization, or assumed independence is a concrete input. |
| Finite real product expectation | `gapOrderedProduct_integrable`, `markedRankCylinder_real_probability_eq_gap_product` | Product masses are measurable and <=1; integrability and all ENNReal conversions are proved. |

| Density representation step | Checked result | Hypotheses and discharge |
|---|---|---|
| Exactly the marked coordinates in one gap | `measurableSet_gapSimplexRegion`, `gapSimplexRegion_preimage`, `gapCoordinates_map_law`, `gapOrderedKernel_eq_simplex_measure` in `Section6GapCoordinates` | GapCoordinates contains precisely positions with q(a)=k. Injective marked labels imply the induced coordinates have the original product exponential marginal; all unused coordinates are integrated out by a proved map identity. |
| Finite product density | `exponentialPDF_toReal_integrable`, `finite_exponential_product_density` in `Section6ExponentialProductDensity` | Pointwise-positive rates only. Prove integrability of each real density from its unit mass, then use the finite-product integral theorem and rectangle measure uniqueness. No density or integrability axiom is assumed. |
| Literal real simplex integrals and cylinder expectation | `gapSimplexIntegral_integrable_and_eq`, `markedRankCylinder_real_probability_eq_simplex_product`, `gapSimplexProduct_integrable` in `Section6SimplexDensityIntegral` | Probability bounds establish finite nonnegative integrals. Positivity of insertion times on the count-gap region identifies the literal product of rates times exponential factors; all real conversions are justified. The concrete cylinder theorem still has only positive finite rates and distinct labels/targets. |

| Open-simplex bridge | Checked result | Hypotheses and discharge |
|---|---|---|
| All order-statistic gaps | `clockBeforeCount_le_size`, `mem_terminalGap_iff_count`, `mem_openOrderGap_iff_count`, `measurableSet_openOrderGap` in `Section6OpenGaps` | The nonterminal case uses the audited existing order/count identity. The terminal case uses the final order statistic and the exact cardinal bound. Empty backgrounds and out-of-range gap indices are handled explicitly. |
| Null boundary sets | `gapSimplexRegion_ae_eq_open` in `Section6OpenSimplex` | Every marked coordinate avoids each old clock value almost everywhere under product Lebesgue measure. This is proved with coordinate null-singleton measures, not assumed. |
| Open-simplex density integrability and equality | `gapSimplexIntegral_eq_open`, `openGapSimplexIntegral_integrable` | Equality of restricted measures on almost-everywhere equal regions transfers the literal real product-density integral and integrability. |
| Concrete open-simplex cylinder identity | `gapSimplexIntegral_ae_eq_openValue`, `markedRankCylinder_real_probability_eq_openSimplex_product`, `openGapSimplexProduct_integrable` in `Section6OpenSimplexCylinder` | Arbitrary positive finite rates and distinct marked labels/targets only. Original exponential-race injectivity and nonnegative clocks discharge all background restrictions almost surely. No rank separation or additional model hypothesis. |

| One-spacing moment step | Checked result | Hypotheses and remaining discharge |
|---|---|---|
| Exact gap survival factor | `exponentialGapMass_factor`, `exponentialGapMass_le_survival` in `Section6OneGapMoment` | Algebra and exp(y)>=1+y retain the complete starting-time factor; no weakened logarithmic time or lost exponential decay. |
| Pointwise normalized-spacing moments | `exponentialGapMass_normalized_pow_le` | Positive marked rate a, remaining rate W, and nonnegative normalized spacing. The pth power is bounded by ((a/W)*survival(s,a))^p*xi^p, with nonnegativity proved. |
| Integrated factorial estimate | `unit_exponential_nonneg_ae`, `one_gap_moment_bound` | For fixed a>0,W>0,s and natural p, prove integrability and integral X^p<=p!*(a/W)^p*exp(-p*a*s). The normalized spacing nonnegativity and factorial moment are proved for the actual unit exponential law. |
| Starting-time reconstruction | `sum_previous_gaps`, `gapStartFromNormalized_eq_previous` in `Section6GapStart` | Exact telescoping for every finite real clock vector, including the initial zero sentinel. The start equals the sum of preceding normalized spacings divided by their actual remaining rates. No order or probabilistic assumption is needed for this identity. |
| Dependence on preceding coordinates | `gapStartFromNormalized_congr`, `continuous_gapStartFromNormalized`, `gapStartFromNormalized_nonneg`, `gapStartFromNormalized_update` | Continuity and invariance under changing the current coordinate are proved. Nonnegativity follows from nonnegative preceding coordinates and the existing strict positivity of actual remaining rates. |
| Current-coordinate integral | `gap_moment_coordinate_slice` | The literal coordinate-update integral has the factorial bound with the complete random-start exponential factor retained. Positivity of the remaining rate is discharged from the original positive finite weights; no lower-rate hypothesis is added. |
| Joint spacing independence | `gapStart_indep_current` in `Section6GapStartIndependence` | Proved from coordinate independence under the actual standardGapLaw. The preceding sum excludes the current index; all scale factors are the actual positive remaining rates. No independence premise is assumed. |
| Survival-weighted factorial factorization | `gapStart_survival_moment_factorization` | The exponential of the reconstructed start and the current spacing power are measurable functions of independent variables. The actual current-coordinate marginal gives the factorial moment. |
| Full product-law moment bound | `standardGapLaw_nonneg_ae`, `joint_gap_moment_bound` in `Section6JointGapMoment` | Proves integrability and E[X^p] <= p!*(a/W)^p*E[exp(-p*a*S)] for each fixed chain under its joint normalized-spacing law. Almost-sure nonnegativity is derived from the actual exponential marginals. Domination by a current-coordinate moment proves integrability, and the proved factorization retains the complete starting-time decay. |
| Order-event race transfer | `ordered_gap_integral_transfer`, `ordered_gap_integrable_transfer`, `ordered_race_gap_moment_bound` in `Section6OrderedGapTransfer` | Proved from the existing restricted-race map identity. Generic measurability/integrability premises are discharged for the actual gap mass and survival function. Both sides of the concrete bound use the original clock variables and exponential-race measure restricted to the specified order event. |
| Deleted-clock probability-space transfer | `deleted_order_gap_moment_bound` in `Section6DeletedOrderMoment` | Compaction is measure preserving; restricting to the inverse image of an order event preserves the exact restricted law. The original marked rate is positive by Weights. No deleted-law or rate-floor premise is assumed. |
| Literal insertion-kernel moment bound on each order event | `deleted_order_kernel_moment_bound` | The existing deletedGapKernel is identified with the actual gap mass almost surely on the order event. Original background injectivity and nonnegativity are proved almost surely and restriction preserves them. The theorem proves integrability and the full survival-weighted factorial bound, with actual remaining rate for each chain. |
| Summation over orders | `deleted_order_integral_partition`, `deleted_kernel_power_integrable`, `deleted_kernel_moment_sum_bound` in `Section6OrderPartition` | Finite fibers of the actual compacted raceDraw partition the space. They equal strict-order events almost surely by injectivity. Kernel powers are integrable because their probability masses lie in [0,1]. No additional integrability assumption survives in the concrete kernel bound. |
| Survival expectation and generic floor reduction | `deleted_survival_integrable`, `deleted_survival_integral_partition`, `deleted_kernel_moment_bound_of_rate_floor` in `Section6DeletedSurvival` | The actual start is nonnegative almost surely, so its survival factor is integrable. The sum equals its full expectation. The generic helper explicitly requires a positive floor for all orders; this premise is discharged in the two concrete endpoint theorems below, not treated as a model assumption. |
| Concrete left endpoint floor discharge | `PowerProfile.left_deleted_insertion_moment` in `Section6LeftInsertionMoment` | Uses original PowerProfile and SampledRates only, plus finite deletion/rank-range conditions. The audited deletedPrefixLabels cardinal bound and exact remaining-rate identity discharge the helper's floor using left_remaining_rate_floor_bounded_displacement. The denominator is C*n^alpha*q^(1-alpha), with the actual marked rate in the numerator. |
| Concrete right endpoint floor discharge | `PowerProfile.right_deleted_insertion_moment` in `Section6RightInsertionMoment` | Uses original PowerProfile and SampledRates only. A positive surviving-label count h with h+removed.card+q<=n gives the deterministic floor C*n^(-beta)*h^(beta+1), uniformly over orders, from right_remaining_rate_floor. No floor is added as an input. |
| Sampled marked-rate upper envelopes | `relative_error_power_upper`, `PowerProfile.left_sampled_rate_upper`, `PowerProfile.right_sampled_rate_upper` in `Section6SampledRateUpper` | The already proved relative expansion bounds imply a uniform power envelope when normalized depth is below delta<1. Reciprocal-depth errors are bounded even at the first label. No additional cutoff or profile hypothesis is assumed. |
| Exact scale cancellation | `left_insertion_ratio_identity`, `right_insertion_ratio_identity` in `Section6InsertionRatio` | Real-power algebra cancels n and identifies the ratio of the marked-rate envelope to the remaining-rate floor with a constant times x_sigma(a,h)/h. Positivity premises are discharged from n, label depth, gap depth, and proved positive profile constants. |
| Insertion reduction in actual gap coordinates | `PowerProfile.left_insertion_moment_reduction`, `PowerProfile.right_insertion_moment_reduction` | Concrete sampled-profile theorems retain the original deletedGapKernel and full survival expectation. The left factor is ((q/(i.val+1))^alpha/q)^p; the right is ((terminalDepth(i)/h)^beta/h)^p for h+removed.card+q<=n. All rate upper/lower bounds are proved from the original profile and exact sampling. The moment constant is explicitly p!*B^p. |
| Demanded-rank offset arithmetic | `shifted_left_gap_bounds`, `shifted_left_nonterminal`, `shifted_right_survivor_bounds`, `sorted_demanded_gap_shift` in `Section6GapOffsets` | Full left rank h has undeleted gap h-1; full right depth h has gap n-h. Distances at most r+1 give factor-two depth comparisons for h>=8r+8, after at most r deletions. Terminal gaps are excluded in the stated endpoint ranges. The actual sortedMarkedGapIndex satisfies the displacement condition by its proved indexing identity. |
| Fixed-factor depth comparison | `power_over_depth_comparison`, `left_depth_factor_comparison`, `right_depth_factor_comparison` in `Section6DepthComparison` | Nonnegative exponents and positive depths imply that factor-two changes cost at most 2^(exponent+1). All exponent/depth premises are discharged by the original PowerProfile and finite-rank conditions in the concrete shifted theorems. The survival time is not changed. |
| Shifted endpoint moment reduction | `PowerProfile.left_shifted_insertion_moment`, `PowerProfile.right_shifted_insertion_moment` in `Section6ShiftedInsertionMoment` | Proved for the literal deletedGapKernel, both grids, all natural moments, deletions<=r, h>=8r+8, and gap displacement<=r+1 from the full-rank gap. The bound is p!*(B*x_sigma(a,h)/h)^p times the original survival expectation. No rate floor, independence, integrability, normalization, or shell hypothesis is added. Positive constants and small endpoint cutoffs are derived from the original profile. |
| Arrival Chernoff bound | `log_two_sub_half_pos`, `bernoulli_mgf_upper`, `bernoulli_upper_tail_of_mean_le_half` in `Section6BernoulliUpperTail` | Proves the nonidentical Bernoulli MGF bound and an upper-tail bound exp(-(log(2)-1/2)*R) when mean<=R/2, with a proved positive exponent constant. Generic measurability, zero-one, and independence inputs are discharged for the clock indicators below. |
| Actual deleted arrival count | `measurable_clockArrivalIndicator`, `clockArrivalIndicator_zero_one`, `clockArrivalIndicator_independent`, `integral_clockArrivalIndicator`, `deleted_beforeCount_le_arrivals`, `deleted_before_count_upper_tail` in `Section6ArrivalConcentration` | Arrival-by-t is the complement of the existing strict survivor indicator. Its mean is the literal population term, and strict before-count is bounded by arrivals-by-t without an atomlessness assumption. The concrete tail theorem's remaining numerical premise is n*deletedG<=R/2. |
| Actual gap-start/count bridge | `gapStart_lt_imp_beforeCount`, `deleted_gap_start_tail_of_arrival_mean` in `Section6GapStartTail` | An early gap start forces at least q earlier clocks. Original background injectivity is discharged almost surely; compaction identifies the exact deleted count. This proves the gap-start tail on the original probability space provided n*deletedG(t)<=q/2. |
| Left arrival envelope | `deletedG_le_populationG`, `PowerProfile.left_populationG_envelope` in `Section6LeftMeanEnvelope` | Nonnegative arrival summands prove deletion decreases the mean. The existing sharp population error and alpha>1 imply G_n(t)<=M*(t^(1/alpha)+1/n) for 0<t<=1, with no extra profile assumption. |
| Left endpoint mean discharge | `PowerProfile.left_early_deleted_mean` in `Section6LeftTailMean` | Constructs a fixed k>0, k<=1, and H>0 from the proved envelope. For h>=H and 0<h<=n, n*deletedG(k*(h/n)^alpha)<=h/4 for every deletion set. All smallness, reciprocal-error absorption, and real-power identities are proved. |
| Concrete left order-statistic tail | `PowerProfile.left_gap_start_lower_tail` in `Section6LeftGapTail` | Proves P(S_q<k*(h/n)^alpha)<=exp(-rho*h) with k,rho,H positive, uniformly over both grids and shifted gaps. Original PowerProfile and SampledRates discharge the mean premise; the offset arithmetic gives h<=2q. No mean, independence, normalization, or shell hypothesis survives. |
| Right survivor concentration | `deleted_beforeCount_add_survivors_le`, `deleted_survivor_mean_identity`, `deleted_gap_start_tail_of_survivor_mean` in `Section6SurvivorConcentration` | Strict arrivals and survivors are disjoint even at ties. The actual survivor mean is exactly n*deletedH. An early gap start forces the existing Bernoulli lower-tail event; the finite helper requires twice the remaining count to be below the mean. All indicator independence and measurability premises are discharged. |
| Right survivor region | `PowerProfile.right_deletedH_lower_region` in `Section6RightSurvivorRegion` | The existing deleted survivor relative error and the proved joint cutoff lemma yield half the literal Gamma power lead for t>=S and n*t^(-1/beta)>=M. Region constants follow from the original profile and deletion budget. |
| Right endpoint mean discharge | `PowerProfile.right_early_deleted_mean` in `Section6RightTailMean` | Constructs k>0,k<=1,H,delta>0 such that the mean at k*(n/h)^beta is at least 8h. With A the original Gamma coefficient, u=min(1,A/16) and k=u^beta. All time-region and reciprocal-error cutoffs are proved, uniformly over both sampling grids and at most r deletions. |
| Concrete right order-statistic tail | `PowerProfile.right_gap_start_lower_tail` in `Section6RightGapTail` | Proves P(S_q<k*(n/h)^beta)<=exp(-rho*h) for shifted demanded gaps, with the numerical mean premise fully discharged. The count comparison h<=2*(n-d-q)<=4h supplies both the threshold and exponential decay. Only original PowerProfile and SampledRates are model inputs. Both sides of eq:sp-order-lower-tail are now proved. |
| Survival split | `survival_integral_split`, `deleted_survival_split` in `Section6SurvivalSplit` | The generic helper uses measurable, almost surely nonnegative S and a nonnegative coefficient. For the original deleted race these are proved from measurable compaction, actual gap starts, and exponential-clock nonnegativity. Integrability is proved by domination by one. |
| Marked rate times endpoint scale | `left_power_time_identity`, `right_power_time_identity`, `PowerProfile.left_marked_rate_time_lower`, `PowerProfile.right_marked_rate_time_lower` in `Section6RateTimeLower` | Exact real-power cancellation of n. The original profile's initial-block/global sampled rate floors discharge the lower bounds. No new rate hypothesis. |
| Both concrete survival envelopes | `deleted_survival_envelope_of_bounds`, `PowerProfile.left_survival_envelope`, `PowerProfile.right_survival_envelope` in `Section6SurvivalEnvelope` | The helper's rate-time and probability-tail premises are discharged by the preceding rate-time estimates and concrete endpoint lower tails. The actual expectation is bounded by exp(-d*x)+exp(-d*h), for every p>=1. Both sampling grids, bounded full-rank shifts, and the right deletion budget are retained. Constants are constructed from the original profile. |
| Concrete exponential insertion moments | `PowerProfile.left_insertion_exponential_moment`, `PowerProfile.right_insertion_exponential_moment` in `Section6InsertionExponentialMoment` | Combine the concrete shifted moment reduction with the proved survival envelope. Obtain E[X^p] <= p!*(B*x/h)^p*(exp(-d*x)+exp(-d*h)). All helper premises are discharged; no survival expectation or assumed moment bound remains in the conclusion's inputs. |
| Moderate region decay | `moderate_ratio_le_depth`, `moderate_exponential_sum_le` in `Section6ModerateDecay` | For depths a,h>=1, nu<=1, and x<=min(a,h)^nu, prove x<=h and the exponential sum <=2*exp(-d*x). These are finite range conditions, not additional model assumptions. |
| Concrete moderate insertion moments | `PowerProfile.left_moderate_insertion_moment`, `PowerProfile.right_moderate_insertion_moment` in `Section6ModerateInsertionMoment` | E[X^p] <= 2*p!*(B*x/h)^p*exp(-d*x) on the literal moderate set. Positivity of label depth and lower full-rank depth discharge the generic comparison conditions. Constants are independent of p and of the chosen moderate exponent nu<=1. |
| Actual insertion Lp norm | `deleted_kernel_memLp`, `deleted_kernel_eLpNorm_eq_moment`, `deleted_kernel_eLpNorm_le_of_moment` in `Section6KernelLp` | Kernel probabilities lie in [0,1], proving MemLp for every exponent. The standard eLpNorm is identified with the pth root of the literal moment for natural p>0. The generic moment-bound premise is discharged by the concrete moderate results below. No norm or law is redefined. |
| Uniform finite moment range | `uniform_moment_decay` in `Section6UniformMomentDecay` | For 1<=p<=p0, absorbs 2*p! into (2*p0!)^p and uses exponential coefficient d/p0. Gives a pth-power bound with constants independent of p. |
| Concrete moderate Lp envelope | `PowerProfile.left_moderate_insertion_Lp`, `PowerProfile.right_moderate_insertion_Lp` in `Section6ModerateInsertionLp` | Prove eLpNorm X p <= ofReal(C*(x/h)*exp(-d*x)) on the literal moderate set, uniformly for 1<=p<=p0 and both grids. Actual kernel measurability/integrability and the moment bound are fully discharged. C=2*p0!*B and the new exponent coefficient is old d/p0. |
| Real comparison depth | `PowerProfile.right_early_deleted_mean_real_depth` in `Section6RightRealDepthMean` | Extends the deterministic comparison variable h to a positive real without changing the actual finite race, row size, rates, deleted transforms, or any frozen object. The same Gamma lower region proves mean>=8*y at k*(n/y)^beta from original profile/sampling only. |
| Sharp survivor-mean probability decay | `deleted_gap_start_tail_of_survivor_mean_sharp` in `Section6SurvivorMeanTail` | Retains exp(-bernoulliLowerTailConstant*n*deletedH(t)) rather than weakening to the demanded depth. Its explicit numerical mean premise is discharged by the real-depth estimate in the concrete comparison-depth result. Actual indicator independence and gap-event inclusion are proved. |
| Comparison-depth survival envelope | `PowerProfile.right_comparison_depth_survival` in `Section6RightComparisonDepth` | For real y>=h in the derived region, proves E exp(-p*theta*S)<=exp(-d*(a/y)^beta)+exp(-d*y). Original sampled rate floors and exact real-power cancellation discharge the rate-time bound; the mean>=8y and shifted survivor count discharge the sharp-tail premise. |
| Balanced extreme scale | `right_extreme_exponent_bounds`, `right_extreme_scale_identity`, `right_extreme_depth_le`, `right_extreme_depth_cutoff` in `Section6ExtremeScale` | With zeta=beta/(beta+1), prove 0<zeta<1, (a/a^zeta)^beta=a^zeta, a^zeta<=a for a>=1, and construct a cutoff ensuring a^zeta exceeds the required comparison depth. No scale identity is assumed. |
| Concrete right extreme Laplace bound | `PowerProfile.right_extreme_survival` in `Section6RightExtremeSurvival` | Proves E exp(-p*theta*S)<=2*(exp(-d*(a/h)^beta)+exp(-d*a^zeta)). For h<=a^zeta uses the different time k*(n/a^zeta)^beta; otherwise uses the proved typical bound. All cutoffs and constants are constructed; only original profile/sampling, bounded deletion/shift, and the manuscript's depth range are inputs. This completes eq:sp-right-laplace-extreme. |
| Extreme-set depth comparisons | `extreme_ratio_bounds`, `ratio_power_over_depth_le` in `Section6ExtremeRatios` | For a,h>=1 and b,v>0, not (a/h)^b<=min(a,h)^v implies h<a, h<a^(b/(b+v)), and a^(b*v/(b+v))<(a/h)^b. A separate bound proves (a/h)^b/s<=a^b for s>=1. The right corner uses the original depths; the left corner exchanges numerator and denominator depth in this numerical helper without changing the kernel or its rank. |
| Uniform polynomial absorption | `extreme_moment_absorption` in `Section6ExtremeMomentAbsorption` | Uses the already proved power_envelope_absorption, p!<=p0!, and B^p<=max(1,B)^p0. Given z<=a^b and x,y>=a^nu, the actual numerical moment bound yields a pth power of C*exp(-d*a^nu/(2*p0)). The comparisons are explicit helper premises, discharged in both endpoint results below. |
| Concrete right extreme Lp bound | `PowerProfile.right_extreme_insertion_Lp` in `Section6RightExtremeLp` | Original PowerProfile and SampledRates only, plus the manuscript's deletion/shift/depth/moment ranges and extreme-set condition. Chooses nu=min(beta*v/(beta+v),beta/(beta+1))/2. The shifted moment reduction and the alternative-time Laplace estimate supply the actual moment inequality. Extreme_ratio_bounds and power monotonicity discharge every absorption comparison. The resulting eLpNorm is <=ofReal(C*exp(-d*a^nu)). |
| Concrete left extreme Lp bound | `PowerProfile.left_extreme_insertion_Lp` in `Section6LeftExtremeLp` | Chooses nu=min(alpha*v/(alpha+v),1)/2. The original left exponential insertion moment supplies the inequality, and the exchanged numerical depth comparison proves x>=h^nu and x/h<=h^alpha. Gives the actual eLpNorm <=ofReal(C*exp(-d*h^nu)), uniformly for 1<=p<=p0 and both grids. No additional model assumption. |
| Common insertion constants | `insertion_envelope_mono` in `Section6EnvelopeMonotonicity` | Enlarging the coefficient and decreasing the exponential decay coefficient gives a proved pointwise comparison for nonnegative depth factors. |
| Combined insertion Lp envelope | `PowerProfile.left_insertion_Lp_envelope`, `PowerProfile.right_insertion_Lp_envelope` in `Section6InsertionLpEnvelope` | Construct C=max(Cm,Ce), d=min(dm,de), delta=min(deltam,deltae), and H absorbing both regional thresholds and 8r+8. Case-split on the literal moderate-set inequality; retain the typical term everywhere and add the exceptional exponential exactly on its complement. Actual eLpNorm and deleted kernel, both grids, original profile/sampling only. The lemma permits every 0<v<=1, hence supplies the manuscript's existential moderate exponent without adding a model hypothesis. |
| Weighted stretched-exponential summability | `summable_weighted_stretched_exp` in `Section6StretchedExponentialSums` | For b>=0,d>0,nu>0 proves summability of n^b*exp(-d*n^nu). The existing polynomial absorption gives an eventual comparison with n^(-2); the zero term is finite and is not restricted by a new assumption. |
| Weighted stretched-exponential tail | `weighted_stretched_exp_tail` | Derives a uniform tail <=C*exp(-(d/2)*A^nu) by splitting the exponential into two halves, retaining one at the cutoff, and summing the other. Summability is proved internally. |
| Literal exceptional comparison kernel | `exceptionalEnvelope`, `exceptionalEnvelope_nonneg`, `exceptionalEnvelope_le_exp`, `exceptionalEnvelope_support` in `Section6ExceptionalSums` | The definition is exactly exp(-d*a^nu) on the complement of (a/h)^b<=min(a,h)^v, zero otherwise. It is a deterministic comparison kernel, not a replacement probability. For positive depths, the proved extreme ratio bounds imply h<a whenever this kernel is nonzero. |
| Exceptional row and column tails | `exceptionalEnvelope_row_tail`, `exceptionalEnvelope_column_tail` | Row support is contained in range a, so the row sum is <=a*exp(-d*a^nu), then bounded by the weighted tail with b=1. Columns are bounded by the weighted tail with b=0. Actual infinite sums are justified by finite support or proved summable domination. |
| Both exceptional restricted supremum sums | `exceptionalEnvelope_restricted_sums`, `exceptionalEnvelope_transpose_restricted_sums` in `Section6ExceptionalRestrictedSums` | Prove the literal sum of the two real suprema <=C*exp(-(d/2)*A^nu), for A>=1. Nonempty supremum sets and their bounds are proved, not assumed. Transposition yields the left kernel with decay in full-rank h. This completes the exceptional-kernel assertion of eq:sp-extreme-restricted-sums for both corners. |
| Typical kernel on the extreme region | `extremeTypicalEnvelope`, `extremeTypicalEnvelope_nonneg`, `extremeTypicalEnvelope_le_exceptional` in `Section6TypicalExtremeEnvelope` | Literal ratio^b/s*exp(-d*ratio^b), zero on the moderate set. For positive depths, the proved ratio power bound and polynomial absorption give C*exceptionalEnvelope with exponent b*v/(b+v) and decay d/2. The denominator s is explicit and is set to the physical full-rank depth in the concrete corner results. No insertion probability is redefined. |
| Summability for transfer | `exceptionalEnvelope_row_summable`, `exceptionalEnvelope_column_summable`, `tsum_le_scaled_of_nonneg` in `Section6ExceptionalSummability` | Actual restricted exceptional rows have finite support and columns have summable exponential domination. The generic series comparison discharges summability of the dominated nonnegative series. |
| Restricted-sum representation and transfer | `restrictedKernelSums`, `restrictedKernelSums_transpose`, `restrictedKernelSums_of_exceptional_domination` in `Section6RestrictedSumDomination` | The definition is the literal sum of real restricted row and column suprema. The generic pointwise-majorant premise is explicit; summability, nonempty supremum sets, and bounds are proved. The following concrete typical-kernel results discharge the majorant premise. |
| Both typical-kernel restricted sums | `right_typical_extreme_restricted_sums`, `left_typical_extreme_restricted_sums` in `Section6TypicalRestrictedSums` | Prove restrictedKernelSums <=C*exp(-(d/4)*A^(b*v/(b+v))) for A>=1. The right kernel has denominator h. For the left kernel, domination is first proved with numerator-depth denominator and then the two indices are transposed; the resulting physical denominator remains full-rank h. All majorant and nonnegativity premises are discharged. This proves the typical-kernel assertion following eq:sp-extreme-restricted-sums. |
| Explicit exponential slack | `stretched_tail_mono` in `Section6TailSlack` | Proves comparison after decreasing the decay coefficient and power on depths >=1. It is used to obtain one tail exponent for both restricted-sum estimates without changing the kernels. |
| Packaged one-insertion estimates | `PowerProfile.left_insertion_moments`, `PowerProfile.right_insertion_moments` in `Section6InsertionMoments` | One common coefficient C covers the actual Lp envelope and both restricted-sum conclusions. The envelope keeps d and nu, while the tails use the explicit slack d/4 and nuTail=min(nu,b*v/(b+v))<=nu. Closed upper endpoint-block inequalities are obtained by halving the earlier cutoff. All constants, positive exponents, and regional premises are constructed from original profile/sampling only. The source's repeated c,nu after 'decreasing' are represented by distinct envelope/tail names, not by silently changing the kernel. |
| Product-box expectation | `deleted_kernel_product_integrable`, `markedRankCylinder_real_probability_le_gap_product` in `Section6InsertionProductBound` | Uses the already proved and re-audited orderedInsertionKernel_le_product. The ordered event is contained in the product of gap events; distinct labels justify the actual fresh-clock product law. The product is integrable by its probability bound. The real cylinder inequality permits adjacent demanded ranks and repeated gaps, with no rank-separation assumption. |
| Holder for dependent insertion factors | `deleted_kernel_product_le_Lp` in `Section6InsertionHolder` | Finite Holder with equal weights 1/r proves the product integral bound by actual eLpNorm factors. Measurability, finite kernel values, sum of weights, and equality with the standard Lp norm are all discharged. The shared old-clock background need not be independent across factors. |
| Concrete cylinder-to-Lp product | `markedRankCylinder_probability_le_Lp_product` | The exact sorted ordered-insertion identity, product-box inclusion, and Holder yield an ENNReal probability bound by the product of the actual single-insertion Lr norms. Inputs are positive finite weights, r>0, and distinct marked labels and demanded ranks. Natural gap indices include the terminal gap; no presumed endpoint domination matrix is supplied. |
| Further insertion analysis | Unresolved | The one-insertion estimates and cylinder-to-Lp product bound are proved. Global domination still requires opposite-corner/middle source estimates, the fixed boundary-gap cases, global row/column and weighted row bounds, then excursions. Local joint insertion asymptotics, counting/moment arguments, and main CLTs remain unproved. |


## Middle-rank estimates and source-uniform remainder

| Intermediate fact | Completed theorem | Inputs and discharge |
|---|---|---|
| Shifted interior gaps retain two margins | `shifted_interior_gap_bounds` | Bounded deletion and rank shift, interior rank range, and explicit large-row inequality; finite cardinality identity proved. |
| Interior gap starts are positive except with exponentially small probability | `PowerProfile.interior_gap_start_lower_tail` | Original profile and sampling; `small_time_populationG` discharges the arrival-mean bound, and the actual deleted-race Chernoff lemma proves the probability bound. |
| Interior survival expectation | `PowerProfile.interior_survival_envelope` | The preceding tail estimate and positive marked rates discharge the exponential split. |
| Actual insertion moments | `PowerProfile.interior_deleted_insertion_moment`, `.interior_shifted_insertion_moment` | `interior_remaining_rate` and actual prefix-complement cardinalities discharge the all-order rate floor. Half-width geometry discharges bounded shifts and deletions. |
| Finite-range Lp conversion | `PowerProfile.interior_insertion_Lp` | Actual kernel moment identity and uniform factorial absorption; all moment and measurability premises proved. Valid for every source label. |
| Global polynomial upper bound on sampled rates | `PowerProfile.sampled_rate_polynomial_upper` | Original PowerProfile and exact SampledRates only. Finite left endpoint uses the proved global bound; power left endpoint combines the sampled endpoint upper bound with boundedness away from zero. |
| Source-uniform middle remainder | `PowerProfile.interior_rate_remainder` | Derives `(theta_i/n)*exp(-d*n) <= C*exp(-(d/2)*n)` for every source. The preceding polynomial bound discharges the premise of polynomial absorption. |

The ordinary middle kernel and its source-uniform remainder are now available as separate checked inequalities. Assembly into the global matrix, its row/column/weighted bounds, opposite-corner bounds, and boundary-gap cases remain unresolved. The main CLTs and their closed contract checks remain unresolved.


## Middle comparison kernel and actual joint cylinders

| Intermediate fact | Completed theorem | Inputs and discharge |
|---|---|---|
| Ordinary middle kernel plus exponential remainder | `PowerProfile.interior_insertion_envelope` in `Section6InteriorEnvelope` | Original profile/sampling and manuscript helper ranges. Combines the actual Lp estimate with the proved source-uniform remainder. Constants are constructed as B*(1+A), d/2. |
| Uniform middle target bound | `interior_envelope_le_inverse_row` in `Section6InteriorTargetBound` | Proves `(x/n)*exp(-d*x)+exp(-d*n) <= (2*exp(-1)/d)/n` for d,n>0, with arbitrary real x, using the proved sharp weighted-exponential maximum. |
| Middle row and column sums | `interior_envelope_sum_bound` | Any subset of Fin n and any real rate list. The pointwise bound and cardinality <= n discharge the finite sum bound. Constant rate lists give rows; actual rates give columns. This does not claim the entire global matrix is constructed. |
| Actual insertion factor target bound | `PowerProfile.interior_insertion_target_bound` | Combines the first two inequalities to give actual eLpNorm <= ofReal(C/n), uniformly over every source label. No assumed source-rate bound. |
| Actual joint middle-rank event | `PowerProfile.interior_rank_cylinder_bound` in `Section6InteriorCylinder` | Original profile/sampling, r>0, injective labels and ranks, and interior demanded ranks. The threshold max(N,(r+1)/eps) proves every sorted gap is nonterminal; exact deleted cardinality and sorted-gap identities discharge the shift and deletion hypotheses. The existing actual-cylinder Holder theorem yields probability <= ofReal(C/n)^r. Adjacent demanded ranks are permitted. |

Remaining global-domination work: opposite-corner and fixed boundary-gap estimates, a single global matrix with weighted rows and all required corner comparisons, cycle-root and excursion counting. Local joint asymptotics, Gaussian convergence, critical-regime results, and all Section 6 main contract checks remain unresolved.


## Sources outside endpoint blocks

| Intermediate fact | Completed theorem | Inputs and discharge |
|---|---|---|
| Upper rate bound outside a left block | `PowerProfile.sampled_upper_outside_left` | Original profile/sampling, fixed eps in (0,1). Half-label sampling comparison and the proved profile upper bound away from zero discharge the bound for both grids. |
| Lower rate bound outside a right block | `PowerProfile.sampled_lower_outside_right` | Original profile/sampling. Exact label reflection, terminal-depth identity, and the proved profile lower bound away from one discharge the bound. |
| Common-cutoff bounded left scale | `PowerProfile.left_outside_scaled_rate_bounded` | Constructs B=M*eps^alpha from the preceding source upper bound; target h/n<=eps and source label/n>=eps. No assertion B<1. |
| Smaller-target strict scale bound | `PowerProfile.left_outside_scaled_rate_small` | Source cutoff eps is fixed; target cutoff is min(1/2,(1/(M+1))^(1/alpha)). Proves theta_i*(h/n)^alpha<1. The two cutoffs are distinct parameters. |
| Restore ordinary exponential factor | `restore_bounded_exponential` | Numerical z>=0 and x<=B imply z<=exp(B)*z*exp(-x). The common-cutoff result above supplies x<=B in the planned concrete application. |

The actual left-outside insertion norm bound still needs its moment/rate-floor assembly. The right-outside alternative-time survival bound, all fixed boundary-gap cases, and the full global matrix remain unresolved. The manuscript common-cutoff strict inequality is discussed explicitly in the assumption audit; no extra hypothesis has been introduced to force it.


## Actual outside-left insertion norm

| Intermediate fact | Completed theorem | Inputs and discharge |
|---|---|---|
| Deleted survival expectation at most one | `deleted_survival_integral_le_one` in `Section6LeftUnrestrictedLp` | Actual positive finite weights. Almost-sure injective nonnegative clocks imply nonnegative gap starts; integrability and comparison with one are proved. |
| Source-unrestricted left norm bound | `PowerProfile.left_unrestricted_insertion_Lp` | Original PowerProfile/SampledRates. The proved remaining-rate floor supplies the actual moment bound. The preceding survival inequality and finite factorial absorption discharge the norm-conversion premises. No source location or rate upper bound is assumed. |
| Exact left floor ratio | `left_rate_floor_ratio` | Positive numerical C,n,h; proves theta/(C*n^alpha*h^(1-alpha))=(1/C)*theta*(h/n)^alpha/h. |
| Outside-left ordinary kernel | `PowerProfile.left_outside_insertion_Lp` in `Section6LeftOutsideLp` | Original profile/sampling, fixed source cutoff sigma in (0,1), bounded deletions and actual gap q>=4r+4. Constructs target cutoff min(sigma,eps/4), coefficient from the factorial/rate floor and exp(B). The source scale bound is proved internally, not assumed below one. Gives actual eLpNorm <= ofReal(C*theta*(q/n)^alpha/q*exp(-theta*(q/n)^alpha)). |

This completes the outside-left estimate at the actual deleted-gap index q. Uniform conversion from q to demanded rank h under bounded shifts remains unresolved, as do right-outside alternative-time estimates, fixed boundary gaps, full global matrix and excursion bounds, and all Section 6 main CLTs/contracts.


## Outside-left demanded-rank conversion

| Intermediate fact | Completed theorem | Inputs and discharge |
|---|---|---|
| Power comparison under depth doubling | `scaled_power_le_of_depth_le` in `Section6ShiftedKernelComparison` | Numerical q,h>=0,n>0,a>=0,q<=2h imply (q/n)^a<=2^a*(h/n)^a. |
| Shifted ordinary exponential comparison | `shifted_exponential_kernel_bound` | Comparable positive depths and nonnegative scales x<=K*y, y<=K*x imply (x/q)*exp(-x)<=2*K*(y/h)*exp(-y/K). Explicit premises are discharged below. |
| Actual outside-left norm in demanded rank h | `PowerProfile.left_outside_shifted_insertion_Lp` in `Section6LeftOutsideShiftedLp` | Original profile/sampling and helper depth/shift/deletion ranges. Existing shifted_left_gap_bounds proves h/2<=q<=2h and the floor cutoff. Power comparison proves both rate-time scale comparisons. The target cutoff is halved; C is multiplied by 2*2^alpha and decay is 1/2^alpha. No shift comparison is assumed as a new model input. |
| Bounded-source kernel target bound | `left_bounded_source_kernel_le_inverse_row` in `Section6LeftOutsideTarget` | Numerical source rate t<=M, alpha>=1, positive h<=n and nonnegative decay give ordinary kernel<=M/n. Uses (h/n)^alpha<=h/n. |
| Concrete outside-left C/n norm bound | `PowerProfile.left_outside_insertion_target_bound` | Derives M from sampled_upper_outside_left. All positivity, exponent and h<=n premises are discharged from the profile and the constructed block. Actual deletedGapKernel/eLpNorm; no source-rate bound is an input. |

The outside-left shift-conversion obligation is now complete. The separate source and target cutoffs remain explicit. Right-outside alternative-time estimates, fixed boundary gaps, assembly of the global matrix with weighted row/cycle excursion bounds, local joint asymptotics, and all main Section 6 CLTs/contract checks remain unresolved.


## Outside-right survival with the actual source rate

| Intermediate fact | Completed theorem | Inputs and discharge |
|---|---|---|
| Comparison-depth survival retaining theta | `PowerProfile.right_comparison_depth_survival_rate` in `Section6RightComparisonRate` | Original profile/sampling, bounded deletion/shift and real comparison depth y>=h. The actual survivor mean and sharp Chernoff estimate are proved; the rate-time identity retains theta_i*(n/y)^beta rather than substituting a source-depth floor. |
| Sublinear comparison-depth range | `sublinear_power_cutoff` in `Section6SublinearCutoff` | For 0<z<1 and delta>0 constructs N so n>=N implies H<=n^z and n^z/n<delta. Uses two actual limits, with no assumed or prescribed convergence rate. |
| Outside-right survival | `PowerProfile.right_outside_survival` in `Section6RightOutsideSurvival` | Original profile/sampling, fixed source cutoff sigma, bounded deletions/shifts. Outside-source lower rate b is derived. At h<=n^(beta/(beta+1)), uses that comparison depth and the exact balance identity; at larger h uses y=h. Proves E exp(-p*theta*S)<=2*(exp(-d*theta*(n/h)^beta)+exp(-d*n^(beta/(beta+1)))). |
| Source-unrestricted shifted right moments | `PowerProfile.right_unrestricted_shifted_moment` in `Section6RightUnrestrictedMoment` | Original profile/sampling and bounded deletion/shift. The actual survivor count m=n-removed.card-q and rate floor discharge the moment bound. h<=2m and the power comparison give prefactor B*theta*(n/h)^beta/h, retaining the actual source rate. |

The outside-right survival and moment ingredients are complete. Their Lp assembly and polynomial-prefactor absorption into the stretched-exponential row remainder remain unresolved, together with fixed boundary gaps, full global domination/weighted sums and excursions, and all Section 6 main CLTs/contract checks.


## Outside-right Lp assembly and remainder absorption

| Intermediate fact | Completed theorem | Inputs and discharge |
|---|---|---|
| Finite-range outside-right insertion norms | `PowerProfile.right_outside_insertion_Lp` in `Section6RightOutsideLp` | Original profile/sampling and existing source/deletion/shift/depth ranges. The actual shifted moment and outside survival estimates are combined. The factor four is absorbed by doubling B before the existing uniform factorial conversion. Actual eLpNorm and its moment identity are used. |
| Source-uniform right remainder | `PowerProfile.right_rate_remainder` in `Section6RightRemainder` | Original profile/sampling, d,z>0 and h>=1. The derived global polynomial rate bound and (n/h)^beta<=n^beta yield a polynomial prefactor. The proved power absorption gives C*exp(-(d/2)*n^z). No polynomial upper bound is assumed. |
| Outside-right ordinary kernel plus remainder | `PowerProfile.right_outside_insertion_envelope` in `Section6RightOutsideEnvelope` | Combines both preceding concrete results with z=beta/(beta+1), whose positivity follows from the profile. Constructs common C=B*(1+A), decay d/2. The remainder has no rate or depth prefactor. |

Outside-right Lp assembly and prefactor absorption are now complete. Its kernel bound is in the demanded right depth h with all permitted shifts/deletions. Fixed earliest/latest gaps, assembly of one global matrix with the required row/column/weighted bounds, cycle-root/excursion counting, local joint asymptotics and all Section 6 main CLTs/contract checks remain unresolved.


## Earliest active-left boundary gaps

| Intermediate fact | Completed theorem | Inputs and discharge |
|---|---|---|
| Fixed-removal rate floor | `PowerProfile.left_fixed_removal_rate_floor` in `Section6EarlyRateFloor` | Original profile/sampling, fixed removal cap s. Uses the proved left rate floor at reference rank s+1 and constructs a row threshold, giving C*n^alpha after any s removals. No arrival before the gap is required. |
| Generic rate-floor norm conversion | `deleted_kernel_Lp_of_rate_floor` in `Section6RateFloorLp` | Positive W and all-order floor are explicit helper premises. Actual moment, survival expectation <=1 and finite factorial conversion are proved. The floor is discharged by the concrete theorem below. |
| Earliest actual insertion gaps | `PowerProfile.early_left_insertion_Lp` in `Section6EarlyLeftLp` | Original profile/sampling, fixed deletion cap r, gap cap Q and moment cap p0. Prefix cardinality <=r+Q supplies the fixed-removal floor for every order. Proves actual eLpNorm<=ofReal(C*theta_i/n^alpha), including q=0. |

The earliest active-left gap norm bound is complete. Its column summation, latest active-right gaps including the infinite final gap, inactive-endpoint boundary regions, full global matrix/weighted sums/cycle excursions and all main CLTs/contracts remain unresolved. No infinite gap is identified with a finite interval by this milestone.


## Infinite final-gap survival reduction

| Intermediate fact | Completed theorem | Inputs and discharge |
|---|---|---|
| Count implies passage of an earlier gap start | `gapStart_lt_of_beforeCount` in `Section6TerminalGapSurvival` | Injective finite clocks, t>0 and at least q arrivals. q=0 is handled separately; otherwise the exact arrival-time/count equivalence is used. |
| Later gap mass dominated by survival | `deletedGapKernel_le_earlier_survival` | Exact event inclusion in the exponential tail after a finite reference gap. The later index k is any natural number >=q, including the final gap index. Nonnegative/injective background conditions are explicit finite helper premises. |
| Almost-sure real-valued bound | `deletedGapKernel_le_earlier_survival_ae` | Actual original exponentialRace supplies all nonnegativity and injectivity premises; finite ENNReal-to-real conversion is proved. |
| Later-gap moment reduction | `deleted_gap_moment_le_earlier_survival` | Actual kernel moments bounded by E exp(-p*theta*S_q). Kernel and survival integrability, pointwise power inequality and exponential power identity are proved. No finite length of the later gap is used. |
| Reference gap construction | `terminal_window_reference_gap` in `Section6TerminalReference` | Given h>=removed.card+Q+1 and h<n, constructs q=n-h in Fin(actual background cardinality) and proves q<=k for every k in the last Q gaps. The witness and both inequalities are proved by exact cardinality arithmetic. |

The infinite-gap reduction is complete. The endpoint-specific choice of reference depth, its survival bound and the final norm estimate for all terminal-window sources remain unresolved; no complete latest-gap theorem is claimed. Inactive-endpoint boundary regions, global matrix estimates, cycle excursions and all main CLTs/contracts also remain open.


## Terminal-window decay for outside-right sources

| Intermediate fact | Completed theorem | Inputs and discharge |
|---|---|---|
| Concrete outside-source terminal norms | `PowerProfile.terminal_outside_right_Lp` in `Section6TerminalOutsideLp` | Original profile/sampling, fixed source cutoff sigma, deletion cap r, terminal width Q and moment cap p0. Constructs h=8r+Q+8, the sublinear row cutoff and q=n-h; proves q is finite and before every terminal-window gap. The actual comparison-depth survival and derived source lower rate imply moment<=2*exp(-d*n^(beta/(beta+1))). The existing uniform factorial conversion proves the actual norm bound, including the infinite final gap. No reference gap or survival estimate is assumed. |
| Uniform bound for bounded source depths | `deleted_kernel_eLpNorm_le_one` in `Section6KernelLpOne` | Every original finite positive-weight model, every natural gap index and every ENNReal norm exponent. Actual kernel<=1 and probability measure normalization prove the bound. This includes final and empty gaps. |

The outside-right terminal-window estimate is complete. Same-corner terminal source-depth decay still requires assembly, including the bounded-depth case; inactive endpoint boundaries, global matrix/weighted estimates, cycle excursions and all main CLTs/contracts remain unresolved.


## Active-right terminal source depths and inactive-left gaps

| Intermediate fact | Completed theorem | Inputs and discharge |
|---|---|---|
| Terminal-window source-depth decay | `PowerProfile.terminal_right_depth_Lp` in `Section6TerminalRightLp` | Original profile/sampling and fixed deletion/window/moment caps. For large source depth a, constructs q=n-(8r+Q+8), compares at y=a^(beta/(beta+1)), and proves the range from a cutoff and n's sublinear cutoff. The exact scale identity gives survival decay in a. For bounded a uses the proved norm<=1 and a constructed exponential coefficient. All source labels and the infinite final gap are included. |
| Inactive-left initial gap norms | `PowerProfile.inactive_left_insertion_Lp` in `Section6InactiveLeftLp` | Original PowerProfile with finite left endpoint and SampledRates. For 4r+4<=n and 4q<=n, prefix cardinality leaves at least n/2 labels. The existing profile-based remaining-rate bound and derived global rate upper bound discharge the generic rate-floor norm theorem, yielding C/n including q=0. |

The active-right terminal source-depth estimate is now complete. The inactive-left actual-gap estimate is complete in the stated initial-quarter range; any demanded-rank shift packaging must still discharge this range. Inactive-right terminal estimates, full global matrix row/column/weighted estimates, cycle excursions and all main CLTs/contracts remain unresolved.


## Logarithmic-time survivor reservoir

| Intermediate fact | Completed theorem | Inputs and discharge |
|---|---|---|
| Deleted interior survivor reservoir | `interior_deleted_survivor_reservoir` in `Section6SurvivorReservoir` | Interior continuity/positivity and exact sampling only. The proved interior block and rate bounds supply at least n/16 remaining labels when n>=8 and n>=16r. Gives (n/16)*exp(-M*t)<=n*deletedH(t) for t>=0. |
| Exact square-root identity | `logarithmic_survival_sqrt_identity` | For positive real n,m, proves n*exp(-log(n/m)/2)=sqrt(n*m) by the exponential/log identity and nonnegative square roots. |
| Logarithmic survivor mean | `interior_logarithmic_survivor_mean` in `Section6LogarithmicReservoir` | Constructs c=1/(4M), with M*c<1/2. The actual reservoir gives n*deletedH(c*log(n/m))>=sqrt(n*m)/16 for 0<m<=n. No assumed reservoir or endpoint floor. |
| Actual logarithmic gap tail | `logarithmic_gap_start_tail` in `Section6LogarithmicGapTail` | The preceding mean plus 16384h<=n and bounded-deletion/shift geometry discharge the sharp Chernoff threshold. Proves probability(S_q<c*log(n/h))<=exp(-rho*sqrt(n*h)); no endpoint assumption is needed for this probability estimate. |

The inactive-right logarithmic probability ingredient is complete. Its rate-floor moment/norm assembly, polynomial-prefactor absorption, fixed final-gap specialization and global matrix estimates remain unresolved, as do all main CLTs/contracts.


## Inactive-right finite shifted-gap kernel

| Intermediate fact | Completed theorem | Inputs and discharge |
|---|---|---|
| Remaining-rate moment bound | `PowerProfile.inactive_right_shifted_moment` in `Section6InactiveRightMoment` | Original finite-right PowerProfile supplies a derived global positive lower bound d. The actual prefix cardinality and bounded rank shift give at least h/2 remaining labels; every order has remaining rate >=d*h/2. Discharges the generic moment lemma's complete rate-floor premise. |
| Logarithmic survival integral | `logarithmic_survival_envelope` in `Section6LogarithmicSurvival` | Interior continuity/positivity and exact sampling; previously proved gap tail, nonnegative log(n/h), p>=1, and the actual source rate discharge every survival-split bound. |
| Actual insertion Lp bound | `PowerProfile.inactive_right_insertion_Lp` in `Section6InactiveRightLp` | Original finite-right PowerProfile plus sampling. Applies the two preceding concrete estimates, uniform factorial/moment decay and the proved moment-to-norm inequality. No moment or survival assertion is an input. |
| Full square-root remainder absorption | `PowerProfile.logarithmic_rate_remainder` in `Section6LogarithmicRemainder` | Original PowerProfile derives theta<=K*n^a. For h>=1, n<=sqrt(n*h)^2 and polynomial absorption yield (theta/h)*exp(-d*sqrt(n*h))<=C*exp(-(d/2)*sqrt(n*h)). No loss to an n-only exponent. |
| Combined inactive terminal kernel | `PowerProfile.inactive_right_insertion_envelope` in `Section6InactiveRightEnvelope` | Constructs constants from the norm and absorption results. All mathematical estimates are discharged; finite-gap geometric ranges remain explicit helper premises. |

Still unresolved: inactive-right fixed final gaps, the global matrix construction and discharge of all helper ranges, global row/weighted bounds, cycle excursions and all main Section 6 CLTs/closed contract checks. The logarithmic-to-real-power formula is used only as explanatory mathematical notation here, not as an unproved imported Lean lemma.


## Inactive-right final gaps and all-gap assembly

| Intermediate fact | Completed theorem | Inputs and discharge |
|---|---|---|
| Terminal-window logarithmic norm | `terminal_logarithmic_insertion_Lp` in `Section6TerminalLogarithmicLp` | Interior continuity/positivity and exact sampling. The finite reference q=n-h is constructed from h>=8r+Q+8 and 16384h<=n; the existing event inclusion, logarithmic survival theorem and uniform moment reduction prove the norm bound. The later index is natural and includes the infinite final gap. |
| Bounded-depth numerical comparison | `bounded_terminal_logarithmic_kernel` in `Section6BoundedTerminalKernel` | Generic explicit premises 1<=m<=H, H^2<=n, theta>=b>0 and d>0 imply log(n/m)<=2log(n/H), sqrt(n*m)<=sqrt(n*H), and 1<=(H/b)*(theta/m). No probabilistic input. |
| Actual fixed inactive-right terminal window | `PowerProfile.terminal_inactive_right_envelope` in `Section6TerminalInactiveRight` | Original finite-right profile and sampling. Chooses H=8r+Q+8 and N=16384H+H^2; proves all geometry and derives b from global_lower_of_right_finite. Discharges every premise of the preceding two helpers for every 1<=m<=Q+1. |
| All positive inactive-right target depths | `PowerProfile.inactive_right_all_gaps_envelope` in `Section6InactiveRightAllGaps` | Original finite-right profile and sampling. For h>=8r+8, shifted survivor arithmetic proves q is finite and discharges finite-gap helper ranges. Otherwise Q=10r+10 supplies terminal-window membership and the bounded-depth theorem. Constants are enlarged and decay reduced by proved monotonicity. Includes actual final gap. |
| Exact logarithmic/power representation | `exp_neg_rate_log_ratio` in `Section6LogarithmicKernelIdentity` | Positive real n,m only; exp(-t*log(n/m))=(m/n)^t, proved by real logarithm division and rpow definition. |
| Literal manuscript inactive terminal kernel | `PowerProfile.inactive_right_all_gaps_power_kernel` in `Section6LogarithmicKernelIdentity` | Uses the all-gap envelope and exact identity. Uniform over all positive depths with 16384h<=n, all bounded deletions/shifts and 1<=p<=p0. No final-gap exclusion. |

The inactive-right final-gap estimate and its merger with finite depths are now complete in the stated endpoint region. Remaining: global matrix construction and region partition, row/weighted estimates, cycle excursions, local joint asymptotics, Gaussian limits and closed main contract checks. No main CLT is claimed.


## Early-target contribution to weighted rows

| Intermediate fact | Completed theorem | Inputs and discharge |
|---|---|---|
| Rate-preserving early gap norm | `PowerProfile.early_insertion_rate_Lp` in `Section6EarlyRateLp` | Original PowerProfile and exact sampling. The interior remaining-rate theorem and actual prefix cardinality derive W>=B*n for q<=n/4 and n>=4r+4. Generic rate-floor Lp premise fully discharged. Gives C*theta_i/n without a source upper-bound hypothesis. |
| Early demanded-rank shifts | `PowerProfile.early_shifted_rate_Lp` in `Section6EarlyShiftedRateLp` | Proves q is a finite background gap and 4q<=n from n>=8r+8, 1<=h, 8h<=n, at most r deletions and dist(q,h-1)<=r+1. Includes earliest targets. |
| Uniform inactive-left consequence | `PowerProfile.inactive_left_shifted_Lp` in `Section6EarlyShiftedRateLp` | Finite-left PowerProfile derives the global source upper bound; combined with the preceding rate-preserving estimate yields C/n. |
| Actual early-target row sum | `PowerProfile.early_insertion_rate_row` in `Section6EarlyRateRow` | Applies the concrete shifted bound to each target, with target-dependent deletion sets and gaps. Uses at most n targets and finite ofReal sum equality to bound the sum of ENNReal Lp norms by ofReal(C*theta_i). No finite norm, row mass or independence is assumed. |
| Reciprocal-depth power cancellation | `reciprocal_depth_power_product_le_one` in `Section6EarlyRightWeightedRow` | Positive a<=n and kappa<=beta imply (n/a)^kappa*(a/n)^beta<=1 by exact rpow identities. |
| Early-target right weighted row | `PowerProfile.early_right_weighted_row` in `Section6EarlyRightWeightedRow` | Original right-power profile supplies theta_i<=K*(a/n)^beta in a constructed source neighborhood. Combines that estimate, the actual early row bound and the proved power cancellation. No right-rate bound is added as input. |

Unresolved: ordinary inactive-right row summation; other global row, column and weighted pieces; construction of the single domination matrix and all region discharges; cycle excursions, local joint asymptotics and main CLTs/contracts. Only the early-target part of the weighted row is proved here.


## Inactive-right row summation

| Intermediate fact | Completed theorem | Inputs and discharge |
|---|---|---|
| Discrete power-sum upper bound | `positive_power_sum_upper` in `Section6PositivePowerSumUpper` | z>-1, M>0. Splits z>=0 and z<0, uses the appropriate monotone integral comparison on [1,M] and the exact rpow integral. The possible singularity at zero is not replaced by a boundedness assumption. |
| Literal kernel power factorization | `terminal_power_kernel_factor` in `Section6TerminalPowerSum` | Positive real depth and row size; exact rpow/division identity. |
| Discrete integral bound | `terminal_power_sum_upper` in `Section6TerminalPowerSum` | t>0 and theta>=0. Instantiates the power sum at z=t-1 and proves the exact integral/endpoint coefficient identities. |
| Half-depth truncation | `terminal_power_sum_half_bound` in `Section6TerminalPowerSum` | Proves sum<=theta/t+2*theta*(1/2)^t from 2M<=n, using M>=1 for both corrections. |
| Uniform endpoint correction | `rate_times_half_power_bound` in `Section6InactiveOrdinaryRow` | d>0 only; proves theta*(1/2)^(d*theta)<=exp(-1)/(d*log 2). |
| Uniform ordinary inactive row | `inactive_terminal_ordinary_row_bound` in `Section6InactiveOrdinaryRow` | Substitutes t=d*theta, theta>0. Includes empty target range. No upper or fixed lower bound on theta is needed by this numerical estimate. |
| Full numerical kernel row | `inactive_terminal_kernel_row_bound` in `Section6InactiveKernelRow` | Adds the correction by proving exp(-d*sqrt(n*h))<=exp(-d*sqrt(h)) for n>=1 and using the existing proved summability of stretched exponentials. |
| Arbitrary actual target subsets | `inactive_terminal_kernel_subset_bound` in `Section6InactiveKernelSubset` | Reverse-label indexing is injective; targets with 16384*depth<=n map into range(n/16384), whose half-depth condition is proved. Missing targets are added only using kernel nonnegativity. |
| Actual inactive-right insertion row | `PowerProfile.inactive_right_insertion_row` in `Section6InactiveInsertionRow` | Original finite-right profile and exact sampling. Applies the concrete all-gap kernel theorem to each target, then sums via ofReal and the proved subset bound. Target-dependent deletions/shifts are allowed; every depth/index range is discharged. No assumed row bound or independence. |

Remaining: single global domination matrix and partition, other row/column/weighted pieces, cycle excursions, joint local asymptotics and all main CLTs/contracts. The inactive-right row contribution itself is now proved in its stated endpoint region.


## Ordinary active row estimates

| Intermediate fact | Completed theorem | Inputs and discharge |
|---|---|---|
| Comparable-depth real powers | `comparable_depth_rpow_bounds` in `Section6ComparableDepthPowers` | Positive h<=x<=2h; handles both signs of the exponent and supplies the explicit factor 2^abs(s). |
| Power density integrability | `power_density_integrable` in `Section6PowerDensityIntegral` | p!=0, A,d>0. Instantiates the proved power-exponential integrability theorem at exponent p-1, checking ((p-1)+1)/p=1. |
| Exact power density integral | `integral_power_density` in `Section6PowerDensityIntegral` | Same inputs; derives integral=1/(d*abs(p)) from the existing Gamma integral with Gamma(1)=1. |
| Unit-cell comparison | `power_density_cell_bound` in `Section6PowerDensityCell` | For h>=1 and h<=x<=h+1, proves density(h)<=2^abs(p-1)*comparison_density(x), with comparison decay d/2^abs(p). All power and exponential comparisons are proved. |
| Uniform positive depth sum | `power_density_positive_sum_bound` in `Section6PowerDensitySum` | The preceding pointwise comparison, integrability on the actual cell union, nonnegativity, and the exact full integral prove a bound independent of A and the finite cutoff M. No summed-kernel estimate is assumed. |
| Forward-ratio identity | `scaled_power_density_factor` in `Section6ActiveOrdinaryRows` | Positive n,h; exact transformation with A=theta/n^p. |
| Forward-ratio ordinary row | `scaled_power_kernel_row_bound` in `Section6ActiveOrdinaryRows` | Positive n,theta and p!=0; A positivity is proved and the uniform density sum is applied. The manuscript's alpha>1 implies the generic p!=0 premise. |
| Inverse-ratio identity | `reverse_ratio_power` in `Section6ActiveOrdinaryRows` | Positive n,h; exact equality (n/h)^beta=(h/n)^(-beta). |
| Active-right ordinary row | `inverse_power_kernel_row_bound` in `Section6ActiveOrdinaryRows` | beta,d>0; applies the previous row bound with p=-beta and proves every required positive-depth premise from finite-index membership. |

Remaining: complete actual active-endpoint row assembly with boundary/exceptional pieces, global matrix construction and partition, columns and remaining weighted rows, excursions/local joint limits and all main CLTs/contracts. The ordinary row formulas themselves are now bounded with no source-rate restriction beyond positivity.


## Weighted cell sums, ordinary columns and right weighted rows

| Intermediate fact | Completed theorem | Inputs and discharge |
|---|---|---|
| Weighted unit-cell comparison | `power_weighted_cell_bound` in `Section6PowerWeightedCell` | h>=1 and x in [h,h+1], A,d>0; independent real weight a and exponential power p. Comparable-depth power bounds supply every comparison. |
| Weighted density integrability | `power_weighted_density_integrable` in `Section6PowerWeightedIntegral` | p!=0, A,d>0, (a+1)/p>0; applies the existing proved power-exponential integrability theorem. |
| Exact weighted density integral | `integral_power_weighted_density` in `Section6PowerWeightedIntegral` | Same hypotheses; proves coefficient ((1/d)^q*Gamma(q)/abs(p))*A^(1-q), q=(a+1)/p, using exact reciprocal/rpow identities. |
| Finite weighted power sum | `power_weighted_positive_sum_bound` in `Section6PowerWeightedSum` | Unit-cell comparison, integrability and positive half-line integral discharge the finite sum bound with the exact A scaling. |
| Signed-power numerator columns | `ordinary_power_column_bound` in `Section6ActiveOrdinaryColumns` | Takes a=p and A=t^(-p), proves the resulting scale is exactly t. Explicit generic condition (p+1)/p>0. |
| Right ordinary numerator column | `right_ordinary_column_bound` in `Section6ActiveOrdinaryColumns` | beta>0 proves the generic integrability premise. Bounds sum (h/t)^beta*exp(-d*(h/t)^beta) by C*t. |
| Left ordinary numerator column | `left_ordinary_column_bound` in `Section6ActiveOrdinaryColumns` | alpha>1 proves (-alpha+1)/(-alpha)>0; exact inverse-ratio identity supplies the original sum (t/h)^alpha*exp(-d*(t/h)^alpha)<=C*t. |
| Weighted inverse-kernel identity | `weighted_inverse_density_identity` in `Section6RightOrdinaryWeightedRow` | Positive source/target depths; exact algebraic/rpow representation, no inequality or model restriction. |
| Same-corner right ordinary weighted row | `right_ordinary_weighted_row_bound` in `Section6RightOrdinaryWeightedRow` | beta,d>0 and kappa<beta imply the required Gamma integrability. Takes A=a^beta; proves its scale is a^kappa and cancels the exterior a^(-kappa). Uniform in source depth and finite target cutoff. |

These numerical kernel components are proved; a global matrix is not assumed or constructed by these statements. Remaining obligations include the left weighted row, assembly with actual-profile, outside-corner, boundary and exceptional estimates, the complete matrix/cylinder bounds, excursions/collisions/local asymptotics and all main CLTs/contracts.


| Additional ordinary left component | Theorem | Inputs and discharge |
|---|---|---|
| Weighted forward-ratio identity | `weighted_forward_density_identity` in `Section6LeftOrdinaryWeightedRow` | Positive source/target depths. Applies the signed inverse identity and proved reciprocal-ratio equality, preserving the manuscript (a/h)^kappa weight. |
| Same-corner ordinary left weighted row | `left_ordinary_weighted_row_bound` in `Section6LeftOrdinaryWeightedRow` | alpha,d>0 and kappa<alpha. Instantiates the weighted sum at exponent alpha-kappa-1 and p=alpha; checks integrability, proves the scale a^(-kappa), and cancels the exterior a^kappa. No assumed weighted-row bound. |

The local ordinary weighted rows on both sides are now supplied by concrete numerical proofs. Full global weighted rows and actual matrix construction remain unresolved, including every required outside-corner and exception contribution.

Combined validation now includes all eleven weighted-integral, column and weighted-row statements: the default build passed (4181 jobs), and all 495 source theorem types and transitive axiom sets were audited successfully. The only axiom dependencies are propext, Classical.choice and Quot.sound; all 13 frozen files are unchanged. See ../audit/section6-progress-validation.json and ../audit/section6-current-audit.log. This checks the helper entries above, not the unresolved global matrix or main CLT contracts.

### Outside-left weighted contribution

| Intermediate fact | Theorem | Inputs and discharge |
|---|---|---|
| Exact weighted power identity | `outside_left_weighted_power_identity` in `Section6OutsideLeftWeightedSum` | Positive depths, arbitrary real exponents; combines reciprocal and real-power identities without changing the kernel. |
| Weighted ordinary finite sum | `outside_left_weighted_sum_bound` | Nonnegative source rate and decay, positive row scale, kappa<alpha, targets in the early half. Drops the exponential by its proved bound by one; applies the existing power-sum comparison at alpha-kappa>0. Retains the source-rate prefactor and handles the empty sum. |
| Arbitrary target subset | `outside_left_weighted_subset_bound` in `Section6OutsideLeftWeightedSubset` | Injects Fin labels into their zero-based values and proves containment in range(n/2); summand nonnegativity is proved. |
| Actual insertion norms with weight (n/h)^kappa | `PowerProfile.outside_left_weighted_insertion_row` in `Section6OutsideLeftWeightedRow` | Original power profile and sampling; source outside a fixed left block, moderate targets h>=8r+8 in a derived smaller block, bounded deletions and rank shifts. Derives the source-rate upper bound from the profile. Natural gap indices are accepted; nonterminal membership is proved from the stated geometry and deletion bound, not hidden in a Fin parameter. Deletions and gaps can vary with the target. |
| Manuscript weight (d_L(i)/d_L(j))^kappa | `PowerProfile.outside_left_manuscript_weighted_row` | Uses 0<=kappa<alpha, covered by the manuscript's chosen positive exponent. Proves d_L(i)<=n and monotonicity of the weight; all other premises are exactly those of the actual moderate-target helper. |

This closes the outside-source, moderate-left-target weighted contribution. The fixed earliest targets, other global regions and exceptional contributions must still be assembled; the complete global matrix and main CLTs remain unresolved. All five statements compiled individually. The full default build passed with 4184 jobs; the regenerated elaborated-type and transitive-axiom audit passed for all 500 source theorems. The validator confirms only propext, Classical.choice and Quot.sound, with all 13 frozen files unchanged. The exact manuscript-weight helper's elaborated type and all three axiom dependencies were inspected in the current audit log.

### Earliest left targets and outside-left assembly

| Intermediate fact | Theorem | Inputs and discharge |
|---|---|---|
| Global left power envelope | `PowerProfile.global_left_sampled_power_upper` in `Section6LeftWeightedRate` | Original profile and exact sampling. Combines the near-left rate envelope with the derived outside-left upper bound; proves that the power envelope is at least one on all sampled label depths. |
| Weighted earliest-gap rate factor | `PowerProfile.left_weighted_rate_bound` | Proves d_L(i)^kappa*theta_i/n^alpha<=K for every source and kappa<=alpha. Cancels the exact powers in the global envelope and uses d_L(i)>=1. No rate bound is assumed. |
| Actual fixed earliest-target weighted row | `PowerProfile.early_left_weighted_insertion_row` in `Section6EarlyLeftWeightedRow` | Original profile, 0<=kappa<=alpha, fixed deletion bound r and target cutoff H. Derives the gap cutoff H+r from bounded shifts, constructs a large-row threshold ensuring finite gap membership, applies the existing actual earliest-gap estimate and the new weighted rate bound, and proves target-subset cardinality<=H. Includes gap zero and target-dependent deletions/gaps. |
| All left-target depths for outside sources | `PowerProfile.outside_left_all_weighted_row` in `Section6OutsideLeftAllWeightedRow` | Original profile and sampling, 0<=kappa<alpha, fixed source cutoff and deletion/order bounds. Constructs delta and N, splits arbitrary target subsets at h=8r+8, discharges both earliest and moderate branches, and adds their bounds. There is no remaining bounded-depth exclusion in this outside-source endpoint contribution. |

The same-corner actual weighted assembly, remaining global regions and exceptions, single global matrix and main CLTs remain unresolved. All four new statements compiled individually. The full default build passed with 4187 jobs; the regenerated type and transitive-axiom audit passed for all 504 source theorems, with only propext, Classical.choice and Quot.sound. The validator confirms all 13 frozen files unchanged. The complete elaborated type of outside_left_all_weighted_row was inspected, including its natural gap indices, source restriction, bounded target shifts/deletions, quantifier order and actual weighted norm conclusion.

### Sampled ordinary weighted kernel contributions

| Intermediate fact | Theorem | Inputs and discharge |
|---|---|---|
| Weighted rate-time comparison | `weighted_rate_kernel_comparison` in `Section6WeightedKernelComparison` | Generic two-sided comparison b*v<=u<=B*v, positive target depth and nonnegative factors. Uses the upper comparison for the prefactor and the lower comparison for exponential decay. These comparison premises must be proved in each concrete use. |
| Left sampled-rate ordinary weighted sum | `PowerProfile.left_sampled_ordinary_weighted_row` in `Section6LeftSampledWeightedRow` | Original left power profile, exact sampling, kappa<alpha and positive decay. The global upper rate envelope and local marked rate-time lower bound are proved existing results; exact power-time identities discharge both comparison premises. Applies the ordinary weighted sum with derived decay d*c/2. Constants and the source block are constructed. |
| Right sampled-rate ordinary weighted sum | `PowerProfile.right_sampled_ordinary_weighted_row` in `Section6RightSampledWeightedRow` | Original right power profile, exact sampling, kappa<beta and positive decay. Derives the source-block upper rate estimate and global marked rate-time lower estimate, then applies the right ordinary weighted sum at the derived decay d*b. |
| Injective target-depth summation | `positive_depth_subset_sum_le` in `Section6SampledWeightedSubsets` | Generic nonnegative function and injective depths in 1..n; proves the image-subset comparison with Ico 1 (n+1). No summation estimate is assumed. |
| Left and right arbitrary target subsets | `PowerProfile.left_sampled_weighted_subset`, `PowerProfile.right_sampled_weighted_subset` | Prove injectivity, range and nonnegativity for j.val+1 and terminalDepth j, then discharge the finite-sum bound using the concrete sampled ordinary row theorem. No hypotheses on targets beyond membership in Fin n. |

These are the ordinary sampled-rate kernel contributions. They do not alone bound actual insertion norms, which also require their endpoint validity ranges, boundary conventions and exceptional kernels. The global matrix and main CLTs remain incomplete. All six new statements compiled individually; the full default build passed with 4191 jobs. The regenerated type and transitive-axiom audit passed for all 510 source theorems, with only propext, Classical.choice and Quot.sound. The validator confirms all 13 frozen files unchanged. Both sampled subset theorem types were inspected, including their implicit parameters, source-block restrictions, constructed constants and literal sampled-rate weighted kernels.

### Exceptional weighted rows and actual left-corner assembly

| Intermediate fact | Theorem | Inputs and discharge |
|---|---|---|
| Exceptional weight is at most one | `exceptional_weighted_factor_le` in `Section6ExceptionalWeightedRows` | Positive power and support exponent, positive integer depths, nonnegative kappa. Uses the proved exceptional support h<a; the zero-envelope case is handled separately. |
| Right and left exceptional weighted sums | `right_exceptional_weighted_row_bound`, `left_exceptional_weighted_row_bound` | Positive decay and stretched-exponential exponent. Derive summability from the existing exceptional row/column theorems and apply their tail bounds at cutoff 1. The left expression is the exact transposed envelope. No summability assumption is added. |
| Complete left numerical envelope weighted sum | `left_envelope_weighted_sum_bound` in `Section6LeftEnvelopeWeightedSum` | 0<=kappa<alpha and positive envelope parameters. Adds the proved ordinary and exceptional weighted bounds, retaining the literal sum of both kernels. |
| Actual left-corner weighted insertion row | `PowerProfile.left_corner_weighted_insertion_row` in `Section6LeftCornerWeightedRow` | Original profile/sampling, fixed r and positive p0, 0<=kappa<alpha. Chooses v=1 internally, constructs source/target cutoff H and delta, proves nonterminal membership for natural shifted gaps, applies the actual insertion envelope and the complete weighted sum. Requires source and target depths at least H, explicitly. |
| Every positive target depth for the left source block | `PowerProfile.left_corner_all_targets_weighted_row` in `Section6LeftCornerAllTargets` | Splits targets below/above the derived H, chooses a natural upper cutoff for the earliest branch, applies the existing earliest weighted row, and discharges both branch premises. Constructs a large-row threshold. No lower target-depth exclusion remains, but source depth>=H remains explicit. |

Remaining: fixed small left-source depths, complete right actual weighted assembly, cross-region contributions, the single global matrix and every main CLT/contract check. The source cutoff in the last two regional helpers is not a permitted extra main-theorem hypothesis. All six new statements compiled individually; the full default build passed with 4195 jobs. The regenerated type and transitive-axiom audit passed for all 516 source theorems, with only propext, Classical.choice and Quot.sound. The validator confirms all 13 frozen files unchanged. The complete elaborated type of left_corner_all_targets_weighted_row was inspected, including its explicit source cutoff, natural gap indices and actual weighted norm conclusion.

### Removal of the redundant left-source cutoff

Inspection of `left_extreme_insertion_Lp` showed that its source-depth H premise supplied only 1<=i.val+1. That fact follows directly from the label type, and the underlying `left_insertion_exponential_moment` already has no lower source cutoff. The extreme theorem now proves the stronger statement directly, retaining the original endpoint source-block condition. The moderate theorem was already valid without a lower source cutoff.

| Intermediate fact | Theorem | Inputs and discharge |
|---|---|---|
| Left extreme estimate at every positive source depth | Strengthened `PowerProfile.left_extreme_insertion_Lp` | Replaces the unnecessary source-H premise by the proof Nat.cast_nonneg i.val; the exponential moment and absorption arguments are unchanged. |
| Full envelope and local weighted rows without the source cutoff | Strengthened `left_insertion_Lp_envelope`, `left_corner_weighted_insertion_row`, `left_corner_all_targets_weighted_row` | Removes the redundant source premise throughout the proof chain. The all-target result now quantifies C,delta,N only; its target partition still constructs H internally. The existing `left_insertion_moments` proof is adapted to the stronger envelope without changing its statement. |
| Left-endpoint weighted row for every source | `PowerProfile.left_endpoint_weighted_insertion_row` in `Section6LeftEndpointWeightedRow` | Splits sources at the derived local source-block cutoff. Applies the local all-target row or the outside-source all-target row, constructs a common target block and large-row threshold, and bounds both cases by the sum of constants. Every source label and every positive depth in the target block is covered. |

The previously recorded small-left-source gap is resolved by this stronger proof. This does not finish the full matrix: right-endpoint, middle/cross-region contributions and the complete global construction remain obligations, followed by the main CLTs. The strengthened proof chain and insertion-moments compatibility rebuild passed, as did the new theorem's individual compilation. The full default build passed with 4196 jobs after a compiled-file read failure was resolved by sequential branch builds. The regenerated type and transitive-axiom audit passed for all 517 source theorems, with only propext, Classical.choice and Quot.sound; all 13 frozen files are unchanged. The complete elaborated new endpoint theorem type was inspected and has no source restriction.

### Right-corner weighted assembly

| Intermediate fact | Theorem | Inputs and discharge |
|---|---|---|
| Right extreme estimate without a separate source-depth cutoff | Strengthened `PowerProfile.right_extreme_insertion_Lp` | Every source has positive depth. The proved extreme-ratio support gives source depth>target depth; the target cutoff therefore discharges the source cutoff of the survival estimate. No source cutoff is assumed in the strengthened statement. |
| Right envelope without the source cutoff | Strengthened `PowerProfile.right_insertion_Lp_envelope` | Both moderate and extreme branches apply without a lower source cutoff. The existing right insertion-moments proof is adapted to this stronger helper without changing its statement. |
| Full right envelope weighted sum | `right_envelope_weighted_sum_bound` in `Section6RightEnvelopeWeightedSum` | Adds the proved ordinary and exceptional sums for 0<=kappa<beta and positive envelope parameters; no summability premise is assumed. |
| Actual right-corner weighted insertion row | `PowerProfile.right_corner_weighted_insertion_row` in `Section6RightCornerWeightedRow` | Constructs H and delta and chooses v=1 internally. Proves terminal-depth injectivity and the exact demanded-rank identity n-terminalDepth(j)=j.val. Bounded shifts/deletions and target depth>=H prove finite membership of natural gap indices. The ordinary and exceptional weighted estimates discharge the full envelope sum. Every positive source depth in the block is covered. |
| Fixed terminal targets with arbitrary natural gaps | `right_fixed_targets_weighted_row` in `Section6RightFixedWeightedRow` | Uses the proved insertion norm<=1, source depth>=1, target depth<=H and kappa>=0. Proves the target-subset cardinality bound and the bounded weight. No endpoint/profile assumption or finite-gap premise is needed; includes the infinite final gap. This estimate addresses weighted rows only. |
| Every positive target depth for right-corner sources | `PowerProfile.right_corner_all_targets_weighted_row` in `Section6RightCornerAllTargets` | Chooses a natural bound above the moderate cutoff, partitions targets, and combines the fixed-target and full moderate-envelope rows. There is no lower source or target cutoff; only membership in the derived right source/target blocks remains. |

Outside-right sources and other global regions remain obligations. The fixed-target norm<=1 argument does not establish the stronger column or target-maximum bounds needed elsewhere. The strengthened proof chain, compatibility rebuild and all four new individual statements passed. After compiled-file read failures during concurrent compilation, sequential branch builds and the full default build succeeded with 4200 jobs. The regenerated type and transitive-axiom audit passed for all 521 source theorems, with only propext, Classical.choice and Quot.sound and all 13 frozen files unchanged. The complete elaborated all-target right-corner theorem was inspected, including its source-block condition and natural gap indices.
