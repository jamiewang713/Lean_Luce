import Luce.EndpointShellCompatibility
import Luce.EndpointJensen
import Luce.EndpointBlockExpectation
import Luce.EndpointShellBufferLimit
import Luce.ShellContractRepresentation

/-! Completed foundations for the shell migration. This is not a proof of
`ShellMigrationContract.section4`: endpoint expectation tightness and the
concrete convergence arguments remain unresolved. -/
